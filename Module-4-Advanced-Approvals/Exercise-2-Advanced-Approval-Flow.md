# Exercise 2: Advanced Approval Flow (Purchase Order/Leave Request)

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Approval Flow ที่รองรับหลายระดับ (Multi-Level Approval)
- จัดการ Approval Response (อนุมัติ/ปฏิเสธ)
- ส่ง Notification ไปยังผู้เกี่ยวข้อง
- จัดการ Error Cases

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (ต้องมี License สำหรับ Approvals)
- SharePoint List หรือ Manual Trigger สำหรับทดสอบ
- Email Address สำหรับทดสอบ

---

## 🚀 ขั้นตอนการปฏิบัติ: Purchase Order Approval

### ขั้นตอนที่ 1: สร้าง Flow ใหม่

1. เข้าสู่ **Power Automate**
2. คลิก **Create** → เลือก **Automated cloud flow**
3. ตั้งชื่อ Flow: `Exercise 2 - Purchase Order Approval`
4. เลือก Trigger: **Manually trigger a flow** (สำหรับทดสอบ)
5. คลิก **Create**

### ขั้นตอนที่ 2: สร้าง Input Parameters

1. เพิ่ม Action: **Initialize variable**
   - **Name**: `poAmount`
   - **Type**: `Integer`
   - **Value**: `15000` (สำหรับทดสอบ)

2. เพิ่ม Action: **Initialize variable**
   - **Name**: `poNumber`
   - **Type**: `String`
   - **Value**: `PO-2024-001`

3. เพิ่ม Action: **Initialize variable**
   - **Name**: `requestorEmail`
   - **Type**: `String`
   - **Value**: `your-email@example.com` (เปลี่ยนเป็น Email ของคุณ)

### ขั้นตอนที่ 3: กำหนดระดับการอนุมัติ (Approval Level)

1. เพิ่ม Action: **Initialize variable**
   - **Name**: `approvalLevel`
   - **Type**: `String`
   - **Value**: คลิก **Expression** และพิมพ์:

```
if(less(variables('poAmount'), 10000), 
   'Level1', 
   if(less(variables('poAmount'), 100000), 
      'Level2', 
      'Level3'))
```

> 💡 **คำอธิบาย**: 
> - ถ้า PO < 10,000 → Level 1 (Department Manager)
> - ถ้า PO >= 10,000 และ < 100,000 → Level 2 (Finance Manager)
> - ถ้า PO >= 100,000 → Level 3 (CFO)

### ขั้นตอนที่ 4: Level 1 Approval (Department Manager)

1. เพิ่ม Action: **Condition**
2. ตั้งค่า:
   - **Condition**: `approvalLevel` `is equal to` `Level1` หรือ `Level2` หรือ `Level3`
   - (ใช้ `or` เพื่อรวมทุกกรณี)

3. ในช่อง **If yes**:
   - เพิ่ม Action: **Start and wait for an approval**
   - ตั้งค่า:
     - **Approval type**: `Approve/Reject - First to respond`
     - **Title**: `คำขออนุมัติ Purchase Order: @{variables('poNumber')}`
     - **Assigned to**: ใส่ Email ของ Manager (หรือ Email ของคุณเองสำหรับทดสอบ)
     - **Details**: 
```
หมายเลข PO: @{variables('poNumber')}
จำนวนเงิน: ฿@{variables('poAmount')}
ผู้ขออนุมัติ: @{variables('requestorEmail')}
```

### ขั้นตอนที่ 5: ตรวจสอบ Response จาก Level 1

1. เพิ่ม Action: **Condition** (ภายหลัง Approval Action)
2. ตั้งค่า:
   - **Condition**: `Response` (จาก Approval) `is equal to` `Approve`

3. ในช่อง **If no** (ถ้าปฏิเสธ):
   - เพิ่ม Action: **Send an email (V2)**
   - ตั้งค่า:
     - **To**: `variables('requestorEmail')`
     - **Subject**: `คำขอ PO ถูกปฏิเสธ`
     - **Body**: `คำขอ Purchase Order @{variables('poNumber')} ถูกปฏิเสธ`
   - เพิ่ม Action: **Terminate**
     - **Status**: `Failed`

4. ในช่อง **If yes** (ถ้าอนุมัติ):
   - ตรวจสอบว่าต้องไป Level 2 หรือไม่

### ขั้นตอนที่ 6: Level 2 Approval (ถ้าจำเป็น)

1. เพิ่ม Action: **Condition** (ในช่อง If yes ของ Level 1)
2. ตั้งค่า:
   - **Condition**: `variables('poAmount')` `is greater than or equal to` `10000`

3. ในช่อง **If yes**:
   - เพิ่ม Action: **Start and wait for an approval**
   - ตั้งค่า:
     - **Approval type**: `Approve/Reject - First to respond`
     - **Title**: `คำขออนุมัติ PO (Level 2): @{variables('poNumber')}`
     - **Assigned to**: Finance Manager Email
     - **Details**: รวมข้อมูลจาก Level 1

4. ตรวจสอบ Response เหมือน Level 1

### ขั้นตอนที่ 7: Level 3 Approval (ถ้าจำเป็น)

1. เพิ่ม Action: **Condition** (ในช่อง If yes ของ Level 2)
2. ตั้งค่า:
   - **Condition**: `variables('poAmount')` `is greater than or equal to` `100000`

3. ในช่อง **If yes**:
   - เพิ่ม Action: **Start and wait for an approval**
   - ตั้งค่า:
     - **Approval type**: `Approve/Reject - First to respond`
     - **Title**: `คำขออนุมัติ PO (Level 3 - CFO): @{variables('poNumber')}`
     - **Assigned to**: CFO Email

### ขั้นตอนที่ 8: Final Processing (เมื่ออนุมัติทั้งหมด)

1. เพิ่ม Action: **Send an email (V2)**
   - **To**: `variables('requestorEmail')`
   - **Subject**: `✅ PO @{variables('poNumber')} ได้รับการอนุมัติแล้ว`
   - **Body**: 
```
ขอแสดงความยินดี!

Purchase Order @{variables('poNumber')} 
จำนวนเงิน ฿@{variables('poAmount')}
ได้รับการอนุมัติแล้ว

คุณสามารถดำเนินการสั่งซื้อได้เลย
```

### ขั้นตอนที่ 9: Error Handling

1. คลิกที่ **Start and wait for an approval** Action
2. คลิก **...** (สามจุด) → **Configure run after**
3. เลือก:
   - ✅ **is failed**
   - ✅ **is skipped**
   - ✅ **is timed out**

4. เพิ่ม Action: **Send an email (V2)**
   - **To**: `variables('requestorEmail')`
   - **Subject**: `⚠️ เกิดปัญหาในการอนุมัติ PO`
   - **Body**: `เกิดปัญหาในการอนุมัติ Purchase Order @{variables('poNumber')} กรุณาติดต่อผู้ดูแลระบบ`

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **Multi-Level Approval**: สร้าง Approval หลายระดับตามเงื่อนไข  
✅ **Approval Response**: จัดการเมื่ออนุมัติ/ปฏิเสธ  
✅ **Conditional Logic**: ใช้ Condition เพื่อตัดสินใจ  
✅ **Error Handling**: จัดการเมื่อเกิดปัญหา  
✅ **Notifications**: ส่ง Email แจ้งเตือนผู้เกี่ยวข้อง  

---

## 💡 Tips & Tricks

1. **สำหรับการทดสอบ**: ใช้ Email ของตัวเองเป็น Approver
2. **Approval Timeout**: ตั้งค่า Due Date ใน Approval Action
3. **Custom Responses**: ใช้ "Custom responses" สำหรับตัวเลือกหลายแบบ

---

## 🔄 แบบฝึกหัดเพิ่มเติม

### แบบฝึกหัด A: Leave Request Approval

สร้าง Flow สำหรับการอนุมัติการลาของพนักงาน:
1. ตรวจสอบ Leave Balance
2. ส่ง Approval ไปยัง Manager
3. ถ้าอนุมัติ: อัพเดท Leave Balance
4. ส่ง Email Confirmation

### แบบฝึกหัด B: Parallel Approval

สร้าง Flow ที่ส่ง Approval ไปยัง Manager หลายคนพร้อมกัน:
- ใครตอบก่อนถือว่าใช้ได้ (First to Respond)
- หรือ ต้องอนุมัติทั้งหมด (All Must Approve)

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: ต้องใช้ License พิเศษสำหรับ Approvals หรือไม่?**  
A: ใช่ ต้องมี Power Automate Premium License

**Q: สามารถอนุมัติผ่าน Mobile ได้หรือไม่?**  
A: ได้ ผ่าน Power Automate Mobile App

**Q: ถ้า Approval หมดเวลา (Timeout) จะทำอย่างไร?**  
A: ใช้ Configure Run After → is timed out เพื่อจัดการ

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การสร้าง Multi-Level Approval
- การจัดการ Approval Response
- การส่ง Notification
- Error Handling

พร้อมสำหรับการสร้าง Approval Flow ที่ซับซ้อนแล้ว! 🎉



