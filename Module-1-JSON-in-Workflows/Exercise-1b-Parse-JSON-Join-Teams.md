# Exercise 1b: Parse JSON → Loop → JOIN → Teams Messaging

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่รับข้อมูล JSON
- แปลง JSON String เป็น Object ที่ใช้งานได้ (Parse JSON)
- วนลูปผ่าน Array เพื่อสร้างข้อความ
- ใช้ JOIN เพื่อรวมข้อความทั้งหมดเป็นข้อความเดียว
- ส่งข้อความรวมไปยัง Teams Chat/Channel

> 💡 **หมายเหตุ**: Exercise นี้แตกต่างจาก Exercise 1a ที่การแสดงผล - แทนที่จะส่งข้อความทีละรายการ จะรวมข้อความทั้งหมดด้วย JOIN แล้วส่งเป็นข้อความเดียว

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- ความรู้พื้นฐานเรื่อง JSON (ถ้ายังไม่รู้ จะสอนในขั้นตอน)
- ควรทำ Exercise 1a เสร็จก่อน (เพื่อเข้าใจพื้นฐาน Parse JSON และ Loop)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: สร้าง Flow ใหม่และตั้งค่า JSON Input

1. เข้าสู่ **Power Automate** (https://powerautomate.microsoft.com)
2. คลิก **Create** → เลือก **Instant cloud flow**
3. ตั้งชื่อ Flow: `Exercise 1b - Parse JSON Join Teams`
4. เลือก Trigger: **Manually trigger a flow**
5. คลิก **Create**
6. ใน **Manually trigger a flow** คลิก **Add an input** → เลือก **JSON**
7. ตั้งชื่อ Input: `JSON input` (หรือชื่ออื่นตามต้องการ)

> 💡 **หมายเหตุ**: JSON Input จะให้คุณสามารถป้อนข้อมูล JSON เมื่อรัน Flow ได้

### ขั้นตอนที่ 2: Parse JSON (แปลง JSON String เป็น Object)

**ก่อนเริ่ม**: เตรียมข้อมูล JSON ตัวอย่างด้านล่างไว้สำหรับทดสอบ:

```json
{
  "year": 2025,
  "employees": [
    {
      "id": 1,
      "name": "สมชาย ใจดี",
      "department": "ไอที",
      "salary": 50000
    },
    {
      "id": 2,
      "name": "สมหญิง รักงาน",
      "department": "ทรัพยากรบุคคล",
      "salary": 45000
    },
    {
      "id": 3,
      "name": "วิชัย ทำงานดี",
      "department": "การเงิน",
      "salary": 55000
    },
    {
      "id": 4,
      "name": "วิไล เก่งมาก",
      "department": "ไอที",
      "salary": 60000
    }
  ]
}
```

> 💡 **หมายเหตุ**: JSON นี้มีโครงสร้าง 2 ชั้น:
> - **ชั้นที่ 1**: Object หลักที่มี Key `year` (ปี) และ `employees` (Array ของพนักงาน)
> - **ชั้นที่ 2**: แต่ละ Object ใน Array `employees` มี Key `id`, `name`, `department`, `salary`

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Parse JSON**
3. ตั้งค่า:
   - **Content**: เลือก `JSON input` จาก Dynamic Content (จาก Manual Trigger)
   - **Schema**: คลิก **Use sample payload to generate schema**
   - วาง JSON ตัวอย่างด้านบนลงไป
   - คลิก **Done**

> 💡 **คำอธิบาย**: Parse JSON จะแปลง JSON String ให้เป็น Object ที่ Power Automate เข้าใจและสามารถใช้งานได้

**หลังจาก Parse JSON แล้ว คุณสามารถเข้าถึงข้อมูลได้:**

**ชั้นที่ 1 (Root Level):**
- `body('Parse_JSON')?['year']` → `2025` (ปี)
- `body('Parse_JSON')?['employees']` → Array ของพนักงานทั้งหมด

**ชั้นที่ 2 (Array Level):**
- `body('Parse_JSON')?['employees'][0]` → พนักงานคนแรก (index 0)
- `body('Parse_JSON')?['employees'][0]?['name']` → `"สมชาย ใจดี"`
- `body('Parse_JSON')?['employees'][0]?['department']` → `"ไอที"`
- `body('Parse_JSON')?['employees'][0]?['salary']` → `50000`

**ตัวอย่างการเข้าถึงข้อมูล 2 ชั้น:**
```
ชั้นที่ 1:
- year → 2025
- employees → [Array ของพนักงาน 4 คน]

ชั้นที่ 2 (employees[0]):
- id → 1
- name → "สมชาย ใจดี"
- department → "ไอที"
- salary → 50000

employees[1]:
- name → "สมหญิง รักงาน"
- department → "ทรัพยากรบุคคล"
- salary → 45000
```

### ขั้นตอนที่ 3: ส่งข้อความเริ่มต้น (Post Message - นอก Loop)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Message**: ใช้ Dynamic Content หรือ Expression เพื่อสร้างข้อความ:
     ```
     📋 เริ่มต้นส่งข้อมูลพนักงานปี body('Parse_JSON')?['year']
     ```
     หรือใช้ Expression:
     ```
     concat('📋 เริ่มต้นส่งข้อมูลพนักงานปี ', body('Parse_JSON')?['year'])
     ```

> 💡 **คำอธิบาย**: Action นี้จะส่งข้อความก่อนเริ่มวนลูป เพื่อแจ้งเตือนว่ากำลังเริ่มกระบวนการ

### ขั้นตอนที่ 4: วนลูปผ่าน Array และสร้างข้อความ (Apply to Each)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Apply to each**
3. ตั้งค่า:
   - **Select an output from previous steps**: เลือก `Body employees` จาก Parse JSON output

> 💡 **คำอธิบาย**: Apply to Each จะวนลูปผ่านทุก item ใน Array `employees` (ในที่นี้มี 4 คน)

**Loop through items using Apply to Each:**

**item()** = ข้อมูลในรอบปัจจุบัน
- รอบที่ 1: `item()` = employees[0] = { "id": 1, "name": "สมชาย ใจดี", ... }
- รอบที่ 2: `item()` = employees[1] = { "id": 2, "name": "สมหญิง รักงาน", ... }
- รอบที่ 3: `item()` = employees[2] = { "id": 3, "name": "วิชัย ทำงานดี", ... }
- รอบที่ 4: `item()` = employees[3] = { "id": 4, "name": "วิไล เก่งมาก", ... }

**Access Values ใน Loop:**
- `item()?['name']` → ชื่อพนักงานในรอบปัจจุบัน
- `item()?['department']` → แผนกในรอบปัจจุบัน
- `item()?['salary']` → เงินเดือนในรอบปัจจุบัน

**ตัวอย่างการใช้งาน:**
```
Apply to Each: body('Parse_JSON')?['employees']
  - item()?['name']       → รอบที่ 1: "สมชาย ใจดี", รอบที่ 2: "สมหญิง รักงาน", ...
  - item()?['department'] → รอบที่ 1: "ไอที", รอบที่ 2: "ทรัพยากรบุคคล", ...
  - item()?['salary']     → รอบที่ 1: 50000, รอบที่ 2: 45000, ...
```

### ขั้นตอนที่ 5: สร้างข้อความในแต่ละรอบ (Append to string variable - ใน Loop)

**ก่อนเริ่ม**: ต้องสร้าง Variable สำหรับเก็บข้อความ

1. **ก่อน Apply to Each** เพิ่ม Action: คลิก **+ New step** (ก่อน Apply to Each)
2. ค้นหาและเลือก **Initialize variable**
3. ตั้งค่า:
   - **Name**: `messageList`
   - **Type**: `Array`
   - **Value**: `[]` (Array ว่าง)

> 💡 **หมายเหตุ**: เราจะเก็บข้อความแต่ละรายการไว้ใน Array แล้วใช้ JOIN ทีหลัง

4. **ภายใน Apply to Each** คลิก **Add an action**
5. ค้นหาและเลือก **Append to array variable**
6. ตั้งค่า:
   - **Name**: เลือก `messageList` จาก Variable
   - **Value**: ใช้ Expression เพื่อสร้างข้อความ:
     ```
     concat('👤 ', item()?['name'], ' | แผนก: ', item()?['department'], ' | เงินเดือน: ', item()?['salary'])
     ```

> 💡 **คำอธิบาย**: Action นี้จะเพิ่มข้อความของแต่ละพนักงานเข้าไปใน Array `messageList`

**ผลลัพธ์ใน Array `messageList` หลังจาก Loop เสร็จ:**
```
[
  "👤 สมชาย ใจดี | แผนก: ไอที | เงินเดือน: 50000",
  "👤 สมหญิง รักงาน | แผนก: ทรัพยากรบุคคล | เงินเดือน: 45000",
  "👤 วิชัย ทำงานดี | แผนก: การเงิน | เงินเดือน: 55000",
  "👤 วิไล เก่งมาก | แผนก: ไอที | เงินเดือน: 60000"
]
```

### ขั้นตอนที่ 6: ใช้ JOIN เพื่อรวมข้อความทั้งหมด

1. **หลังจาก Apply to Each** เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Compose** (หรือ **Data operations** → **Compose**)
3. ตั้งค่า:
   - **Inputs**: คลิก **Expression** และพิมพ์:
     ```
     join(variables('messageList'), '\n')
     ```

> 💡 **คำอธิบาย**: `join()` จะรวม Array ทั้งหมดเป็น String โดยใช้ `\n` (newline) เป็นตัวคั่นระหว่างแต่ละข้อความ

**ผลลัพธ์ของ JOIN:**
```
👤 สมชาย ใจดี | แผนก: ไอที | เงินเดือน: 50000
👤 สมหญิง รักงาน | แผนก: ทรัพยากรบุคคล | เงินเดือน: 45000
👤 วิชัย ทำงานดี | แผนก: การเงิน | เงินเดือน: 55000
👤 วิไล เก่งมาก | แผนก: ไอที | เงินเดือน: 60000
```

**ตัวเลือกอื่นๆ สำหรับ JOIN:**
- `join(variables('messageList'), ', ')` → ใช้ comma และ space คั่น
- `join(variables('messageList'), ' | ')` → ใช้ pipe คั่น
- `join(variables('messageList'), '\n\n')` → ใช้ newline 2 บรรทัดคั่น (เว้นบรรทัด)

### ขั้นตอนที่ 7: ส่งข้อความรวมทั้งหมด (Post Message)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: เลือก Email address เดียวกัน (หรือใช้ Dynamic Content)
   - **Message**: เลือก `Outputs` จาก **Compose** Action (ผลลัพธ์ของ JOIN)

> 💡 **คำอธิบาย**: Action นี้จะส่งข้อความรวมทั้งหมดเป็นข้อความเดียว แทนการส่งทีละรายการ

### ขั้นตอนที่ 8: ทดสอบ Flow

1. คลิก **Save**
2. คลิก **Test** → **Manually** → **Run flow**
3. ในหน้า **Run flow**:
   - วาง JSON ตัวอย่างด้านบนลงในช่อง **JSON input**
   - คลิก **Run flow**
4. รอให้ Flow รันเสร็จ
5. ตรวจสอบข้อความใน Teams Chat:
   - ข้อความแรก: `📋 เริ่มต้นส่งข้อมูลพนักงานปี 2025`
   - ข้อความที่สอง: ข้อความรวมทั้งหมด 4 รายการในข้อความเดียว

---

## 📊 ผลลัพธ์ที่คาดหวัง

คุณควรจะเห็นข้อความใน Teams Chat:
- **ข้อความที่ 1**: ข้อความเริ่มต้นพร้อมปี (เช่น `📋 เริ่มต้นส่งข้อมูลพนักงานปี 2025`)
- **ข้อความที่ 2**: ข้อความรวมทั้งหมด 4 รายการในข้อความเดียว แสดงเป็น:
  ```
  👤 สมชาย ใจดี | แผนก: ไอที | เงินเดือน: 50000
  👤 สมหญิง รักงาน | แผนก: ทรัพยากรบุคคล | เงินเดือน: 45000
  👤 วิชัย ทำงานดี | แผนก: การเงิน | เงินเดือน: 55000
  👤 วิไล เก่งมาก | แผนก: ไอที | เงินเดือน: 60000
  ```

**ข้อแตกต่างจาก Exercise 1a:**
- **Exercise 1a**: ส่งข้อความ 5 ข้อความ (1 ข้อความเริ่มต้น + 4 ข้อความแยก)
- **Exercise 1b**: ส่งข้อความ 2 ข้อความ (1 ข้อความเริ่มต้น + 1 ข้อความรวม)

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **JSON Basics**: เข้าใจโครงสร้าง JSON Objects และ Arrays (2 ชั้น)  
✅ **Parse JSON**: แปลง JSON String เป็น Object ที่ใช้งานได้ และเข้าถึงข้อมูลหลายชั้น  
✅ **Apply to Each**: วนลูปผ่าน Array เพื่อสร้าง Array ของข้อความ  
✅ **Append to array variable**: เพิ่มข้อมูลเข้าไปใน Array ในแต่ละรอบของ Loop  
✅ **JOIN Function**: รวม Array เป็น String โดยใช้ตัวคั่น (delimiter)  
✅ **Teams Integration**: ส่งข้อความรวมไปยัง Teams Chat/Channel  

---

## 💡 Tips & Tricks

1. **JOIN กับ Apply to Each ต่างกันอย่างไร?**
   - **Apply to Each**: ใช้ทำอะไรบางอย่างกับแต่ละ Item (เช่น สร้างข้อความ)
   - **JOIN**: ใช้รวม Array ทั้งหมดเป็น String เดียว

2. **ตัวคั่น (Delimiter) ที่ใช้ใน JOIN:**
   - `'\n'` → ขึ้นบรรทัดใหม่ (newline)
   - `', '` → comma และ space
   - `' | '` → pipe และ space
   - `'\n\n'` → ขึ้นบรรทัดใหม่ 2 บรรทัด (เว้นบรรทัด)

3. **ทำไมต้องใช้ Array Variable?**
   - เพราะ Apply to Each ทำงานทีละรายการ ต้องเก็บข้อความไว้ใน Array ก่อน แล้วค่อย JOIN ทีหลัง

4. **เมื่อไหร่ควรใช้ JOIN แทนการส่งทีละรายการ?**
   - เมื่อต้องการส่งข้อความรวมทั้งหมดเป็นข้อความเดียว
   - เมื่อต้องการลดจำนวนข้อความที่ส่ง (เช่น ส่ง 1 ข้อความแทน 4 ข้อความ)
   - เมื่อต้องการแสดงผลในรูปแบบที่อ่านง่ายขึ้น

5. **ถ้าต้องการ Format เงินเดือน**: ใช้ Expression ใน Append to array:
   ```
   concat('👤 ', item()?['name'], ' | แผนก: ', item()?['department'], ' | เงินเดือน: ฿', formatNumber(item()?['salary'], '#,##0'))
   ```

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **เปลี่ยนตัวคั่น**: ลองใช้ตัวคั่นอื่นๆ เช่น `' | '`, `', '`, หรือ `'\n\n'`

2. **เพิ่ม Header**: เพิ่ม Header ก่อนข้อความรวม:
   ```
   join(concat('📋 รายชื่อพนักงานทั้งหมด:\n\n', join(variables('messageList'), '\n')), '')
   ```

3. **ใช้ Select แทน Append**: ใช้ Select Action เพื่อสร้าง Array แล้วค่อย JOIN:
   - ใช้ Select แทน Append to array variable
   - ตั้งค่า Select: `concat('👤 ', item()?['name'], ' | แผนก: ', item()?['department'], ' | เงินเดือน: ', item()?['salary'])`

4. **เพิ่ม Footer**: เพิ่ม Footer หลังข้อความรวม เช่น จำนวนพนักงานทั้งหมด

5. **Format เป็น List**: ใช้ JOIN กับ numbering:
   ```
   join(select(variables('messageList'), concat(outputs('Compose'), ' ', item())), '\n')
   ```

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: JOIN กับ Apply to Each ต่างกันอย่างไร?**  
A: Apply to Each ใช้ทำอะไรบางอย่างกับแต่ละ Item, JOIN ใช้รวม Array ทั้งหมดเป็น String เดียว

**Q: ทำไมต้องใช้ Array Variable?**  
A: เพราะ Apply to Each ทำงานทีละรายการ ต้องเก็บข้อความไว้ใน Array ก่อน แล้วค่อย JOIN ทีหลัง

**Q: ตัวคั่น (Delimiter) คืออะไร?**  
A: ตัวคั่นคือตัวอักษรหรือสัญลักษณ์ที่ใช้แยกระหว่างแต่ละรายการใน String เช่น `, `, `\n`, ` | `

**Q: เมื่อไหร่ควรใช้ JOIN แทนการส่งทีละรายการ?**  
A: เมื่อต้องการส่งข้อความรวมทั้งหมดเป็นข้อความเดียว หรือต้องการลดจำนวนข้อความที่ส่ง

**Q: JOIN ทำงานกับ Array อะไรได้บ้าง?**  
A: JOIN ทำงานกับ Array ของ String, Number, หรือ Object ที่แปลงเป็น String แล้ว

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การรับและ Parse JSON
- การวนลูปผ่าน Array ด้วย Apply to Each
- การสร้าง Array ของข้อความด้วย Append to array variable
- การใช้ JOIN เพื่อรวม Array เป็น String
- การส่งข้อความรวมไปยัง Teams Chat/Channel

**ข้อแตกต่างจาก Exercise 1a:**
- **Exercise 1a**: ส่งข้อความทีละรายการ (5 ข้อความ)
- **Exercise 1b**: รวมข้อความทั้งหมดด้วย JOIN แล้วส่งเป็นข้อความเดียว (2 ข้อความ)

พร้อมสำหรับบทต่อไปแล้ว! 🎉

