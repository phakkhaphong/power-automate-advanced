# บทที่ 1: JSON in Workflows – The Core of Power Automate

## 🎯 ภาพรวม

บทนี้จะช่วยให้คุณเข้าใจว่า **JSON** คืออะไร และทำไมมันถึงสำคัญมากใน Power Automate

**JSON** (JavaScript Object Notation) คือรูปแบบการเก็บข้อมูลที่คอมพิวเตอร์เข้าใจง่าย เปรียบเหมือนภาษาโปรแกรมที่ใช้สื่อสารระหว่างระบบต่างๆ

> 💡 **คิดง่ายๆ**: JSON ก็เหมือนตาราง Excel ที่เก็บข้อมูลเป็นโครงสร้าง แต่เขียนด้วยโค้ด

---

## 📚 เนื้อหาการเรียนรู้

### 1. JSON Basics: Objects, Arrays, Key–Value

#### JSON Objects (วัตถุ JSON)

**JSON Object** คือการเก็บข้อมูลแบบคู่ **Key-Value** (คีย์-ค่า)

**ตัวอย่างง่ายๆ**:
```json
{
  "name": "สมชาย",
  "age": 20,
  "city": "กรุงเทพ"
}
```

**อธิบาย**:
- `"name"` = **Key** (ชื่อของข้อมูล)
- `"สมชาย"` = **Value** (ค่าของข้อมูล)
- `{ }` = วงเล็บปีกกา หมายถึง Object

**การเข้าถึงข้อมูล**:
- `object.name` → จะได้ `"สมชาย"`
- `object["name"]` → เหมือนกัน แต่ใช้แบบนี้ก็ได้

**Nested Objects** (วัตถุซ้อนกัน):
```json
{
  "student": {
    "name": "สมชาย",
    "address": {
      "street": "ถนนสุขุมวิท",
      "district": "คลองตัน"
    }
  }
}
```

#### JSON Arrays (อาร์เรย์ JSON)

**Array** คือรายการข้อมูลหลายตัว เรียงเป็นแถว

**ตัวอย่างง่ายๆ**:
```json
["แอปเปิ้ล", "ส้ม", "กล้วย"]
```

**Array of Objects** (อาร์เรย์ของวัตถุ):
```json
[
  { "id": 1, "name": "สมชาย" },
  { "id": 2, "name": "สมหญิง" },
  { "id": 3, "name": "วิชัย" }
]
```

**การเข้าถึงข้อมูล**:
- `array[0]` → ข้อมูลตัวแรก (index เริ่มจาก 0)
- `array[1]` → ข้อมูลตัวที่สอง

#### Key–Value Pairs (คู่คีย์-ค่า)

**Key-Value** คือการจับคู่ระหว่าง **ชื่อ** กับ **ค่า**

**ตัวอย่างในชีวิตจริง**:
- หนังสือ: ชื่อหนังสือ (Key) = เนื้อหา (Value)
- พจนานุกรม: คำ (Key) = ความหมาย (Value)

**ใน Power Automate**:
- Dynamic Content จาก Actions ก่อนหน้า = Value
- ชื่อ Field = Key

---

### 2. Parse JSON → Extract & Loop Through Arrays

#### Parse JSON Action (แปลง JSON String)

**ปัญหาที่พบบ่อย**: 
เมื่อเราได้รับข้อมูล JSON มา มันมักจะเป็น **String** (ข้อความ) ต้องแปลงให้เป็น **Object** (วัตถุ) ก่อนใช้งาน

**เปรียบเทียบ**:
- **JSON String**: `"{'name': 'สมชาย'}"` ← เป็นข้อความ
- **JSON Object**: `{name: 'สมชาย'}` ← เป็นข้อมูลที่ใช้งานได้

**วิธีการ**:
1. ใช้ Action **Parse JSON**
2. ใส่ JSON String ที่ต้องการแปลง
3. กำหนด **Schema** (โครงสร้าง) ของ JSON

**Schema Generation**:
- Power Automate สามารถสร้าง Schema อัตโนมัติได้
- ใส่ JSON ตัวอย่าง → คลิก "Generate from sample" → เสร็จ!

**Manual Schema**:
- ถ้าต้องการความแม่นยำ ใส่ Schema เองได้

#### Use Parse JSON to Extract items[]

**ตัวอย่าง JSON Input:**
```json
{
  "orderId": "ORD-001",
  "items": [
    { "product": "แอปเปิ้ล", "quantity": 5, "price": 30 },
    { "product": "ส้ม", "quantity": 3, "price": 25 },
    { "product": "กล้วย", "quantity": 10, "price": 20 }
  ]
}
```

**ขั้นตอน**:
1. ใช้ **Parse JSON** Action
2. ใส่ JSON String ใน Content field
3. Generate Schema จาก sample payload

**หลัง Parse JSON แล้ว:**
- `body('Parse_JSON')?['orderId']` → `"ORD-001"`
- `body('Parse_JSON')?['items']` → Array ของ items

**Extract items[]:**
```
body('Parse_JSON')?['items']
```

**ผลลัพธ์**:
```json
[
  { "product": "แอปเปิ้ล", "quantity": 5, "price": 30 },
  { "product": "ส้ม", "quantity": 3, "price": 25 },
  { "product": "กล้วย", "quantity": 10, "price": 20 }
]
```

#### Access Values Using Index (เข้าถึงข้อมูลด้วย Index)

**หลังจาก Parse JSON แล้ว:**

**Access items โดยใช้ Index:**
- `body('Parse_JSON')?['items'][0]` → Item แรก (index 0)
- `body('Parse_JSON')?['items'][1]` → Item ที่สอง (index 1)
- `body('Parse_JSON')?['items'][2]` → Item ที่สาม (index 2)

**Access Properties ใน Object:**
- `body('Parse_JSON')?['items'][0]?['product']` → `"แอปเปิ้ล"`
- `body('Parse_JSON')?['items'][0]?['quantity']` → `5`
- `body('Parse_JSON')?['items'][0]?['price']` → `30`

**ตัวอย่าง:**
```
items[0].product  → "แอปเปิ้ล"
items[0].quantity → 5
items[0].price    → 30

items[1].product  → "ส้ม"
items[1].quantity → 3
items[1].price    → 25
```

**Power Automate Expression:**
```
body('Parse_JSON')?['items'][0]?['product']
body('Parse_JSON')?['items'][1]?['quantity']
```

#### Extract Data from Arrays (ดึงข้อมูลจากอาร์เรย์)

**Loop through items using Apply to Each**

**ขั้นตอน**:
1. ใช้ **Parse JSON** เพื่อ extract `items[]`
2. ใช้ **Apply to Each** เพื่อวนลูปผ่าน items

**ตัวอย่าง**:
ถ้าเรามี items 3 รายการ และต้องการประมวลผลแต่ละ item
- ใช้ **Apply to Each** วนลูป 3 ครั้ง
- แต่ละครั้งจะประมวลผล 1 item

**item()** = ข้อมูลในรอบปัจจุบัน

**ตัวอย่าง**:
```
Apply to Each: body('Parse_JSON')?['items']
  - รอบที่ 1: item() = items[0] = { "product": "แอปเปิ้ล", "quantity": 5, "price": 30 }
  - รอบที่ 2: item() = items[1] = { "product": "ส้ม", "quantity": 3, "price": 25 }
  - รอบที่ 3: item() = items[2] = { "product": "กล้วย", "quantity": 10, "price": 20 }
```

**Access Values ใน Loop:**
- `item()?['product']` → ชื่อสินค้าในรอบปัจจุบัน
- `item()?['quantity']` → จำนวนในรอบปัจจุบัน
- `item()?['price']` → ราคาในรอบปัจจุบัน

**ตัวอย่างการใช้งาน:**
```
Apply to Each: body('Parse_JSON')?['items']
  - item()?['product']  → รอบที่ 1: "แอปเปิ้ล", รอบที่ 2: "ส้ม", รอบที่ 3: "กล้วย"
  - item()?['quantity'] → รอบที่ 1: 5, รอบที่ 2: 3, รอบที่ 3: 10
  - item()?['price']    → รอบที่ 1: 30, รอบที่ 2: 25, รอบที่ 3: 20
```

**เปรียบเทียบ:**
- **Access by Index**: `items[0].product` → ใช้เมื่อรู้ว่าต้องการ item ไหนแน่นอน
- **Access in Loop**: `item()?['product']` → ใช้เมื่อต้องการประมวลผลทุก item

---

### 3. Filtering & Selecting Arrays

#### Filter Array Action
- เงื่อนไขการกรอง: `item()?['property'] eq 'value'`
- ใช้ Expressions สำหรับเงื่อนไขที่ซับซ้อน
- กรองหลายเงื่อนไขด้วย `and`, `or`

#### Select Action
- Transform Array: แปลง Array จากรูปแบบหนึ่งไปอีกรูปแบบหนึ่ง
- สร้าง Object ใหม่จาก Properties ที่เลือก
- ใช้ Expressions ใน Map ฟังก์ชัน

---

### 4. Expressions ที่ใช้กับ JSON

#### String Functions
- `first()`: ดึง Item แรกจาก Array
- `last()`: ดึง Item สุดท้ายจาก Array
- `join()`: รวม Array เป็น String โดยมี Delimiter

**ตัวอย่างการใช้ `join()`:**

**รวม Email Addresses:**
```json
Input Array: ["user1@example.com", "user2@example.com", "user3@example.com"]
```

**Expression:**
```
join(variables('emailList'), '; ')
```

**ผลลัพธ์:**
```
"user1@example.com; user2@example.com; user3@example.com"
```

**Syntax:**
```
join(array, delimiter)
```
- `array`: Array ที่ต้องการรวม
- `delimiter`: ตัวคั่นระหว่าง items (เช่น `,` `;` ` - `)

#### Table Functions
- `createHTMLTable()`: สร้าง HTML Table จาก Array
- `createCSVTable()`: สร้าง CSV Table จาก Array
- กำหนด Headers และ Format

**ตัวอย่างการสร้าง HTML Table:**

**Input Data (List of Objects):**
```json
[
  { "id": 1, "name": "สมชาย", "department": "ไอที", "salary": 50000 },
  { "id": 2, "name": "สมหญิง", "department": "HR", "salary": 45000 },
  { "id": 3, "name": "วิชัย", "department": "การเงิน", "salary": 55000 }
]
```

**Expression:**
```
createHTMLTable(outputs('Select')?['body'])
```

**ผลลัพธ์:** HTML Table ที่พร้อมใช้งานใน Email หรือ Web Page

**ตัวอย่างการสร้าง CSV Table:**

**Expression:**
```
createCSVTable(outputs('Select')?['body'])
```

**ผลลัพธ์:**
```csv
id,name,department,salary
1,สมชาย,ไอที,50000
2,สมหญิง,HR,45000
3,วิชัย,การเงิน,55000
```

**ใช้ CSV สำหรับ:**
- Export ข้อมูลไปยัง Excel
- Attach ไฟล์ CSV ใน Email
- ส่งข้อมูลไปยังระบบอื่น

---

### 5. ความเข้าใจ: ทุก Action = JSON Input → JSON Output

#### Core Concept
- ทุก Action รับ Input เป็น JSON
- ทุก Action ส่ง Output เป็น JSON
- Dynamic Content คือการอ้างอิง JSON Output จาก Actions ก่อนหน้า

#### Workflow Data Flow
```
Trigger → JSON Output
    ↓
Action 1 → JSON Output
    ↓
Action 2 → JSON Output (ใช้ Output จาก Action 1)
    ↓
Action N → Final JSON Output
```

---

## 📝 Lab Exercise

### Lab 1: JSON → Parse JSON → Loop & Select → HTML Table

**Exercise นี้อยู่ในไฟล์แยก**: [Exercise-1-JSON-to-HTML-Table.md](./Exercise-1-JSON-to-HTML-Table.md)

**สิ่งที่คุณจะได้ทำ**:
1. ✅ สร้าง Flow ที่รับ JSON String
2. ✅ แปลง JSON String เป็น Object ด้วย Parse JSON
3. ✅ วนลูปผ่าน Array เพื่อดึงข้อมูล
4. ✅ แปลงข้อมูลเป็น HTML Table สวยงาม

**ตัวอย่าง JSON Input**:
```json
{
  "employees": [
    {
      "id": 1,
      "name": "สมชาย ใจดี",
      "department": "ไอที",
      "salary": 50000
    },
    {
      "id": 2,
      "name": "สมหญิง รักงาน",
      "department": "ทรัพยากรบุคคล",
      "salary": 45000
    },
    {
      "id": 3,
      "name": "วิชัย ทำงานดี",
      "department": "การเงิน",
      "salary": 55000
    }
  ]
}
```

**ผลลัพธ์**: HTML Table ที่แสดงข้อมูลพนักงานทั้งหมด พร้อม Headers: ID, ชื่อ, แผนก, เงินเดือน

> 📖 **คำแนะนำ**: เปิดไฟล์ Exercise เพื่อดูขั้นตอนการทำแบบละเอียด พร้อมคำอธิบายทุกขั้นตอน

---

## 📊 สรุปบทเรียน

หลังจากเรียนจบบทนี้ คุณจะสามารถ:

- ✅ **เข้าใจโครงสร้าง JSON**: รู้ว่า JSON Objects และ Arrays ทำงานอย่างไร
- ✅ **Parse JSON**: แปลง JSON String เป็น Object ที่ใช้งานได้
- ✅ **วนลูป**: ใช้ Apply to Each เพื่อวนลูปผ่าน Arrays
- ✅ **กรองข้อมูล**: ใช้ Filter Array เพื่อเลือกข้อมูลที่ต้องการ
- ✅ **แปลงข้อมูล**: ใช้ Select เพื่อสร้าง Array ใหม่
- ✅ **สร้างตาราง**: สร้าง HTML/CSV Tables จาก JSON Data
- ✅ **เข้าใจ Flow**: เข้าใจว่า JSON ไหลผ่าน Actions อย่างไร

---

## 🎯 แบบฝึกหัดเพิ่มเติม

ลองทำแบบฝึกหัดเหล่านี้เพื่อฝึกฝน:

1. **JSON to Email**: สร้าง Flow ที่รับ JSON จาก API และแปลงเป็น HTML Email
2. **Filter Array**: กรอง Array โดยใช้เงื่อนไขหลายเงื่อนไข (เช่น เงินเดือน > 50000 และแผนก = "ไอที")
3. **Combine Arrays**: สร้าง Flow ที่รวมข้อมูลจากหลาย Arrays เข้าด้วยกัน
4. **Transform Array**: ใช้ Select เพื่อสร้าง Array ใหม่ที่มีโครงสร้างแตกต่างจากเดิม

---

## 💡 คำศัพท์ที่ควรรู้

- **JSON**: รูปแบบการเก็บข้อมูลแบบ Key-Value
- **Object**: วัตถุที่เก็บข้อมูลแบบ Key-Value
- **Array**: รายการข้อมูลหลายตัว
- **Parse**: แปลง String เป็น Object
- **Loop**: วนลูป (ทำซ้ำ)
- **Filter**: กรอง (เลือกข้อมูลตามเงื่อนไข)
- **Select**: เลือกและแปลงข้อมูล

---

## ❓ คำถามที่พบบ่อย

**Q: JSON ต่างจาก Excel อย่างไร?**  
A: JSON เป็นรูปแบบข้อมูลที่คอมพิวเตอร์อ่านได้ง่าย ส่วน Excel เป็นไฟล์ที่มนุษย์อ่านได้

**Q: ทำไมต้อง Parse JSON?**  
A: เพราะข้อมูลที่เราได้รับมาส่วนใหญ่เป็น String (ข้อความ) ต้องแปลงเป็น Object ก่อนใช้งาน

**Q: Apply to Each ใช้เมื่อไหร่?**  
A: ใช้เมื่อต้องการทำอะไรบางอย่างกับทุก item ใน Array

**Q: Filter Array vs Select ต่างกันอย่างไร?**  
A: Filter = กรองข้อมูลตามเงื่อนไข, Select = แปลงข้อมูลเป็นรูปแบบใหม่

---

## 🎉 พร้อมสำหรับบทต่อไปแล้ว!

หากคุณเข้าใจเนื้อหาบทนี้แล้ว คุณพร้อมสำหรับบทต่อไปแล้ว! 🚀

