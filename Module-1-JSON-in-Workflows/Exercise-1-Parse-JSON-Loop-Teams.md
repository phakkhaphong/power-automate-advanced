# Exercise 1: Parse JSON → Loop → Teams Messaging

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่รับข้อมูล JSON
- แปลง JSON String เป็น Object ที่ใช้งานได้ (Parse JSON)
- วนลูปผ่าน Array เพื่อดึงข้อมูล
- ส่งข้อความไปยัง Teams Chat/Channel สำหรับแต่ละรายการ

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- ความรู้พื้นฐานเรื่อง JSON (ถ้ายังไม่รู้ จะสอนในขั้นตอน)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: สร้าง Flow ใหม่

1. เข้าสู่ **Power Automate** (https://powerautomate.microsoft.com)
2. คลิก **Create** → เลือก **Instant cloud flow**
3. ตั้งชื่อ Flow: `Exercise 1 - Parse JSON Loop Teams`
4. เลือก Trigger: **Manually trigger a flow**
5. คลิก **Create**

### ขั้นตอนที่ 2: สร้างข้อมูล JSON ตัวอย่าง

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Initialize variable**
3. ตั้งค่า:
   - **Name**: `jsonData`
   - **Type**: `String`
   - **Value**: คัดลอก JSON ด้านล่างไปวาง

```json
{
  "employees": [
    {
      "id": 1,
      "name": "สมชาย ใจดี",
      "department": "ไอที",
      "salary": 50000
    },
    {
      "id": 2,
      "name": "สมหญิง รักงาน",
      "department": "ทรัพยากรบุคคล",
      "salary": 45000
    },
    {
      "id": 3,
      "name": "วิชัย ทำงานดี",
      "department": "การเงิน",
      "salary": 55000
    },
    {
      "id": 4,
      "name": "วิไล เก่งมาก",
      "department": "ไอที",
      "salary": 60000
    }
  ]
}
```

> 💡 **หมายเหตุ**: JSON นี้มีโครงสร้าง Object หลักที่มี Key ชื่อ `employees` ซึ่งเป็น Array ของพนักงานแต่ละคน

### ขั้นตอนที่ 3: Parse JSON (แปลง JSON String เป็น Object)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Parse JSON**
3. ตั้งค่า:
   - **Content**: เลือก `jsonData` จาก Dynamic Content
   - **Schema**: คลิก **Use sample payload to generate schema**
   - วาง JSON ตัวอย่างด้านบนลงไป
   - คลิก **Done**

> 💡 **คำอธิบาย**: Parse JSON จะแปลง JSON String ให้เป็น Object ที่ Power Automate เข้าใจและสามารถใช้งานได้

**หลังจาก Parse JSON แล้ว คุณสามารถเข้าถึงข้อมูลได้:**

**Extract items[]:**
- `body('Parse_JSON')?['employees']` → Array ของพนักงานทั้งหมด

**Access Values Using Index:**
- `body('Parse_JSON')?['employees'][0]` → พนักงานคนแรก (index 0)
- `body('Parse_JSON')?['employees'][0]?['name']` → `"สมชาย ใจดี"`
- `body('Parse_JSON')?['employees'][0]?['department']` → `"ไอที"`
- `body('Parse_JSON')?['employees'][0]?['salary']` → `50000`

**ตัวอย่าง:**
```
employees[0].name       → "สมชาย ใจดี"
employees[0].department → "ไอที"
employees[0].salary     → 50000

employees[1].name       → "สมหญิง รักงาน"
employees[1].department → "ทรัพยากรบุคคล"
employees[1].salary     → 45000
```

### ขั้นตอนที่ 4: ส่งข้อความเริ่มต้น (Post Message - นอก Loop)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Message**: พิมพ์ข้อความเริ่มต้น เช่น:
     ```
     📋 เริ่มต้นส่งข้อมูลพนักงาน
     ```

> 💡 **คำอธิบาย**: Action นี้จะส่งข้อความก่อนเริ่มวนลูป เพื่อแจ้งเตือนว่ากำลังเริ่มกระบวนการ

### ขั้นตอนที่ 5: วนลูปผ่าน Array (Apply to Each)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Apply to each**
3. ตั้งค่า:
   - **Select an output from previous steps**: เลือก `employees` จาก Parse JSON output

> 💡 **คำอธิบาย**: Apply to Each จะวนลูปผ่านทุก item ใน Array `employees` (ในที่นี้มี 4 คน)

**Loop through items using Apply to Each:**

**item()** = ข้อมูลในรอบปัจจุบัน
- รอบที่ 1: `item()` = employees[0] = { "id": 1, "name": "สมชาย ใจดี", ... }
- รอบที่ 2: `item()` = employees[1] = { "id": 2, "name": "สมหญิง รักงาน", ... }
- รอบที่ 3: `item()` = employees[2] = { "id": 3, "name": "วิชัย ทำงานดี", ... }
- รอบที่ 4: `item()` = employees[3] = { "id": 4, "name": "วิไล เก่งมาก", ... }

**Access Values ใน Loop:**
- `item()?['name']` → ชื่อพนักงานในรอบปัจจุบัน
- `item()?['department']` → แผนกในรอบปัจจุบัน
- `item()?['salary']` → เงินเดือนในรอบปัจจุบัน

**ตัวอย่างการใช้งาน:**
```
Apply to Each: body('Parse_JSON')?['employees']
  - item()?['name']       → รอบที่ 1: "สมชาย ใจดี", รอบที่ 2: "สมหญิง รักงาน", ...
  - item()?['department'] → รอบที่ 1: "ไอที", รอบที่ 2: "ทรัพยากรบุคคล", ...
  - item()?['salary']     → รอบที่ 1: 50000, รอบที่ 2: 45000, ...
```

### ขั้นตอนที่ 6: ส่งข้อความสำหรับแต่ละพนักงาน (Post Message - ใน Loop)

1. **ภายใน Apply to Each** คลิก **Add an action**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: เลือก Email address เดียวกัน (หรือใช้ Dynamic Content)
   - **Message**: ใช้ Dynamic Content หรือ Expression เพื่อสร้างข้อความ:
     ```
     Interests: Current item
     ```
     หรือใช้ Expression:
     ```
     concat('👤 ', item()?['name'], ' | แผนก: ', item()?['department'], ' | เงินเดือน: ', item()?['salary'])
     ```

> 💡 **คำอธิบาย**: Action นี้จะส่งข้อความแยกสำหรับแต่ละพนักงานใน Array โดยใช้ `item()` เพื่อเข้าถึงข้อมูลในแต่ละรอบ

**ตัวอย่างข้อความที่ส่งออกไป:**
- รอบที่ 1: `👤 สมชาย ใจดี | แผนก: ไอที | เงินเดือน: 50000`
- รอบที่ 2: `👤 สมหญิง รักงาน | แผนก: ทรัพยากรบุคคล | เงินเดือน: 45000`
- รอบที่ 3: `👤 วิชัย ทำงานดี | แผนก: การเงิน | เงินเดือน: 55000`
- รอบที่ 4: `👤 วิไล เก่งมาก | แผนก: ไอที | เงินเดือน: 60000`

### ขั้นตอนที่ 7: ทดสอบ Flow

1. คลิก **Save**
2. คลิก **Test** → **Manually** → **Run flow**
3. รอให้ Flow รันเสร็จ
4. ตรวจสอบข้อความใน Teams Chat:
   - ข้อความแรก: `📋 เริ่มต้นส่งข้อมูลพนักงาน`
   - ข้อความถัดไป: ข้อความแยกสำหรับแต่ละพนักงาน (4 ข้อความ)

---

## 📊 ผลลัพธ์ที่คาดหวัง

คุณควรจะเห็นข้อความใน Teams Chat:
- **ข้อความที่ 1**: ข้อความเริ่มต้น (ส่งก่อน Loop)
- **ข้อความที่ 2-5**: ข้อความแยกสำหรับแต่ละพนักงาน 4 คน (ส่งใน Loop)
- แต่ละข้อความแสดงข้อมูล: ชื่อ, แผนก, เงินเดือน

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **JSON Basics**: เข้าใจโครงสร้าง JSON Objects และ Arrays  
✅ **Parse JSON**: แปลง JSON String เป็น Object ที่ใช้งานได้  
✅ **Apply to Each**: วนลูปผ่าน Array เพื่อประมวลผลแต่ละรายการ  
✅ **Dynamic Content**: ใช้ `item()` เพื่อเข้าถึงข้อมูลในแต่ละรอบของ Loop  
✅ **Teams Integration**: ส่งข้อความไปยัง Teams Chat/Channel  

---

## 💡 Tips & Tricks

1. **ถ้า JSON ผิดรูปแบบ**: ตรวจสอบว่า JSON มี `{}` และ `[]` ครบถ้วน
2. **ถ้า Parse JSON ไม่ทำงาน**: ลองใช้ **Generate from sample** เพื่อสร้าง Schema อัตโนมัติ
3. **ถ้าต้องการ Format เงินเดือน**: ใช้ Expression `concat('฿', formatNumber(item()?['salary'], '#,##0'))`
4. **ถ้า Teams Message ไม่แสดง**: ตรวจสอบว่าได้เชื่อมต่อ Teams Connector แล้ว และ Email ที่ใช้ถูกต้อง
5. **การใช้ item() ใน Loop**: ใน Apply to Each, `item()` จะอ้างอิงถึงข้อมูลในรอบปัจจุบันเสมอ

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **เพิ่ม Condition**: ส่งข้อความแยกตามแผนก (ถ้าแผนก = "ไอที" ส่งข้อความพิเศษ)
2. **Format ข้อความ**: ใช้ Rich Text Formatting ใน Teams Message (Bold, Italic, Lists)
3. **ส่งไป Channel แทน Chat**: เปลี่ยนจาก "Chat with Flow bot" เป็น "In a channel"
4. **เพิ่มข้อมูล**: ส่งข้อความรวมข้อมูลหลายฟิลด์พร้อมกัน (เช่น ID, ชื่อ, แผนก, เงินเดือน)
5. **ใช้ Filter Array**: กรองเฉพาะพนักงานที่เงินเดือน > 50000 ก่อนส่งข้อความ

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: ทำไมต้องใช้ Parse JSON?**  
A: เพราะข้อมูลที่เรารับมาเป็น String (ข้อความ) แต่เราต้องการให้เป็น Object เพื่อใช้งานและเข้าถึงข้อมูลได้

**Q: item() คืออะไร?**  
A: `item()` คือตัวแปรที่อ้างอิงถึงข้อมูลในรอบปัจจุบันของ Apply to Each Loop

**Q: ทำไมต้องส่งข้อความนอก Loop ก่อน?**  
A: เพื่อแจ้งเตือนหรือแสดงข้อมูลเริ่มต้นก่อนเริ่มประมวลผลข้อมูลใน Loop

**Q: Teams Connector ต้องตั้งค่าอะไรบ้าง?**  
A: ต้องเชื่อมต่อ Teams Connector และให้สิทธิ์ในการส่งข้อความใน Chat หรือ Channel

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การรับและ Parse JSON
- การวนลูปผ่าน Array ด้วย Apply to Each
- การเข้าถึงข้อมูลใน Loop ด้วย item()
- การส่งข้อความไปยัง Teams Chat/Channel

พร้อมสำหรับบทต่อไปแล้ว! 🎉



