# Exercise 1: JSON → Parse JSON → Loop & Select → HTML Table

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่รับข้อมูล JSON
- แปลง JSON String เป็น Object ที่ใช้งานได้ (Parse JSON)
- วนลูปผ่าน Array เพื่อดึงข้อมูล
- แปลงข้อมูลเป็น HTML Table สวยงาม

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- ความรู้พื้นฐานเรื่อง JSON (ถ้ายังไม่รู้ จะสอนในขั้นตอน)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: สร้าง Flow ใหม่

1. เข้าสู่ **Power Automate** (https://powerautomate.microsoft.com)
2. คลิก **Create** → เลือก **Instant cloud flow**
3. ตั้งชื่อ Flow: `Exercise 1 - JSON to HTML Table`
4. เลือก Trigger: **Manually trigger a flow**
5. คลิก **Create**

### ขั้นตอนที่ 2: สร้างข้อมูล JSON ตัวอย่าง

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Initialize variable**
3. ตั้งค่า:
   - **Name**: `jsonData`
   - **Type**: `String`
   - **Value**: คัดลอก JSON ด้านล่างไปวาง

```json
{
  "employees": [
    {
      "id": 1,
      "name": "สมชาย ใจดี",
      "department": "ไอที",
      "salary": 50000
    },
    {
      "id": 2,
      "name": "สมหญิง รักงาน",
      "department": "ทรัพยากรบุคคล",
      "salary": 45000
    },
    {
      "id": 3,
      "name": "วิชัย ทำงานดี",
      "department": "การเงิน",
      "salary": 55000
    },
    {
      "id": 4,
      "name": "วิไล เก่งมาก",
      "department": "ไอที",
      "salary": 60000
    }
  ]
}
```

### ขั้นตอนที่ 3: Parse JSON (แปลง JSON String เป็น Object)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Parse JSON**
3. ตั้งค่า:
   - **Content**: เลือก `jsonData` จาก Dynamic Content
   - **Schema**: คลิก **Use sample payload to generate schema**
   - วาง JSON ตัวอย่างด้านบนลงไป
   - คลิก **Done**

> 💡 **คำอธิบาย**: Parse JSON จะแปลง JSON String ให้เป็น Object ที่ Power Automate เข้าใจและสามารถใช้งานได้

**หลังจาก Parse JSON แล้ว คุณสามารถเข้าถึงข้อมูลได้:**

**Extract items[]:**
- `body('Parse_JSON')?['employees']` → Array ของพนักงานทั้งหมด

**Access Values Using Index:**
- `body('Parse_JSON')?['employees'][0]` → พนักงานคนแรก (index 0)
- `body('Parse_JSON')?['employees'][0]?['name']` → `"สมชาย ใจดี"`
- `body('Parse_JSON')?['employees'][0]?['department']` → `"ไอที"`
- `body('Parse_JSON')?['employees'][0]?['salary']` → `50000`

**ตัวอย่าง:**
```
employees[0].name       → "สมชาย ใจดี"
employees[0].department → "ไอที"
employees[0].salary     → 50000

employees[1].name       → "สมหญิง รักงาน"
employees[1].department → "ทรัพยากรบุคคล"
employees[1].salary     → 45000
```

### ขั้นตอนที่ 4: วนลูปผ่าน Array (Apply to Each)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Apply to each**
3. ตั้งค่า:
   - **Select an output from previous steps**: เลือก `employees` จาก Parse JSON output

> 💡 **คำอธิบาย**: Apply to Each จะวนลูปผ่านทุก item ใน Array `employees` (ในที่นี้มี 4 คน)

**Loop through items using Apply to Each:**

**item()** = ข้อมูลในรอบปัจจุบัน
- รอบที่ 1: `item()` = employees[0] = { "id": 1, "name": "สมชาย ใจดี", ... }
- รอบที่ 2: `item()` = employees[1] = { "id": 2, "name": "สมหญิง รักงาน", ... }
- รอบที่ 3: `item()` = employees[2] = { "id": 3, "name": "วิชัย ทำงานดี", ... }
- รอบที่ 4: `item()` = employees[3] = { "id": 4, "name": "วิไล เก่งมาก", ... }

**Access Values ใน Loop:**
- `item()?['name']` → ชื่อพนักงานในรอบปัจจุบัน
- `item()?['department']` → แผนกในรอบปัจจุบัน
- `item()?['salary']` → เงินเดือนในรอบปัจจุบัน

**ตัวอย่างการใช้งาน:**
```
Apply to Each: body('Parse_JSON')?['employees']
  - item()?['name']       → รอบที่ 1: "สมชาย ใจดี", รอบที่ 2: "สมหญิง รักงาน", ...
  - item()?['department'] → รอบที่ 1: "ไอที", รอบที่ 2: "ทรัพยากรบุคคล", ...
  - item()?['salary']     → รอบที่ 1: 50000, รอบที่ 2: 45000, ...
```

### ขั้นตอนที่ 5: สร้าง Array ใหม่ด้วย Select

1. **อย่าเพิ่ม Action ใน Apply to Each** ให้เราเพิ่ม Action ใหม่นอก Loop
2. คลิกที่ **Apply to each** → เลือก **Add an action**
3. ค้นหาและเลือก **Select**
4. ตั้งค่า:
   - **From**: เลือก `employees` จาก Parse JSON output
   - คลิก **Switch to text mode**
   - วางโค้ดด้านล่าง:

```
item()?['id']
```

5. คลิก **Add new item** เพื่อเพิ่ม Column ใหม่ (ทำซ้ำ 3 ครั้ง)
6. ตั้งค่า Columns:
   - Column 1: `item()?['id']` → ชื่อ: `ID`
   - Column 2: `item()?['name']` → ชื่อ: `ชื่อ`
   - Column 3: `item()?['department']` → ชื่อ: `แผนก`
   - Column 4: `item()?['salary']` → ชื่อ: `เงินเดือน`

> 💡 **คำอธิบาย**: Select จะแปลง Array เดิมให้เป็น Array ใหม่ที่มีโครงสร้างที่เราต้องการ

### ขั้นตอนที่ 6: สร้าง HTML Table

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Compose** (หรือ **Data operations** → **Compose**)
3. ตั้งค่า:
   - **Inputs**: คลิก **Expression** และพิมพ์:

```
createHTMLTable(outputs('Select')?['body'])
```

> 💡 **คำอธิบาย**: `createHTMLTable()` จะแปลง Array เป็น HTML Table ที่สวยงาม

### ขั้นตอนที่ 7: ทดสอบ Flow

1. คลิก **Save**
2. คลิก **Test** → **Manually** → **Run flow**
3. รอให้ Flow รันเสร็จ
4. ดูผลลัพธ์ใน **Compose** Action

---

## 📊 ผลลัพธ์ที่คาดหวัง

คุณควรจะเห็น HTML Table ที่มี:
- Header: ID, ชื่อ, แผนก, เงินเดือน
- 4 แถว (Rows) แสดงข้อมูลพนักงาน 4 คน
- ตารางที่สามารถ Copy ไปใช้ใน Email หรือ Web Page ได้

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **JSON Basics**: เข้าใจโครงสร้าง JSON Objects และ Arrays  
✅ **Parse JSON**: แปลง JSON String เป็น Object ที่ใช้งานได้  
✅ **Apply to Each**: วนลูปผ่าน Array  
✅ **Select**: แปลง Array เป็นรูปแบบใหม่  
✅ **createHTMLTable()**: สร้าง HTML Table จาก Array  

---

## 💡 Tips & Tricks

1. **ถ้า JSON ผิดรูปแบบ**: ตรวจสอบว่า JSON มี `{}` และ `[]` ครบถ้วน
2. **ถ้า Parse JSON ไม่ทำงาน**: ลองใช้ **Generate from sample** เพื่อสร้าง Schema อัตโนมัติ
3. **ถ้าต้องการ Format เงินเดือน**: ใช้ Expression `concat('฿', item()?['salary'])`

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **เพิ่ม Filter**: กรองเฉพาะพนักงานที่เงินเดือน > 50000
2. **เพิ่ม Column**: เพิ่ม Column "เงินเดือนต่อเดือน" (หาร 12)
3. **ส่ง Email**: ใช้ HTML Table ที่สร้างแล้วส่งไปยัง Email
4. **ใช้ join()**: สร้าง String ที่รวม Email ของพนักงานทั้งหมดด้วย `join()`
5. **สร้าง CSV Table**: ใช้ `createCSVTable()` แทน `createHTMLTable()` เพื่อสร้าง CSV

---

## 📚 ตัวอย่างเพิ่มเติม: การใช้ join() และ CSV Table

### ตัวอย่างที่ 1: ใช้ join() เพื่อรวม Email Addresses

**สถานการณ์**: คุณต้องการส่ง Email ไปยังหลายคนพร้อมกัน

**ขั้นตอน**:
1. สร้าง Array ของ Email addresses:
   ```json
   ["user1@example.com", "user2@example.com", "user3@example.com"]
   ```

2. ใช้ Expression `join()`:
   ```
   join(variables('emailList'), '; ')
   ```

3. **ผลลัพธ์**:
   ```
   "user1@example.com; user2@example.com; user3@example.com"
   ```

4. ใช้ผลลัพธ์ใน Email Action → **To** field

**ตัวอย่าง Expression อื่นๆ:**
- `join(variables('names'), ', ')` → `"สมชาย, สมหญิง, วิชัย"`
- `join(variables('products'), ' | ')` → `"แอปเปิ้ล | ส้ม | กล้วย"`

### ตัวอย่างที่ 2: สร้าง CSV Table แทน HTML Table

**ขั้นตอน**:
1. ทำตามขั้นตอนที่ 1-5 เหมือนเดิม (Parse JSON → Select)

2. แทนที่จะใช้ `createHTMLTable()`, ใช้ `createCSVTable()`:
   ```
   createCSVTable(outputs('Select')?['body'])
   ```

3. **ผลลัพธ์ CSV**:
   ```csv
   ID,ชื่อ,แผนก,เงินเดือน
   1,สมชาย ใจดี,ไอที,50000
   2,สมหญิง รักงาน,ทรัพยากรบุคคล,45000
   3,วิชัย ทำงานดี,การเงิน,55000
   4,วิไล เก่งมาก,ไอที,60000
   ```

4. **ใช้ CSV สำหรับ**:
   - Export ข้อมูลไปยัง Excel
   - Attach เป็นไฟล์ CSV ใน Email
   - ส่งข้อมูลไปยังระบบอื่นที่ต้องการ CSV format

**ข้อแตกต่างระหว่าง HTML และ CSV:**
- **HTML Table**: ใช้แสดงผลใน Email หรือ Web Page (สวยงาม, มีสี, Format ได้)
- **CSV Table**: ใช้สำหรับ Export ข้อมูล (ง่าย, เปิดใน Excel ได้)

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: ทำไมต้องใช้ Parse JSON?**  
A: เพราะข้อมูลที่เรารับมาเป็น String (ข้อความ) แต่เราต้องการให้เป็น Object เพื่อใช้งาน

**Q: Select กับ Apply to Each ต่างกันอย่างไร?**  
A: Select ใช้แปลง Array ทั้งหมดในครั้งเดียว, Apply to Each ใช้ทำอะไรบางอย่างกับแต่ละ Item

**Q: HTML Table ใช้ทำอะไรได้บ้าง?**  
A: ใช้แสดงใน Email, Web Page, หรือ Report ได้เลย

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การรับและ Parse JSON
- การวนลูปและเลือกข้อมูล
- การสร้าง HTML Table

พร้อมสำหรับบทต่อไปแล้ว! 🎉


