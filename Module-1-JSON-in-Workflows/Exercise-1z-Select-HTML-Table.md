# Exercise 1A: Select → HTML Table

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- แปลง JSON String เป็น Object ที่ใช้งานได้ (Parse JSON)
- ใช้ Select เพื่อแปลง Array เป็นรูปแบบใหม่
- สร้าง HTML Table จาก Array ที่แปลงแล้ว
- สร้าง CSV Table สำหรับ Export ข้อมูล

> 💡 **หมายเหตุ**: Exercise นี้เป็นส่วนเสริมของ Exercise 1 ที่เน้นการสร้าง Table แทนการส่งข้อความ

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- ความรู้พื้นฐานเรื่อง JSON
- ควรทำ Exercise 1 เสร็จก่อน (เพื่อเข้าใจพื้นฐาน Parse JSON และ Loop)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: สร้าง Flow ใหม่

1. เข้าสู่ **Power Automate** (https://powerautomate.microsoft.com)
2. คลิก **Create** → เลือก **Instant cloud flow**
3. ตั้งชื่อ Flow: `Exercise 1A - JSON to HTML Table`
4. เลือก Trigger: **Manually trigger a flow**
5. คลิก **Create**

### ขั้นตอนที่ 2: สร้างข้อมูล JSON ตัวอย่าง

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Initialize variable**
3. ตั้งค่า:
   - **Name**: `jsonData`
   - **Type**: `String`
   - **Value**: คัดลอก JSON ด้านล่างไปวาง

```json
{
  "employees": [
    {
      "id": 1,
      "name": "สมชาย ใจดี",
      "department": "ไอที",
      "salary": 50000
    },
    {
      "id": 2,
      "name": "สมหญิง รักงาน",
      "department": "ทรัพยากรบุคคล",
      "salary": 45000
    },
    {
      "id": 3,
      "name": "วิชัย ทำงานดี",
      "department": "การเงิน",
      "salary": 55000
    },
    {
      "id": 4,
      "name": "วิไล เก่งมาก",
      "department": "ไอที",
      "salary": 60000
    }
  ]
}
```

### ขั้นตอนที่ 3: Parse JSON (แปลง JSON String เป็น Object)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Parse JSON**
3. ตั้งค่า:
   - **Content**: เลือก `jsonData` จาก Dynamic Content
   - **Schema**: คลิก **Use sample payload to generate schema**
   - วาง JSON ตัวอย่างด้านบนลงไป
   - คลิก **Done**

> 💡 **คำอธิบาย**: Parse JSON จะแปลง JSON String ให้เป็น Object ที่ Power Automate เข้าใจและสามารถใช้งานได้

**หลังจาก Parse JSON แล้ว คุณสามารถเข้าถึงข้อมูลได้:**
- `body('Parse_JSON')?['employees']` → Array ของพนักงานทั้งหมด

### ขั้นตอนที่ 4: สร้าง Array ใหม่ด้วย Select

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Select**
3. ตั้งค่า:
   - **From**: เลือก `employees` จาก Parse JSON output
   - คลิก **Switch to text mode**
   - วางโค้ดด้านล่าง:

```
item()?['id']
```

4. คลิก **Add new item** เพื่อเพิ่ม Column ใหม่ (ทำซ้ำ 3 ครั้ง)
5. ตั้งค่า Columns:
   - Column 1: `item()?['id']` → ชื่อ: `ID`
   - Column 2: `item()?['name']` → ชื่อ: `ชื่อ`
   - Column 3: `item()?['department']` → ชื่อ: `แผนก`
   - Column 4: `item()?['salary']` → ชื่อ: `เงินเดือน`

> 💡 **คำอธิบาย**: Select จะแปลง Array เดิมให้เป็น Array ใหม่ที่มีโครงสร้างที่เราต้องการ โดยแต่ละ Column จะเป็น Field ที่เราสามารถกำหนดชื่อได้

**ผลลัพธ์ของ Select:**
```json
[
  {
    "ID": 1,
    "ชื่อ": "สมชาย ใจดี",
    "แผนก": "ไอที",
    "เงินเดือน": 50000
  },
  {
    "ID": 2,
    "ชื่อ": "สมหญิง รักงาน",
    "แผนก": "ทรัพยากรบุคคล",
    "เงินเดือน": 45000
  },
  ...
]
```

### ขั้นตอนที่ 5: สร้าง HTML Table

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Compose** (หรือ **Data operations** → **Compose**)
3. ตั้งค่า:
   - **Inputs**: คลิก **Expression** และพิมพ์:

```
createHTMLTable(outputs('Select')?['body'])
```

> 💡 **คำอธิบาย**: `createHTMLTable()` จะแปลง Array เป็น HTML Table ที่สวยงามพร้อม Header และ Formatting

### ขั้นตอนที่ 6: ทดสอบ Flow

1. คลิก **Save**
2. คลิก **Test** → **Manually** → **Run flow**
3. รอให้ Flow รันเสร็จ
4. ดูผลลัพธ์ใน **Compose** Action
5. คัดลอก HTML Table ที่ได้ไปใช้ใน Email หรือ Web Page

---

## 📊 ผลลัพธ์ที่คาดหวัง

คุณควรจะเห็น HTML Table ที่มี:
- **Header**: ID, ชื่อ, แผนก, เงินเดือน
- **4 แถว (Rows)**: แสดงข้อมูลพนักงาน 4 คน
- **Formatting**: ตารางที่สวยงามพร้อมสีและรูปแบบ
- **ใช้งานได้ทันที**: สามารถ Copy ไปใช้ใน Email หรือ Web Page ได้

**ตัวอย่าง HTML Table ที่ได้:**
```html
<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>ชื่อ</th>
      <th>แผนก</th>
      <th>เงินเดือน</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>สมชาย ใจดี</td>
      <td>ไอที</td>
      <td>50000</td>
    </tr>
    ...
  </tbody>
</table>
```

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **Select Action**: แปลง Array เป็นรูปแบบใหม่ที่กำหนดเอง  
✅ **createHTMLTable()**: สร้าง HTML Table จาก Array  
✅ **Array Transformation**: การแปลงโครงสร้างข้อมูลให้เหมาะกับการใช้งาน  
✅ **HTML Formatting**: การสร้างตารางที่พร้อมใช้งานใน Email/Web

---

## 💡 Tips & Tricks

1. **Select กับ Apply to Each ต่างกันอย่างไร?**
   - **Select**: ใช้แปลง Array ทั้งหมดในครั้งเดียว (เร็ว, ใช้งานง่าย)
   - **Apply to Each**: ใช้ทำอะไรบางอย่างกับแต่ละ Item (ยืดหยุ่น, แต่ช้ากว่า)

2. **ถ้าต้องการ Format เงินเดือน**: ใช้ Expression ใน Select:
   ```
   concat('฿', formatNumber(item()?['salary'], '#,##0'))
   ```

3. **ถ้าต้องการเพิ่ม Column**: คลิก **Add new item** ใน Select และกำหนด Expression

4. **HTML Table ใช้ทำอะไรได้บ้าง?**
   - ส่ง Email แบบ Rich Text
   - แสดงผลใน Web Page
   - สร้าง Report
   - แชร์ใน Teams/SharePoint

---

## 🔄 แบบฝึกหัดเพิ่มเติม

### 1. สร้าง CSV Table แทน HTML Table

**ขั้นตอน**:
1. ทำตามขั้นตอนที่ 1-4 เหมือนเดิม (Parse JSON → Select)

2. แทนที่จะใช้ `createHTMLTable()`, ใช้ `createCSVTable()`:
   ```
   createCSVTable(outputs('Select')?['body'])
   ```

3. **ผลลัพธ์ CSV**:
   ```csv
   ID,ชื่อ,แผนก,เงินเดือน
   1,สมชาย ใจดี,ไอที,50000
   2,สมหญิง รักงาน,ทรัพยากรบุคคล,45000
   3,วิชัย ทำงานดี,การเงิน,55000
   4,วิไล เก่งมาก,ไอที,60000
   ```

4. **ใช้ CSV สำหรับ**:
   - Export ข้อมูลไปยัง Excel
   - Attach เป็นไฟล์ CSV ใน Email
   - ส่งข้อมูลไปยังระบบอื่นที่ต้องการ CSV format

**ข้อแตกต่างระหว่าง HTML และ CSV:**
- **HTML Table**: ใช้แสดงผลใน Email หรือ Web Page (สวยงาม, มีสี, Format ได้)
- **CSV Table**: ใช้สำหรับ Export ข้อมูล (ง่าย, เปิดใน Excel ได้)

### 2. ส่ง HTML Table ไปยัง Email

**ขั้นตอน**:
1. เพิ่ม Action: **Send an email (V2)**
2. ตั้งค่า:
   - **To**: Email address ของคุณ
   - **Subject**: `รายงานข้อมูลพนักงาน`
   - **Body**: เลือก `Outputs` จาก **Compose** Action (HTML Table)
   - **Is HTML**: เปิดใช้งาน (Enable)

3. **ผลลัพธ์**: Email จะแสดง HTML Table ที่สวยงาม

### 3. ใช้ join() เพื่อรวมข้อมูล

**สถานการณ์**: คุณต้องการสร้าง String ที่รวมชื่อพนักงานทั้งหมด

**ขั้นตอน**:
1. ใช้ Select เพื่อสร้าง Array ของชื่อ:
   - Column 1: `item()?['name']` → ชื่อ: `Name`

2. ใช้ Expression `join()`:
   ```
   join(outputs('Select')?['body'], ', ')
   ```

3. **ผลลัพธ์**:
   ```
   "สมชาย ใจดี, สมหญิง รักงาน, วิชัย ทำงานดี, วิไล เก่งมาก"
   ```

**ตัวอย่าง Expression อื่นๆ:**
- `join(variables('emailList'), '; ')` → `"user1@example.com; user2@example.com"`
- `join(variables('products'), ' | ')` → `"แอปเปิ้ล | ส้ม | กล้วย"`

### 4. เพิ่ม Filter ก่อน Select

**สถานการณ์**: กรองเฉพาะพนักงานที่เงินเดือน > 50000

**ขั้นตอน**:
1. หลังจาก Parse JSON, เพิ่ม Action: **Filter array**
2. ตั้งค่า:
   - **From**: เลือก `employees` จาก Parse JSON
   - **เงื่อนไข**: `salary` `is greater than` `50000`

3. ใช้ผลลัพธ์จาก Filter array ใน Select แทน employees

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: Select กับ Apply to Each ควรใช้เมื่อไหร่?**  
A: ใช้ Select เมื่อต้องการแปลง Array ทั้งหมดในครั้งเดียว, ใช้ Apply to Each เมื่อต้องการทำ Action กับแต่ละ Item

**Q: HTML Table ใช้ทำอะไรได้บ้าง?**  
A: ใช้แสดงใน Email, Web Page, Teams Message, หรือ Report ได้เลย

**Q: CSV Table ดีกว่าหรือ HTML Table?**  
A: ขึ้นอยู่กับการใช้งาน - HTML สำหรับแสดงผล, CSV สำหรับ Export ข้อมูล

**Q: จะ Format ข้อมูลใน Select ได้อย่างไร?**  
A: ใช้ Expression เช่น `concat()`, `formatNumber()`, `formatDateTime()` ในแต่ละ Column

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การใช้ Select เพื่อแปลง Array
- การสร้าง HTML Table จาก Array
- การสร้าง CSV Table สำหรับ Export
- การใช้ join() เพื่อรวมข้อมูล

พร้อมสำหรับบทต่อไปแล้ว! 🎉

