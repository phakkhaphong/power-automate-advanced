# Exercise 1d: Parse JSON → Select → HTML Table → Teams Messaging

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่รับข้อมูล JSON
- แปลง JSON String เป็น Object ที่ใช้งานได้ (Parse JSON)
- ใช้ Select เพื่อแปลง Array เป็นรูปแบบใหม่
- สร้าง HTML Table จาก Array ที่แปลงแล้ว
- ส่ง HTML Table ไปยัง Teams Chat/Channel

> 💡 **หมายเหตุ**: Exercise นี้แตกต่างจาก Exercise 1a ที่ใช้ **Select** และ **Create HTML table Action** เพื่อสร้างตาราง HTML สวยงามแทนการส่งข้อความทีละรายการ

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- ความรู้พื้นฐานเรื่อง JSON (ถ้ายังไม่รู้ จะสอนในขั้นตอน)
- ควรทำ Exercise 1a เสร็จก่อน (เพื่อเข้าใจพื้นฐาน Parse JSON)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: สร้าง Flow ใหม่และตั้งค่า JSON Input

1. เข้าสู่ **Power Automate** (https://powerautomate.microsoft.com)
2. คลิก **Create** → เลือก **Instant cloud flow**
3. ตั้งชื่อ Flow: `Exercise 1d - Parse JSON Select HTML Teams`
4. เลือก Trigger: **Manually trigger a flow**
5. คลิก **Create**
6. ใน **Manually trigger a flow** คลิก **Add an input** → เลือก **JSON**
7. ตั้งชื่อ Input: `JSON input` (หรือชื่ออื่นตามต้องการ)

> 💡 **หมายเหตุ**: JSON Input จะให้คุณสามารถป้อนข้อมูล JSON เมื่อรัน Flow ได้

### ขั้นตอนที่ 2: Parse JSON (แปลง JSON String เป็น Object)

**ก่อนเริ่ม**: เตรียมข้อมูล JSON ตัวอย่างด้านล่างไว้สำหรับทดสอบ:

```json
{
  "year": 2025,
  "employees": [
    {
      "id": 1,
      "name": "สมชาย ใจดี",
      "department": "ไอที",
      "salary": 50000
    },
    {
      "id": 2,
      "name": "สมหญิง รักงาน",
      "department": "ทรัพยากรบุคคล",
      "salary": 45000
    },
    {
      "id": 3,
      "name": "วิชัย ทำงานดี",
      "department": "การเงิน",
      "salary": 55000
    },
    {
      "id": 4,
      "name": "วิไล เก่งมาก",
      "department": "ไอที",
      "salary": 60000
    }
  ]
}
```

> 💡 **หมายเหตุ**: JSON นี้มีโครงสร้าง 2 ชั้น:
> - **ชั้นที่ 1**: Object หลักที่มี Key `year` (ปี) และ `employees` (Array ของพนักงาน)
> - **ชั้นที่ 2**: แต่ละ Object ใน Array `employees` มี Key `id`, `name`, `department`, `salary`

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Parse JSON**
3. ตั้งค่า:
   - **Content**: เลือก `JSON input` จาก Dynamic Content (จาก Manual Trigger)
   - **Schema**: คลิก **Use sample payload to generate schema**
   - วาง JSON ตัวอย่างด้านบนลงไป
   - คลิก **Done**

> 💡 **คำอธิบาย**: Parse JSON จะแปลง JSON String ให้เป็น Object ที่ Power Automate เข้าใจและสามารถใช้งานได้

**หลังจาก Parse JSON แล้ว คุณสามารถเข้าถึงข้อมูลได้:**

**ชั้นที่ 1 (Root Level):**
- `body('Parse_JSON')?['year']` → `2025` (ปี)
- `body('Parse_JSON')?['employees']` → Array ของพนักงานทั้งหมด (4 คน)

**ชั้นที่ 2 (Array Level):**
- `body('Parse_JSON')?['employees'][0]` → พนักงานคนแรก (index 0)
- `body('Parse_JSON')?['employees'][0]?['name']` → `"สมชาย ใจดี"`
- `body('Parse_JSON')?['employees'][0]?['department']` → `"ไอที"`
- `body('Parse_JSON')?['employees'][0]?['salary']` → `50000`

### ขั้นตอนที่ 3: สร้าง Array ใหม่ด้วย Select

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Select**
3. ตั้งค่า:
   - **From**: เลือก `Body employees` จาก Parse JSON output
4. คลิก **Add new item** เพื่อเพิ่ม Column ใหม่ (ทำซ้ำ 4 ครั้ง)
5. ตั้งค่า Columns:
   - Column 1: Key: `ID` → `item()?['id']`
   - Column 2: Key: `ชื่อ` → `item()?['name']`
   - Column 3: Key: `แผนก` → `item()?['department']`
   - Column 4: Key: `เงินเดือน` → `item()?['salary']`

> 💡 **คำอธิบาย**: Select จะแปลง Array เดิมให้เป็น Array ใหม่ที่มีโครงสร้างที่เราต้องการ โดยแต่ละ Column จะเป็น Field ที่เราสามารถกำหนดชื่อได้

**ผลลัพธ์ของ Select:**
```json
[
  {
    "ID": 1,
    "ชื่อ": "สมชาย ใจดี",
    "แผนก": "ไอที",
    "เงินเดือน": 50000
  },
  {
    "ID": 2,
    "ชื่อ": "สมหญิง รักงาน",
    "แผนก": "ทรัพยากรบุคคล",
    "เงินเดือน": 45000
  },
  {
    "ID": 3,
    "ชื่อ": "วิชัย ทำงานดี",
    "แผนก": "การเงิน",
    "เงินเดือน": 55000
  },
  {
    "ID": 4,
    "ชื่อ": "วิไล เก่งมาก",
    "แผนก": "ไอที",
    "เงินเดือน": 60000
  }
]
```

**ข้อดีของการใช้ Select:**
- เปลี่ยนชื่อ Field ให้อ่านง่ายขึ้น (เช่น `id` → `ID`, `name` → `ชื่อ`)
- เลือกเฉพาะ Field ที่ต้องการแสดง
- เปลี่ยนลำดับ Column
- Format ข้อมูลก่อนแสดง (เช่น Format เงินเดือน)

### ขั้นตอนที่ 4: สร้าง HTML Table

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Create HTML table** (หรือ **Data operations** → **Create HTML table**)
3. ตั้งค่า:
   - **From**: เลือก `Output` จาก Select output (Array ที่แปลงแล้วจาก Select)

**ผลลัพธ์ของ HTML Table:**
```html
<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>ชื่อ</th>
      <th>แผนก</th>
      <th>เงินเดือน</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>สมชาย ใจดี</td>
      <td>ไอที</td>
      <td>50000</td>
    </tr>
    <tr>
      <td>2</td>
      <td>สมหญิง รักงาน</td>
      <td>ทรัพยากรบุคคล</td>
      <td>45000</td>
    </tr>
    ...
  </tbody>
</table>
```

### ขั้นตอนที่ 5: ส่ง HTML Table ไปยัง Teams

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Message**: เลือก `output` จาก **Create HTML table** Action (HTML Table)
   - **Important**: Teams Message รองรับ HTML formatting ซึ่งจะทำให้ HTML Table แสดงผลสวยงาม

> 💡 **คำอธิบาย**: Action นี้จะส่ง HTML Table ไปยัง Teams Chat/Channel โดย Teams จะแสดง Table ที่สวยงามพร้อม Header และ Formatting

> ⚠️ **หมายเหตุ**: Teams Message รองรับ HTML tags เช่น `<table>`, `<tr>`, `<td>`, `<th>` ซึ่งจะทำให้ HTML Table แสดงผลถูกต้อง


### ขั้นตอนที่ 6: ทดสอบ Flow

1. คลิก **Save**
2. คลิก **Test** → **Manually** → **Run flow**
3. ในหน้า **Run flow**:
   - วาง JSON ตัวอย่างด้านบนลงในช่อง **JSON input**
   - คลิก **Run flow**
4. รอให้ Flow รันเสร็จ
5. ตรวจสอบข้อความใน Teams Chat:
   - ควรเห็น HTML Table ที่สวยงามแสดงข้อมูลพนักงานทั้งหมด 4 คน
   - Table จะมี Header: ID, ชื่อ, แผนก, เงินเดือน
   - แต่ละแถวแสดงข้อมูลพนักงานแต่ละคน

---

## 📊 ผลลัพธ์ที่คาดหวัง

คุณควรจะเห็น HTML Table ใน Teams Chat ที่มี:
- **Header**: ID, ชื่อ, แผนก, เงินเดือน
- **4 แถว (Rows)**: แสดงข้อมูลพนักงาน 4 คน
- **Formatting**: ตารางที่สวยงามพร้อมสีและรูปแบบ
- **อ่านง่าย**: ข้อมูลจัดเรียงเป็นตารางที่อ่านง่าย

**ข้อแตกต่างจาก Exercise 1a:**
- **Exercise 1a**: ส่งข้อความทีละรายการ (5 ข้อความ)
- **Exercise 1d**: ส่ง HTML Table เป็นข้อความเดียว (1 ข้อความ)

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **JSON Basics**: เข้าใจโครงสร้าง JSON Objects และ Arrays (2 ชั้น)  
✅ **Parse JSON**: แปลง JSON String เป็น Object ที่ใช้งานได้ และเข้าถึงข้อมูลหลายชั้น  
✅ **Select Action**: แปลง Array เป็นรูปแบบใหม่ที่กำหนดเอง  
✅ **Create HTML table Action**: สร้าง HTML Table จาก Array โดยใช้ Built-in Action  
✅ **HTML Formatting**: การสร้างตารางที่พร้อมใช้งานใน Teams  
✅ **Teams Integration**: ส่ง HTML Table ไปยัง Teams Chat/Channel  

---

## 💡 Tips & Tricks

1. **Select กับ Apply to Each ต่างกันอย่างไร?**
   - **Select**: ใช้แปลง Array ทั้งหมดในครั้งเดียว (เร็ว, ใช้งานง่าย)
   - **Apply to Each**: ใช้ทำอะไรบางอย่างกับแต่ละ Item (ยืดหยุ่น, แต่ช้ากว่า)

2. **เมื่อไหร่ควรใช้ Select?**
   - เมื่อต้องการเปลี่ยนโครงสร้าง Array
   - เมื่อต้องการเปลี่ยนชื่อ Field
   - เมื่อต้องการเลือกเฉพาะ Field ที่ต้องการ
   - เมื่อต้องการ Format ข้อมูลก่อนแสดง

3. **Format ข้อมูลใน Select:**
   - Format เงินเดือน: `concat('฿', formatNumber(item()?['salary'], '#,##0'))`
   - Format วันที่: `formatDateTime(item()?['date'], 'dd/MM/yyyy')`
   - รวมข้อมูล: `concat(item()?['firstName'], ' ', item()?['lastName'])`

4. **HTML Table ใช้ทำอะไรได้บ้าง?**
   - ส่งไป Teams Chat/Channel
   - ส่ง Email แบบ Rich Text
   - แสดงผลใน Web Page
   - สร้าง Report

5. **ถ้า HTML Table ไม่แสดงใน Teams หรือแสดงข้อมูลไม่ถูกต้อง:**
   - ตรวจสอบว่าเลือก `Output` จาก Select output (ไม่ใช่ `Body employees` จาก Parse JSON)
   - ตรวจสอบว่าได้ใช้ Select ก่อน Create HTML table เพื่อแปลงโครงสร้าง Array
   - ตรวจสอบว่า Teams Message รองรับ HTML formatting
   - ตรวจสอบว่า HTML Table ถูกสร้างถูกต้องโดยดู Output ของ Create HTML table Action

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **Format เงินเดือน**: ใช้ Select เพื่อ Format เงินเดือนเป็น `฿50,000`:
   ```
   concat('฿', formatNumber(item()?['salary'], '#,##0'))
   ```

2. **เพิ่ม Header**: เพิ่มข้อความ Header ก่อน HTML Table:
   ```
   concat('📋 รายชื่อพนักงานปี ', body('Parse_JSON')?['year'], '<br><br>', body('Create_HTML_table'))
   ```

3. **Filter ก่อน Select**: ใช้ Filter Array เพื่อกรองพนักงานก่อน Select (ดู Exercise 1c)

4. **สร้าง CSV Table**: ใช้ **Create CSV table** Action แทน Create HTML table:
   - เปลี่ยนจาก Create HTML table เป็น Create CSV table
   - เลือก `Output` จาก Select output เหมือนกัน

5. **เพิ่ม Column**: เพิ่ม Column ใหม่ใน Select เช่น "เงินเดือนต่อเดือน" (หาร 12)

6. **Conditional Formatting**: ใช้ Expression ใน Select เพื่อเพิ่มสีหรือ Format ตามเงื่อนไข:
   ```
   if(greater(item()?['salary'], 50000), concat('<span style="color:green;">', item()?['salary'], '</span>'), item()?['salary'])
   ```

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: Select กับ Apply to Each ต่างกันอย่างไร?**  
A: Select ใช้แปลง Array ทั้งหมดในครั้งเดียว (เร็ว, ใช้งานง่าย), Apply to Each ใช้ทำอะไรบางอย่างกับแต่ละ Item (ยืดหยุ่น, แต่ช้ากว่า)

**Q: HTML Table ใช้ทำอะไรได้บ้าง?**  
A: ใช้แสดงใน Teams Chat, Email, Web Page, หรือ Report ได้เลย

**Q: ทำไมต้องใช้ Select ก่อน Create HTML table?**  
A: เพื่อเปลี่ยนโครงสร้าง Array ให้เหมาะสมและเปลี่ยนชื่อ Field ให้อ่านง่ายขึ้น โดย Select จะสร้าง Array ใหม่ที่มี Field names ที่เหมาะสมสำหรับ HTML Table

**Q: HTML Table ใน Teams แสดงผลถูกต้องไหม?**  
A: ใช่ Teams รองรับ HTML formatting ทำให้ HTML Table แสดงผลสวยงาม

**Q: สามารถ Format ข้อมูลใน Select ได้ไหม?**  
A: ได้ ใช้ Expression เช่น `formatNumber()`, `concat()`, `formatDateTime()` ในแต่ละ Column

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การรับและ Parse JSON
- การใช้ Select เพื่อแปลง Array เป็นรูปแบบใหม่
- การสร้าง HTML Table จาก Array ด้วย Create HTML table Action
- การส่ง HTML Table ไปยัง Teams Chat/Channel

**ข้อแตกต่างจาก Exercise 1a:**
- **Exercise 1a**: ส่งข้อความทีละรายการ (5 ข้อความ)
- **Exercise 1d**: ส่ง HTML Table เป็นข้อความเดียว (1 ข้อความ) โดยใช้ Select และ Create HTML table Action

**Flow Structure:**
```
Manual Trigger (JSON Input)
  ↓
Parse JSON
  ↓
Select (แปลง Array เป็นรูปแบบใหม่)
  ↓
Create HTML table (สร้าง HTML Table จาก Array)
  ↓
Post Message (ส่ง HTML Table ไป Teams)
```

**ข้อดีของ HTML Table:**
- ส่งข้อความเดียวแทนหลายข้อความ
- แสดงผลสวยงามและอ่านง่าย
- มี Header และ Formatting
- ใช้พื้นที่น้อยกว่า

พร้อมสำหรับบทต่อไปแล้ว! 🎉

