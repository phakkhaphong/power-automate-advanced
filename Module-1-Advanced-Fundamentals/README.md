# บทที่ 1: Advanced Fundamentals

## 🎯 ภาพรวม

บทนี้จะเสริมความรู้พื้นฐานที่สำคัญสำหรับการสร้าง Flow ขั้นสูง 

**สิ่งที่คุณจะได้เรียนรู้**:
- ✅ Triggers และ Actions ที่ใช้งานจริง
- ✅ Variables (ตัวแปร) สำหรับเก็บข้อมูล
- ✅ Expressions (นิพจน์) สำหรับคำนวณและจัดการข้อมูล
- ✅ Data Types (ประเภทข้อมูล) ต่างๆ

> 💡 **คิดง่ายๆ**: บทนี้เหมือนเรียนคำศัพท์พื้นฐานก่อนที่จะสร้างประโยคยาวๆ

---

## เนื้อหาการเรียนรู้

### 1. Trigger & Action Revisited (ทบทวนทริกเกอร์และแอ็กชัน)

#### Trigger Types (ประเภทของทริกเกอร์)

**Trigger** = สิ่งที่ทำให้ Flow เริ่มทำงาน

**ประเภทของ Trigger**:
- **Manual Trigger** (ทริกเกอร์แบบมือ): กดปุ่มเพื่อรัน Flow (ใช้สำหรับทดสอบ)
- **Scheduled Trigger** (ทริกเกอร์ตามเวลา): รัน Flow อัตโนมัติตามเวลาที่กำหนด (เช่น ทุกวัน 9 นาฬิกา)
- **Event-based Triggers** (ทริกเกอร์ตามเหตุการณ์): รัน Flow เมื่อมีเหตุการณ์เกิดขึ้น (เช่น ได้รับ Email, ไฟล์ใหม่ใน SharePoint)

**Trigger Conditions** (เงื่อนไขทริกเกอร์):
- ตั้งเงื่อนไขว่า Flow จะเริ่มเมื่อไหร่ (เช่น เริ่มเมื่อ Email มีคำว่า "สำคัญ")

#### Action Fundamentals (พื้นฐานของแอ็กชัน)

**Action** = คำสั่งที่ Flow จะทำ

**Action Execution Order** (ลำดับการทำงาน):
- **Sequential** (ตามลำดับ): ทำทีละอัน ตามลำดับ
- **Parallel** (ขนาน): ทำหลายอันพร้อมกัน

**Action Input/Output** (ข้อมูลเข้า/ออก):
- **Input** = ข้อมูลที่ใส่เข้าไป (เช่น Email Address)
- **Output** = ข้อมูลที่ได้ออกมา (เช่น Email ที่ส่งสำเร็จ)

**Action Dependencies** (การอ้างอิง):
- Action หลังสามารถใช้ Output จาก Action ก่อนหน้าได้

---

### 2. Dynamic Data และ Variables (ข้อมูลแบบไดนามิกและตัวแปร)

#### Dynamic Content (เนื้อหาแบบไดนามิก)

**Dynamic Content** = ข้อมูลที่เปลี่ยนได้ตามข้อมูลจริง

**ตัวอย่าง**:
- **Static Content**: "สวัสดี" (ไม่เปลี่ยน)
- **Dynamic Content**: `@{triggerBody()?['name']}` (เปลี่ยนตามชื่อจริง)

**การใช้**:
1. คลิก Dynamic Content ที่แสดงใน Actions
2. หรือใช้ Expressions เพื่อแปลงข้อมูล

**การผสมผสาน**:
- `concat('สวัสดี', variables('name'), '!')` → "สวัสดีสมชาย!"

#### Variables (ตัวแปร)

**Variable** = กล่องเก็บข้อมูลที่สามารถเปลี่ยนค่าได้

**การใช้งานตัวแปร**:
- **Initialize Variable**: สร้างตัวแปรใหม่ (เหมือนสร้างกล่อง)
- **Set Variable**: เปลี่ยนค่าในตัวแปร (เหมือนใส่ของใหม่ในกล่อง)
- **Increment Variable**: เพิ่มค่าตัวแปร (เช่น นับจำนวน +1)

**Variable Scopes** (ขอบเขตตัวแปร):
- **Global**: ใช้ได้ทุกที่ใน Flow
- **Local**: ใช้ได้เฉพาะใน Scope นั้น

#### Variable Types (ประเภทของตัวแปร)

**String** (ข้อความ): เช่น `"สวัสดี"`
**Number** (ตัวเลข): เช่น `123`, `45.67`
**Boolean** (จริง/เท็จ): เช่น `true`, `false`
**Array** (รายการ): เช่น `["a", "b", "c"]`
**Object** (วัตถุ): เช่น `{"name": "สมชาย"}`

---

### 3. Expressions สำคัญ (นิพจน์ที่ใช้บ่อย)

**Expression** = สูตรคำนวณหรือจัดการข้อมูล

#### Coalesce Function (ฟังก์ชันคอยลิส)

**ใช้ทำอะไร**: ตรวจสอบว่าค่าเป็น null หรือไม่ ถ้าเป็น null ให้ใช้ค่า Default

**ตัวอย่าง**:
```
coalesce(triggerBody()?['name'], 'ไม่ระบุชื่อ')
```

**อธิบาย**: ถ้ามีชื่อ → ใช้ชื่อ, ถ้าไม่มีชื่อ → ใช้ "ไม่ระบุชื่อ"

**ใช้เมื่อไหร่**: เมื่อไม่แน่ใจว่าข้อมูลจะมีหรือไม่

#### GUID Function (ฟังก์ชันสร้าง ID)

**ใช้ทำอะไร**: สร้าง ID ที่ไม่ซ้ำ (Unique ID)

**ตัวอย่าง**:
```
guid()
```

**ผลลัพธ์**: `"a1b2c3d4-e5f6-7890-abcd-ef1234567890"`

**ใช้เมื่อไหร่**: สร้าง ID สำหรับ Order, Invoice, หรือ Record ใหม่

#### Replace Function (ฟังก์ชันแทนที่)

**ใช้ทำอะไร**: แทนที่ข้อความใน String

**ตัวอย่าง**:
```
replace('สวัสดีโลก', 'โลก', 'Power Automate')
```

**ผลลัพธ์**: `"สวัสดีPower Automate"`

**ใช้เมื่อไหร่**: แก้ไขข้อความ, ลบข้อความบางส่วน

#### UTC Now Function (ฟังก์ชันวันที่ปัจจุบัน)

**ใช้ทำอะไร**: ดึงวันที่และเวลาปัจจุบัน

**ตัวอย่าง**:
```
utcNow()
```

**ผลลัพธ์**: `"2024-01-15T10:30:00Z"`

**ใช้เมื่อไหร่**: บันทึกเวลา, สร้าง Timestamp

#### String Function (ฟังก์ชันแปลงเป็นข้อความ)

**ใช้ทำอะไร**: แปลงค่าเป็น String (ข้อความ)

**ตัวอย่าง**:
```
string(123)
```

**ผลลัพธ์**: `"123"` (เป็นข้อความ ไม่ใช่ตัวเลข)

**ใช้เมื่อไหร่**: เมื่อต้องการแปลงตัวเลขหรือค่าเป็นข้อความ

#### JSON Function (ฟังก์ชันแปลงเป็น JSON)

**ใช้ทำอะไร**: แปลง Object เป็น JSON String

**ตัวอย่าง**:
```
json({name: 'สมชาย', age: 20})
```

**ผลลัพธ์**: `'{"name":"สมชาย","age":20}'`

**ใช้เมื่อไหร่**: เมื่อต้องการส่งข้อมูลเป็น JSON

---

### 4. Handling Null และ Data Types (จัดการค่า null และประเภทข้อมูล)

#### Null Handling (จัดการค่า null)

**Null** = ไม่มีค่า (ว่างเปล่า)

**วิธีการจัดการ**:
- **Coalesce**: ใช้ค่า Default เมื่อเป็น null
  ```
  coalesce(variables('name'), 'ไม่ระบุชื่อ')
  ```

- **Optional Chaining**: ใช้ `?` เพื่อหลีกเลี่ยง Error
  ```
  object?['property']
  ```
  ถ้า object เป็น null → ไม่ Error, คืนค่า null

- **Null Check**: ตรวจสอบก่อนใช้
  ```
  if(empty(variables('name')), 'ไม่ระบุชื่อ', variables('name'))
  ```

#### Data Types (ประเภทข้อมูล)

**Data Type** = ประเภทของข้อมูล

##### String (ข้อความ)
- **ต่อ String**: `concat('สวัสดี', ' ', 'คุณ')` → `"สวัสดี คุณ"`
- **ตัด String**: `substring('สวัสดี', 0, 3)` → `"สวั"`
- **นับความยาว**: `length('สวัสดี')` → `6`
- **แปลงเป็นตัวใหญ่**: `toUpper('hello')` → `"HELLO"`
- **แปลงเป็นตัวเล็ก**: `toLower('HELLO')` → `"hello"`

##### Number (ตัวเลข)
- **บวก**: `add(10, 5)` → `15`
- **ลบ**: `sub(10, 5)` → `5`
- **คูณ**: `mul(10, 5)` → `50`
- **หาร**: `div(10, 5)` → `2`
- **เปรียบเทียบ**: `greater(10, 5)` → `true`
- **ปัดเศษ**: `round(3.7)` → `4`

##### Boolean (จริง/เท็จ)
- **AND**: `and(true, false)` → `false` (ต้องทั้งสองเป็น true)
- **OR**: `or(true, false)` → `true` (มีอันใดอันหนึ่งเป็น true)
- **NOT**: `not(true)` → `false` (กลับค่า)

##### Array (รายการ)
- **นับจำนวน**: `length(array)` → จำนวน items
- **ดึงตัวแรก**: `first(array)` → item แรก
- **ดึงตัวสุดท้าย**: `last(array)` → item สุดท้าย

##### DateTime (วันที่และเวลา)
- **วันที่ปัจจุบัน**: `utcNow()` → `"2024-01-15T10:30:00Z"`
- **เพิ่มวัน**: `addDays(utcNow(), 7)` → เพิ่ม 7 วัน
- **เพิ่มชั่วโมง**: `addHours(utcNow(), 2)` → เพิ่ม 2 ชั่วโมง
- **จัดรูปแบบ**: `formatDateTime(utcNow(), 'yyyy-MM-dd')` → `"2024-01-15"`

---

## ตัวอย่างการใช้งาน

### ตัวอย่าง 1: ใช้ Coalesce สำหรับ Default Value
```
coalesce(triggerBody()?['email'], 'no-email@example.com')
```

### ตัวอย่าง 2: สร้าง Unique ID
```
concat('PROD-', guid())
```

### ตัวอย่าง 3: แทนที่ข้อความ
```
replace(triggerBody()?['description'], 'old', 'new')
```

### ตัวอย่าง 4: Format DateTime
```
formatDateTime(utcNow(), 'yyyy-MM-dd HH:mm:ss')
```

### ตัวอย่าง 5: Null Safety Check
```
if(empty(triggerBody()?['amount']), 0, triggerBody()?['amount'])
```

---

## Best Practices

1. **Always Use Null Safety**
   - ใช้ `?` สำหรับ Optional Chaining
   - ใช้ `coalesce()` สำหรับ Default Values

2. **Type Conversion**
   - ตรวจสอบ Data Type ก่อนใช้งาน
   - ใช้ Functions เช่น `string()`, `int()`, `float()` เมื่อจำเป็น

3. **Variable Management**
   - ตั้งชื่อตัวแปรให้ชัดเจน
   - ใช้ Variables สำหรับค่าที่ต้องใช้ซ้ำ

4. **Expression Readability**
   - ใช้ Nested Expressions อย่างระมัดระวัง
   - แยก Complex Expressions ออกเป็นหลาย Steps

---

## สรุปบทเรียน

หลังจากจบบทนี้ คุณจะสามารถ:
- ✅ เข้าใจ Trigger และ Action ในระดับ Advanced
- ✅ ใช้ Variables อย่างมีประสิทธิภาพ
- ✅ ใช้ Expressions สำคัญต่างๆ ได้อย่างถูกต้อง
- ✅ จัดการ Null และ Data Types ได้อย่างปลอดภัย
- ✅ แปลงและจัดการ Data Types ต่างๆ

---

## แบบฝึกหัดเพิ่มเติม

1. สร้าง Flow ที่ใช้ Variables เก็บ State ตลอดการทำงาน
2. ใช้ Expressions หลายตัวเพื่อแปลงและจัดรูปแบบข้อมูล
3. สร้าง Flow ที่จัดการ Null Values อย่างปลอดภัย
4. ใช้ DateTime Functions เพื่อคำนวณและจัดรูปแบบวันที่

