# Exercise 1e: Parse JSON → Compose → Report Generation

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่รับข้อมูล JSON
- แปลง JSON String เป็น Object ที่ใช้งานได้ (Parse JSON)
- ใช้ **Compose** เพื่อสร้างรายงานสรุปที่มีการคำนวณ
- ใช้ Expression functions หลายตัวรวมกัน (concat, sum, length, formatNumber)
- สร้างข้อความรายงานแบบ Rich Text ที่มี Formatting

> 💡 **หมายเหตุ**: Exercise นี้แสดงกรณีที่**ต้องใช้ Compose จริงๆ** เพราะ:
> - ต้องใช้ Expression functions หลายตัวรวมกัน (ไม่มี Action โดยตรง)
> - ต้องคำนวณและ Format ข้อมูลหลายขั้นตอน
> - ต้องสร้างข้อความที่ซับซ้อนที่มีการ Format หลายแบบ

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- ความรู้พื้นฐานเรื่อง JSON
- ควรทำ Exercise 1a-1d เสร็จก่อน (เพื่อเข้าใจพื้นฐาน)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: สร้าง Flow ใหม่และตั้งค่า JSON Input

1. เข้าสู่ **Power Automate** (https://powerautomate.microsoft.com)
2. คลิก **Create** → เลือก **Instant cloud flow**
3. ตั้งชื่อ Flow: `Exercise 2e - Parse JSON Compose Report`
4. เลือก Trigger: **Manually trigger a flow**
5. คลิก **Create**
6. ใน **Manually trigger a flow** คลิก **Add an input** → เลือก **JSON**
7. ตั้งชื่อ Input: `JSON input` (หรือชื่ออื่นตามต้องการ)

> 💡 **หมายเหตุ**: JSON Input จะให้คุณสามารถป้อนข้อมูล JSON เมื่อรัน Flow ได้

### ขั้นตอนที่ 2: Parse JSON (แปลง JSON String เป็น Object)

**ก่อนเริ่ม**: เตรียมข้อมูล JSON ตัวอย่างด้านล่างไว้สำหรับทดสอบ:

```json
{
  "year": 2025,
  "employees": [
    {
      "id": 1,
      "name": "สมชาย ใจดี",
      "department": "ไอที",
      "salary": 50000
    },
    {
      "id": 2,
      "name": "สมหญิง รักงาน",
      "department": "ทรัพยากรบุคคล",
      "salary": 45000
    },
    {
      "id": 3,
      "name": "วิชัย ทำงานดี",
      "department": "การเงิน",
      "salary": 55000
    },
    {
      "id": 4,
      "name": "วิไล เก่งมาก",
      "department": "ไอที",
      "salary": 60000
    }
  ]
}
```

> 💡 **หมายเหตุ**: JSON นี้มีโครงสร้าง 2 ชั้น:
> - **ชั้นที่ 1**: Object หลักที่มี Key `year` (ปี) และ `employees` (Array ของพนักงาน)
> - **ชั้นที่ 2**: แต่ละ Object ใน Array `employees` มี Key `id`, `name`, `department`, `salary`

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Parse JSON**
3. ตั้งค่า:
   - **Content**: เลือก `JSON input` จาก Dynamic Content (จาก Manual Trigger)
   - **Schema**: คลิก **Use sample payload to generate schema**
   - วาง JSON ตัวอย่างด้านบนลงไป
   - คลิก **Done**

> 💡 **คำอธิบาย**: Parse JSON จะแปลง JSON String ให้เป็น Object ที่ Power Automate เข้าใจและสามารถใช้งานได้

### ขั้นตอนที่ 3: ใช้ Compose เพื่อคำนวณเงินเดือนรวม

**ทำไมต้องใช้ Compose?** เพราะไม่มี Action ที่คำนวณ sum ของ Array โดยตรง ต้องใช้ Expression `sum()` ร่วมกับ Apply to Each หรือใช้ Compose

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Compose** (หรือ **Data operations** → **Compose**)
3. ตั้งค่า:
   - **Inputs**: คลิก **Expression** และพิมพ์:

```
sum(select(body('Parse_JSON')?['employees'], item()?['salary']))
```

> 💡 **คำอธิบาย**: 
> - `select()` → ใช้ดึงเฉพาะ `salary` จากแต่ละ employee
> - `sum()` → รวมค่าทั้งหมด
> - **นี่คือกรณีที่ต้องใช้ Compose**: เพราะไม่มี Action ที่คำนวณ sum โดยตรง

**ผลลัพธ์**: `210000` (50000 + 45000 + 55000 + 60000)

### ขั้นตอนที่ 4: ใช้ Compose เพื่อนับจำนวนพนักงาน

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Compose**
3. ตั้งค่า:
   - **Inputs**: คลิก **Expression** และพิมพ์:

```
length(body('Parse_JSON')?['employees'])
```

> 💡 **คำอธิบาย**: `length()` คำนวณจำนวน item ใน Array
> - **นี่คือกรณีที่ต้องใช้ Compose**: เพราะไม่มี Action ที่นับจำนวน Array โดยตรง

**ผลลัพธ์**: `4`

### ขั้นตอนที่ 5: ใช้ Compose เพื่อคำนวณเงินเดือนเฉลี่ย

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Compose**
3. ตั้งค่า:
   - **Inputs**: คลิก **Expression** และพิมพ์:

```
div(sum(select(body('Parse_JSON')?['employees'], item()?['salary'])), length(body('Parse_JSON')?['employees']))
```

> 💡 **คำอธิบาย**: 
> - `div()` → หาร (เงินเดือนรวม / จำนวนพนักงาน)
> - **นี่คือกรณีที่ต้องใช้ Compose**: เพราะต้องใช้ Expression functions หลายตัวรวมกัน

**ผลลัพธ์**: `52500` (210000 / 4)

### ขั้นตอนที่ 6: ใช้ Compose เพื่อสร้างรายงานสรุป (Rich Text Formatting)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Compose**
3. ตั้งค่า:
   - **Inputs**: คลิก **Expression** และพิมพ์:

```
concat(
  '📊 รายงานสรุปข้อมูลพนักงานปี ', 
  body('Parse_JSON')?['year'], 
  '<br><br>',
  '👥 จำนวนพนักงานทั้งหมด: ', 
  length(body('Parse_JSON')?['employees']), 
  ' คน<br>',
  '💰 เงินเดือนรวม: ฿', 
  formatNumber(sum(select(body('Parse_JSON')?['employees'], item()?['salary'])), '#,##0'), 
  '<br>',
  '📈 เงินเดือนเฉลี่ย: ฿', 
  formatNumber(div(sum(select(body('Parse_JSON')?['employees'], item()?['salary'])), length(body('Parse_JSON')?['employees'])), '#,##0'), 
  '<br><br>',
  '📅 สร้างรายงานเมื่อ: ', 
  formatDateTime(utcNow(), 'dd/MM/yyyy HH:mm น.')
)
```

> 💡 **คำอธิบาย**: 
> - **นี่คือกรณีที่ต้องใช้ Compose**: เพราะต้องใช้ Expression functions หลายตัวรวมกัน:
>   - `concat()` → รวมข้อความ
>   - `length()` → นับจำนวน
>   - `sum()` → รวมค่า
>   - `select()` → ดึงข้อมูลจาก Array
>   - `formatNumber()` → Format ตัวเลข
>   - `div()` → หาร
>   - `formatDateTime()` → Format วันที่/เวลา
>   - `utcNow()` → วันที่/เวลาปัจจุบัน
> - ไม่มี Action เดียวที่ทำทั้งหมดนี้ได้

**ผลลัพธ์ของรายงาน:**
```
📊 รายงานสรุปข้อมูลพนักงานปี 2025

👥 จำนวนพนักงานทั้งหมด: 4 คน
💰 เงินเดือนรวม: ฿210,000
📈 เงินเดือนเฉลี่ย: ฿52,500

📅 สร้างรายงานเมื่อ: 15/01/2025 14:30 น.
```

### ขั้นตอนที่ 7: ส่งรายงานไปยัง Teams

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Message**: เลือก `Outputs` จาก **Compose** Action ขั้นตอนที่ 6 (รายงานสรุป)

> 💡 **คำอธิบาย**: Action นี้จะส่งรายงานสรุปไปยัง Teams Chat/Channel

### ขั้นตอนที่ 8: ทดสอบ Flow

1. คลิก **Save**
2. คลิก **Test** → **Manually** → **Run flow**
3. ในหน้า **Run flow**:
   - วาง JSON ตัวอย่างด้านบนลงในช่อง **JSON input**
   - คลิก **Run flow**
4. รอให้ Flow รันเสร็จ
5. ตรวจสอบผลลัพธ์:
   - ดู Output ของ Compose แต่ละขั้นตอน (เงินเดือนรวม, จำนวน, เฉลี่ย)
   - ตรวจสอบข้อความรายงานใน Teams Chat

---

## 📊 ผลลัพธ์ที่คาดหวัง

คุณควรจะเห็นรายงานสรุปใน Teams Chat ที่มี:
- **Header**: รายงานสรุปข้อมูลพนักงานปี 2025
- **จำนวนพนักงาน**: 4 คน
- **เงินเดือนรวม**: ฿210,000 (Format ด้วย comma)
- **เงินเดือนเฉลี่ย**: ฿52,500 (Format ด้วย comma)
- **วันที่สร้างรายงาน**: วันที่/เวลาปัจจุบัน (Format แบบไทย)

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **JSON Basics**: เข้าใจโครงสร้าง JSON Objects และ Arrays (2 ชั้น)  
✅ **Parse JSON**: แปลง JSON String เป็น Object ที่ใช้งานได้  
✅ **Compose Action**: ใช้ Compose สำหรับ Expression ที่ซับซ้อน  
✅ **Expression Functions**: ใช้ functions หลายตัวรวมกัน:
  - `sum()` - รวมค่า
  - `length()` - นับจำนวน
  - `select()` - ดึงข้อมูลจาก Array
  - `div()` - หาร
  - `formatNumber()` - Format ตัวเลข
  - `formatDateTime()` - Format วันที่/เวลา
  - `utcNow()` - วันที่/เวลาปัจจุบัน
  - `concat()` - รวมข้อความ  
✅ **Rich Text Formatting**: สร้างข้อความที่มี HTML formatting  
✅ **Teams Integration**: ส่งรายงานไปยัง Teams Chat/Channel  

---

## 💡 เมื่อไหร่ควรใช้ Compose?

**ใช้ Compose เมื่อ:**
1. ✅ **ต้องใช้ Expression functions หลายตัวรวมกัน** (เช่น sum, length, formatNumber)
2. ✅ **ต้องคำนวณข้อมูลที่ไม่มี Action โดยตรง** (เช่น หาเฉลี่ย, หาร)
3. ✅ **ต้อง Format ข้อมูลหลายขั้นตอน** (เช่น Format ตัวเลข + Format วันที่)
4. ✅ **ต้องสร้าง JSON/XML String** สำหรับส่งต่อ
5. ✅ **ต้อง Debug/Troubleshoot Expression** (ดู Output ของ Expression)
6. ✅ **ต้องใช้ Expression functions ที่ซับซ้อน** (เช่น nested functions)

**ไม่ต้องใช้ Compose เมื่อ:**
1. ❌ **มี Action โดยตรง** (เช่น Join, Create HTML table, Select)
2. ❌ **Expression ง่ายๆ** (เช่น concat แบบง่ายๆ ที่ใช้ใน Action อื่นได้)
3. ❌ **แค่ Format ข้อมูลง่ายๆ** (ใช้ใน Action นั้นๆ ได้เลย)

---

## 💡 Tips & Tricks

1. **ทำไมต้องใช้ Compose แทน Expression ใน Action อื่น?**
   - เมื่อ Expression ซับซ้อนมาก ทำให้อ่านยาก
   - เมื่อต้องการ Debug โดยดู Output แยก
   - เมื่อต้องการ Reuse Expression หลายครั้ง

2. **Expression Functions ที่ใช้บ่อย:**
   - `sum(array)` → รวมค่าทั้งหมดใน Array
   - `length(array)` → นับจำนวน item
   - `select(array, expression)` → ดึงข้อมูลจาก Array
   - `div(number1, number2)` → หาร (number1 / number2)
   - `formatNumber(number, format)` → Format ตัวเลข (`#,##0` = 1,000)
   - `formatDateTime(datetime, format)` → Format วันที่/เวลา
   - `utcNow()` → วันที่/เวลาปัจจุบัน (UTC)
   - `concat(...)` → รวมข้อความ

3. **Format Patterns:**
   - `formatNumber(50000, '#,##0')` → `50,000`
   - `formatNumber(50000, '#,##0.00')` → `50,000.00`
   - `formatDateTime(utcNow(), 'dd/MM/yyyy')` → `15/01/2025`
   - `formatDateTime(utcNow(), 'dd/MM/yyyy HH:mm น.')` → `15/01/2025 14:30 น.`

4. **การ Debug Expression:**
   - ใช้ Compose เพื่อทดสอบ Expression ก่อน
   - ดู Output ของ Compose เพื่อตรวจสอบว่าถูกต้องหรือไม่
   - แล้วค่อยใช้ Expression นั้นใน Action อื่น

5. **การ Reuse Expression:**
   - ใช้ Compose เพื่อสร้าง Expression ครั้งเดียว
   - แล้วใช้ Output จาก Compose ใน Action อื่นๆ

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **คำนวณเงินเดือนสูงสุด/ต่ำสุด**:
   ```
   max(select(body('Parse_JSON')?['employees'], item()?['salary']))
   min(select(body('Parse_JSON')?['employees'], item()?['salary']))
   ```

2. **นับพนักงานตามแผนก**: ใช้ Filter Array + length() เพื่อนับพนักงานแต่ละแผนก

3. **สร้าง JSON String สำหรับส่งต่อ**:
   ```
   json(concat('{"total": ', sum(select(body('Parse_JSON')?['employees'], item()?['salary'])), ', "count": ', length(body('Parse_JSON')?['employees']), '}'))
   ```

4. **คำนวณเปอร์เซ็นต์**: หาเปอร์เซ็นต์ของพนักงานที่เงินเดือน > 50000

5. **Format วันที่แบบไทย**: สร้างวันที่แบบ "วันนี้ 15 มกราคม 2568"

6. **สร้าง Report แบบ CSV Format**:
   ```
   concat('Year,Total Employees,Total Salary,Average Salary', '<br>', body('Parse_JSON')?['year'], ',', length(body('Parse_JSON')?['employees']), ',', sum(select(body('Parse_JSON')?['employees'], item()?['salary'])), ',', div(sum(select(body('Parse_JSON')?['employees'], item()?['salary'])), length(body('Parse_JSON')?['employees'])))
   ```

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: ทำไมต้องใช้ Compose แทน Expression ใน Action อื่น?**  
A: เมื่อ Expression ซับซ้อนมาก (ใช้ functions หลายตัว) หรือต้องการ Debug โดยดู Output แยก

**Q: Compose กับ Actions อื่นๆ ต่างกันอย่างไร?**  
A: Compose ใช้สำหรับ Expression ที่ซับซ้อน, Actions อื่นๆ มีฟังก์ชันเฉพาะ (เช่น Join, Select, Create HTML table)

**Q: เมื่อไหร่ควรใช้ Compose?**  
A: เมื่อต้องใช้ Expression functions หลายตัวรวมกัน, คำนวณข้อมูลที่ไม่มี Action โดยตรง, หรือต้อง Format ข้อมูลหลายขั้นตอน

**Q: Expression Functions ที่ใช้บ่อยคืออะไร?**  
A: `sum()`, `length()`, `select()`, `div()`, `formatNumber()`, `formatDateTime()`, `concat()`, `utcNow()`

**Q: สามารถใช้ Compose เพื่อ Debug Expression ได้ไหม?**  
A: ได้ Compose เป็นวิธีที่ดีในการทดสอบและ Debug Expression ก่อนใช้ใน Action อื่น

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การรับและ Parse JSON
- **การใช้ Compose สำหรับ Expression ที่ซับซ้อน**
- **การใช้ Expression functions หลายตัวรวมกัน** (sum, length, select, div, formatNumber, formatDateTime)
- **การสร้างรายงานสรุปที่มีการคำนวณและ Formatting**
- การส่งรายงานไปยัง Teams Chat/Channel

**กรณีที่ต้องใช้ Compose จริงๆ:**
- ✅ ต้องใช้ Expression functions หลายตัวรวมกัน
- ✅ ต้องคำนวณข้อมูลที่ไม่มี Action โดยตรง (เช่น sum, average)
- ✅ ต้อง Format ข้อมูลหลายขั้นตอน
- ✅ ต้อง Debug/Troubleshoot Expression
- ✅ ต้องสร้าง JSON/XML String

**Flow Structure:**
```
Manual Trigger (JSON Input)
  ↓
Parse JSON
  ↓
Compose (คำนวณเงินเดือนรวม - sum)
  ↓
Compose (นับจำนวนพนักงาน - length)
  ↓
Compose (คำนวณเงินเดือนเฉลี่ย - div)
  ↓
Compose (สร้างรายงานสรุป - concat + formatNumber + formatDateTime)
  ↓
Post Message (ส่งรายงานไป Teams)
```

**ข้อดีของการใช้ Compose:**
- ✅ แสดง Output ของ Expression เพื่อ Debug
- ✅ Reuse Expression หลายครั้ง
- ✅ อ่าน Expression ที่ซับซ้อนได้ง่ายขึ้น
- ✅ ใช้ Expression functions ที่ไม่มี Action โดยตรง

พร้อมสำหรับบทต่อไปแล้ว! 🎉

