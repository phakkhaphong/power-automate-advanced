# Exercise 3g: Terminate Action → Switch → Random Status

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่ใช้ **Manual Trigger**
- ใช้ **Compose** action พร้อม Expression `rand()` เพื่อสุ่มตัวเลข
- ใช้ **Switch Condition** เพื่อตรวจสอบค่าที่สุ่มได้
- ใช้ **Terminate** action เพื่อหยุด Flow ด้วย Status ต่างๆ (Succeeded, Failed, Cancelled)
- เข้าใจความแตกต่างของ Terminate Status และการใช้งาน

> 💡 **หมายเหตุ**: Exercise นี้แสดงการใช้งาน **Terminate Action**:
> - Terminate ใช้เพื่อหยุด Flow ด้วย Status ที่กำหนด
> - Status ที่ใช้ได้: Succeeded, Failed, Cancelled
> - ใช้ร่วมกับ Switch เพื่อจัดการ Status ต่างๆ

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- ความรู้พื้นฐานเรื่อง Flow Creation และการใช้งาน Expression
- ความรู้พื้นฐานเรื่อง Switch Condition

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: สร้าง Flow ใหม่

1. เข้าสู่ **Power Automate** (https://make.powerautomate.com/)
2. คลิก **Create** → เลือก **Instant cloud flow**
3. ตั้งชื่อ Flow: `Exercise 3g - Terminate Action Switch`
4. เลือก Trigger: **Manually trigger a flow**
5. คลิก **Create**

### ขั้นตอนที่ 2: เพิ่ม Compose Action พร้อม rand() Expression

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Compose** (หรือ **Data operations** → **Compose**)
3. ตั้งค่า **Inputs**:
   - คลิก **Expression** (fx) เพื่อเปิด Expression Editor
   - พิมพ์ Expression:
     ```
     rand(1,4)
     ```
   - คลิก **OK** หรือ **Update**

> 💡 **คำอธิบาย**: 
> - `rand(1,4)` → สุ่มตัวเลขระหว่าง 1 ถึง 3 (ไม่รวม 4)
> - `rand()` → Function ที่สุ่มตัวเลขระหว่าง 2 ค่า
> - **Syntax**: `rand(min, max)` → สุ่มตัวเลขตั้งแต่ min จนถึง max-1
> - ในกรณีนี้ `rand(1,4)` จะได้ค่า 1, 2, หรือ 3

**Expression Details**:
- `rand(1,4)` → สุ่มตัวเลข 1, 2, หรือ 3
- Output จะถูกเก็บไว้ใน `outputs('Compose')` หรือ `body('Compose')`

**ผลลัพธ์ที่คาดหวัง**:
- ครั้งที่ 1: อาจได้ `1`
- ครั้งที่ 2: อาจได้ `2`
- ครั้งที่ 3: อาจได้ `3`
- (ค่าไม่แน่นอน เพราะเป็น Random)

### ขั้นตอนที่ 3: เพิ่ม Switch Condition

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Switch** (หรือ **Control** → **Switch**)
3. ตั้งค่า:
   - **On**: เลือก `Outputs` จาก Dynamic Content (จาก Compose action)
     หรือใช้ Expression: `outputs('Compose')`

> 💡 **คำอธิบาย**: Switch จะตรวจสอบค่าที่สุ่มได้จาก Compose และทำงานตาม Case ที่ตรงกัน
> - **Case 1**: ถ้าได้ `1` → Terminate with "Succeeded"
> - **Case 2**: ถ้าได้ `2` → Terminate with "Failed"
> - **Case 3**: ถ้าได้ `3` → Terminate with "Cancelled"
> - **Default**: ถ้าได้ค่าอื่น (ไม่น่าจะเกิดขึ้น) → ไม่ทำอะไร

**Switch Structure**:
```
Switch: outputs('Compose')
  ├─ Case 1: Equals 1 → Terminate (Succeeded)
  ├─ Case 2: Equals 2 → Terminate (Failed)
  ├─ Case 3: Equals 3 → Terminate (Cancelled)
  └─ Default: (Empty)
```

### ขั้นตอนที่ 4: เพิ่ม Case 1 - Terminate with "Succeeded"

1. **ภายใน Case 1** (คลิกที่ Case 1 หรือ **Add case** ถ้ายังไม่มี)
2. ตั้งค่า Case:
   - **Equals**: พิมพ์ `1` (เป็น Number)

3. **ภายใน Case 1** คลิก **Add an action**
4. ค้นหาและเลือก **Terminate** (หรือ **Control** → **Terminate**)
5. ตั้งค่า:
   - **Status**: เลือก `Succeeded` จาก Dropdown

> 💡 **คำอธิบาย**: Terminate action จะหยุด Flow และตั้ง Status เป็น "Succeeded"
> - **Status**: "Succeeded" → Flow ทำงานสำเร็จ (Success)
> - Flow จะหยุดทันทีและไม่ทำงาน Actions ที่อยู่หลัง Terminate

**Terminate Status Options**:
- **Succeeded**: Flow ทำงานสำเร็จ (Success)
- **Failed**: Flow ล้มเหลว (Failure)
- **Cancelled**: Flow ถูกยกเลิก (Cancellation)

### ขั้นตอนที่ 5: เพิ่ม Case 2 - Terminate with "Failed"

1. **ภายใน Switch** คลิก **Add case** เพื่อเพิ่ม Case 2
2. ตั้งค่า Case:
   - **Equals**: พิมพ์ `2` (เป็น Number)

3. **ภายใน Case 2** คลิก **Add an action**
4. ค้นหาและเลือก **Terminate**
5. ตั้งค่า:
   - **Status**: เลือก `Failed` จาก Dropdown

> 💡 **คำอธิบาย**: Terminate action จะหยุด Flow และตั้ง Status เป็น "Failed"
> - **Status**: "Failed" → Flow ล้มเหลว (Failure)
> - Flow จะหยุดทันทีและแสดงสถานะเป็น Failed

### ขั้นตอนที่ 6: เพิ่ม Case 3 - Terminate with "Cancelled"

1. **ภายใน Switch** คลิก **Add case** เพื่อเพิ่ม Case 3
2. ตั้งค่า Case:
   - **Equals**: พิมพ์ `3` (เป็น Number)

3. **ภายใน Case 3** คลิก **Add an action**
4. ค้นหาและเลือก **Terminate**
5. ตั้งค่า:
   - **Status**: เลือก `Cancelled` จาก Dropdown

> 💡 **คำอธิบาย**: Terminate action จะหยุด Flow และตั้ง Status เป็น "Cancelled"
> - **Status**: "Cancelled" → Flow ถูกยกเลิก (Cancellation)
> - Flow จะหยุดทันทีและแสดงสถานะเป็น Cancelled

### ขั้นตอนที่ 7: ทดสอบ Flow

1. คลิก **Save**
2. คลิก **Test** → **Manually** → **Run flow**
3. รัน Flow หลายครั้งเพื่อดูผลลัพธ์ที่แตกต่างกัน:
   - **ครั้งที่ 1**: อาจได้ `1` → Flow หยุดด้วย Status "Succeeded"
   - **ครั้งที่ 2**: อาจได้ `2` → Flow หยุดด้วย Status "Failed"
   - **ครั้งที่ 3**: อาจได้ `3` → Flow หยุดด้วย Status "Cancelled"

4. ตรวจสอบผลลัพธ์:
   - ดู Run History เพื่อดูว่า Flow หยุดด้วย Status อะไร
   - ตรวจสอบว่า Flow หยุดทันทีหลังจาก Terminate

---

## 📊 ผลลัพธ์ที่คาดหวัง

### กรณีที่ 1: rand() = 1

คุณควรจะเห็น:
- ✅ **Compose**: Output = `1`
- ✅ **Switch**: ตรงกับ Case 1
- ✅ **Terminate**: Status = "Succeeded"
- ✅ **Flow Status**: "Succeeded" (ใน Run History)

### กรณีที่ 2: rand() = 2

คุณควรจะเห็น:
- ✅ **Compose**: Output = `2`
- ✅ **Switch**: ตรงกับ Case 2
- ✅ **Terminate**: Status = "Failed"
- ✅ **Flow Status**: "Failed" (ใน Run History)

### กรณีที่ 3: rand() = 3

คุณควรจะเห็น:
- ✅ **Compose**: Output = `3`
- ✅ **Switch**: ตรงกับ Case 3
- ✅ **Terminate**: Status = "Cancelled"
- ✅ **Flow Status**: "Cancelled" (ใน Run History)

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **Compose Action**: ใช้ Compose เพื่อสร้าง Expression ที่ซับซ้อน  
✅ **rand() Function**: ใช้ `rand(min, max)` เพื่อสุ่มตัวเลข  
✅ **Switch Condition**: ใช้ Switch เพื่อจัดการหลาย Case จากค่าเดียว  
✅ **Terminate Action**: ใช้ Terminate เพื่อหยุด Flow ด้วย Status ที่กำหนด  
✅ **Terminate Status**: เข้าใจความแตกต่างของ Status ต่างๆ (Succeeded, Failed, Cancelled)  
✅ **Flow Control**: ใช้ Terminate เพื่อควบคุมการทำงานของ Flow  

---

## 💡 Terminate Action: เมื่อไหร่ควรใช้?

### ใช้ Terminate เมื่อ:
- ✅ ต้องการหยุด Flow ทันทีเมื่อเงื่อนไขบางอย่างเป็นจริง
- ✅ ต้องการตั้ง Status ของ Flow ให้ชัดเจน (Succeeded, Failed, Cancelled)
- ✅ ต้องการป้องกัน Flow ทำงานต่อหลังจากเงื่อนไขบางอย่าง
- ✅ ต้องการจัดการ Error Handling แบบชัดเจน

**ตัวอย่างการใช้งาน**:
```
Condition: Error occurred
  ├─ True: Terminate (Failed)
  └─ False: Continue processing
```

### ไม่ใช้ Terminate เมื่อ:
- ❌ ต้องการให้ Flow ทำงานต่อหลังจากเงื่อนไข
- ❌ ต้องการให้ Flow ทำงานเสร็จสมบูรณ์
- ❌ ต้องการให้ Actions อื่นๆ ทำงานต่อ

---

## 💡 Terminate Status: ความแตกต่างของแต่ละ Status

### 1. Succeeded (สำเร็จ)
- **ความหมาย**: Flow ทำงานสำเร็จ
- **ใช้เมื่อ**: งานเสร็จสมบูรณ์ตามที่ต้องการ
- **ตัวอย่าง**: การประมวลผลข้อมูลสำเร็จ, การส่ง Email สำเร็จ

### 2. Failed (ล้มเหลว)
- **ความหมาย**: Flow ล้มเหลว
- **ใช้เมื่อ**: เกิด Error หรือไม่สามารถทำงานต่อได้
- **ตัวอย่าง**: API Call ล้มเหลว, ข้อมูลไม่ถูกต้อง, Validation Failed

### 3. Cancelled (ยกเลิก)
- **ความหมาย**: Flow ถูกยกเลิก
- **ใช้เมื่อ**: ต้องการยกเลิกการทำงานโดยไม่ถือว่าเป็น Error
- **ตัวอย่าง**: User ยกเลิก, เงื่อนไขไม่เหมาะสม, Skip Processing

---

## 💡 Tips & Tricks

1. **การใช้ rand() Function**:
   - `rand(min, max)` → สุ่มตัวเลขตั้งแต่ min จนถึง max-1
   - `rand(1,4)` → สุ่มตัวเลข 1, 2, หรือ 3 (ไม่รวม 4)
   - `rand(0,10)` → สุ่มตัวเลข 0, 1, 2, ..., 9 (ไม่รวม 10)

2. **Terminate Action Best Practices**:
   - ใช้ Terminate เมื่อต้องการหยุด Flow ทันที
   - เลือก Status ให้เหมาะสมกับสถานการณ์
   - ใช้ร่วมกับ Condition หรือ Switch เพื่อจัดการ Status ต่างๆ

3. **การ Debug Terminate**:
   - ตรวจสอบ Run History เพื่อดูว่า Flow หยุดด้วย Status อะไร
   - ใช้ Compose เพื่อดูค่าก่อน Terminate
   - ตรวจสอบว่า Terminate ทำงานถูกต้อง

4. **Terminate vs Continue**:
   - Terminate → หยุด Flow ทันที
   - Continue → Flow ทำงานต่อ
   - ใช้ Terminate เมื่อต้องการหยุด Flow อย่างชัดเจน

5. **การจัดการ Error**:
   - ใช้ Terminate (Failed) เมื่อเกิด Error
   - ใช้ Configure run after เพื่อจัดการ Error
   - ใช้ Scope เพื่อแยก Error Handling

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **เพิ่ม Default Case**:
   - เพิ่ม Default case ใน Switch
   - เพิ่ม Terminate (Cancelled) ใน Default case
   - ดูว่า Default case ทำงานเมื่อไหร่

2. **เปลี่ยน Status**:
   - เปลี่ยน Status ในแต่ละ Case
   - ดูว่าผลลัพธ์เปลี่ยนแปลงอย่างไร

3. **เพิ่ม Condition ก่อน Terminate**:
   - เพิ่ม Condition เพื่อตรวจสอบเงื่อนไขเพิ่มเติม
   - ถ้าเงื่อนไขเป็นจริง → Terminate
   - ถ้าไม่ใช่ → Continue

4. **ใช้ Terminate ใน Loop**:
   - เพิ่ม Terminate ใน Apply to Each หรือ Do until
   - ดูว่า Loop หยุดเมื่อไหร่

5. **เพิ่ม Logging ก่อน Terminate**:
   - เพิ่ม Compose เพื่อบันทึกค่าก่อน Terminate
   - เพิ่ม Post message เพื่อแจ้งเตือนก่อน Terminate

6. **ใช้ Terminate กับ Error Handling**:
   - ใช้ Configure run after เพื่อจัดการ Error
   - ใช้ Terminate (Failed) เมื่อเกิด Error
   - ใช้ Terminate (Succeeded) เมื่อทำงานสำเร็จ

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: Terminate Action คืออะไร?**  
A: Terminate Action คือ Action ที่ใช้หยุด Flow ทันทีและตั้ง Status ของ Flow (Succeeded, Failed, Cancelled)

**Q: Terminate กับ Stop Flow ต่างกันอย่างไร?**  
A: Terminate เป็น Action ที่สามารถตั้ง Status ได้, Stop Flow เป็นการหยุด Flow โดยทั่วไป (ไม่มี Status ที่ชัดเจน)

**Q: Terminate Status มีอะไรบ้าง?**  
A: มี 3 Status: **Succeeded** (สำเร็จ), **Failed** (ล้มเหลว), **Cancelled** (ยกเลิก)

**Q: เมื่อไหร่ควรใช้ Terminate?**  
A: เมื่อต้องการหยุด Flow ทันทีเมื่อเงื่อนไขบางอย่างเป็นจริง หรือต้องการตั้ง Status ของ Flow ให้ชัดเจน

**Q: Terminate จะหยุด Flow ทันทีหรือไม่?**  
A: ใช่, Terminate จะหยุด Flow ทันทีและไม่ทำงาน Actions ที่อยู่หลัง Terminate

**Q: rand() Function ทำงานอย่างไร?**  
A: `rand(min, max)` จะสุ่มตัวเลขตั้งแต่ min จนถึง max-1 (ไม่รวม max)

**Q: วิธีทดสอบ Terminate?**  
A: รัน Flow หลายครั้งเพื่อดูผลลัพธ์ที่แตกต่างกัน, ตรวจสอบ Run History เพื่อดู Status ของ Flow

**Q: สามารถใช้ Terminate ใน Loop ได้ไหม?**  
A: ได้, สามารถใช้ Terminate ใน Apply to Each หรือ Do until เพื่อหยุด Loop

**Q: Terminate vs Return (ใน Child Flow)?**  
A: Terminate ใช้สำหรับหยุด Flow ปัจจุบัน, Return ใช้สำหรับส่งผลลัพธ์กลับไปยัง Parent Flow

**Q: วิธีใช้ Expression ใน Terminate?**  
A: Terminate ไม่มี Expression Field, แต่สามารถใช้ร่วมกับ Condition หรือ Switch เพื่อจัดการ Status

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การใช้ Compose action พร้อม Expression `rand()`
- **การใช้ Terminate Action เพื่อหยุด Flow**
- **การใช้ Switch Condition กับ Terminate**
- **ความแตกต่างของ Terminate Status ต่างๆ**
- การควบคุมการทำงานของ Flow ด้วย Terminate

**Flow Structure**:
```
Manual Trigger
  ↓
Compose: rand(1,4)
  ↓
Switch: outputs('Compose')
  ├─ Case 1: Equals 1 → Terminate (Succeeded)
  ├─ Case 2: Equals 2 → Terminate (Failed)
  ├─ Case 3: Equals 3 → Terminate (Cancelled)
  └─ Default: (Empty)
```

**Terminate Logic**:
```
1. สุ่มตัวเลขด้วย rand(1,4) → ได้ค่า 1, 2, หรือ 3
2. Switch ตรวจสอบค่า:
   - ถ้า = 1 → Terminate (Succeeded)
   - ถ้า = 2 → Terminate (Failed)
   - ถ้า = 3 → Terminate (Cancelled)
3. Flow หยุดทันทีหลังจาก Terminate
```

**Terminate Status**:
- ✅ **Succeeded**: Flow ทำงานสำเร็จ (Success)
- ❌ **Failed**: Flow ล้มเหลว (Failure)
- ⚠️ **Cancelled**: Flow ถูกยกเลิก (Cancellation)

**ข้อดีของการใช้ Terminate**:
- ✅ หยุด Flow ทันทีเมื่อเงื่อนไขเป็นจริง
- ✅ ตั้ง Status ของ Flow ให้ชัดเจน
- ✅ ป้องกัน Flow ทำงานต่อหลังจากเงื่อนไขบางอย่าง
- ✅ จัดการ Error Handling แบบชัดเจน

**เมื่อไหร่ควรใช้ Terminate**:
- ✅ เมื่อต้องการหยุด Flow ทันทีเมื่อเงื่อนไขบางอย่างเป็นจริง
- ✅ เมื่อต้องการตั้ง Status ของ Flow ให้ชัดเจน
- ✅ เมื่อต้องการป้องกัน Flow ทำงานต่อหลังจาก Error
- ✅ เมื่อต้องการจัดการ Error Handling แบบชัดเจน

**คำแนะนำ**:
- ⚠️ ใช้ Terminate เมื่อต้องการหยุด Flow อย่างชัดเจน
- ⚠️ เลือก Status ให้เหมาะสมกับสถานการณ์
- ⚠️ ใช้ร่วมกับ Condition หรือ Switch เพื่อจัดการ Status ต่างๆ
- ⚠️ ตรวจสอบ Run History เพื่อดู Status ของ Flow

พร้อมสำหรับบทต่อไปแล้ว! 🎉

