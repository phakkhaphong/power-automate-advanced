# Exercise 3a: Condition → If-Else → Email/Teams

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่รับ Input Parameters จาก Manual Trigger
- ใช้ **Condition** (If-Else) เพื่อตรวจสอบเงื่อนไข
- ส่ง Email เมื่อเงื่อนไขเป็นจริง (True)
- ส่งข้อความไป Teams เมื่อเงื่อนไขเป็นเท็จ (False)

> 💡 **หมายเหตุ**: Exercise นี้แสดงการใช้งาน **Condition** (If-Else) แบบพื้นฐาน:
> - ตรวจสอบเงื่อนไขเดียว
> - มี 2 ทางเลือก (True/False)
> - แยกการทำงานตามเงื่อนไข

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- ความรู้พื้นฐานเรื่อง Flow Creation
- Email Account ที่เชื่อมต่อกับ Power Automate
- Teams Account (สำหรับส่งข้อความ)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: สร้าง Flow ใหม่

1. เข้าสู่ **Power Automate** (https://powerautomate.microsoft.com)
2. คลิก **Create** → เลือก **Instant cloud flow**
3. ตั้งชื่อ Flow: `Exercise 3a - Condition If Else Email Teams`
4. เลือก Trigger: **Manually trigger a flow**
5. คลิก **Create**

### ขั้นตอนที่ 2: เพิ่ม Input Parameters ใน Manual Trigger

1. ใน **Manually trigger a flow** คลิก **Add an input** → เลือก **Text**
2. ตั้งชื่อ Input: `Delivery`
3. เพิ่ม placeholder: `Please enter your delivery method` (เช่น `Email`, `Teams`, `SMS`)
4. คลิก **Add an input** อีกครั้ง → เลือก **Text**
5. ตั้งชื่อ Input: `Message`
6. เพิ่ม placeholder: `Please enter your message`
7. คลิก **Save**

> 💡 **หมายเหตุ**: Input Parameters จะทำให้คุณสามารถป้อนข้อมูลเมื่อรัน Flow ได้
> - **Delivery**: ระบุวิธีการส่งข้อความ (เช่น `Email`, `Teams`)
> - **Message**: ข้อความที่ต้องการส่ง

**ตัวอย่างการใช้งาน**:
- **Delivery**: `Email`
- **Message**: `สวัสดีครับ นี่คือข้อความทดสอบ`

### ขั้นตอนที่ 3: เพิ่ม Condition (If-Else)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Condition** (หรือ **Control** → **Condition**)
3. ตั้งค่า Condition Expression:
   - **Field 1**: เลือก `Delivery` จาก Dynamic Content (จาก Manual Trigger)
   - **Operator**: เลือก `is equal to`
   - **Field 2**: พิมพ์ `Email` (เป็น Text)

> 💡 **คำอธิบาย**: Condition จะตรวจสอบว่า `Delivery` เท่ากับ `Email` หรือไม่
> - ถ้า **True** (Delivery = "Email") → ส่ง Email
> - ถ้า **False** (Delivery ≠ "Email") → ส่งข้อความไป Teams

**Condition Structure**:
```
Condition: Delivery is equal to "Email"
  ├─ True Branch: Send Email
  └─ False Branch: Send Teams Message
```

### ขั้นตอนที่ 4: เพิ่ม Action ใน True Branch (ส่ง Email)

1. **ภายใน True Branch** (ด้านซ้าย) คลิก **Add an action**
2. ค้นหาและเลือก **Send an email (V2)** (หรือ **Office 365 Outlook** → **Send an email (V2)**)
3. ตั้งค่า:
   - **To**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Subject**: ใช้ Dynamic Content หรือ Expression:
     ```
     แจ้งเตือน: ข้อความจาก Flow
     ```
     หรือใช้ Expression:
     ```
     concat('แจ้งเตือน: ข้อความจาก Flow - ', utcNow())
     ```
   - **Body**: เลือก `Message` จาก Dynamic Content (จาก Manual Trigger)
   - **Importance**: `Normal` (หรือ `High` ตามต้องการ)

> 💡 **คำอธิบาย**: Action นี้จะส่ง Email เมื่อ `Delivery` เท่ากับ `Email`
> - ใช้ `Message` จาก Manual Trigger เป็นเนื้อหาของ Email

**ตัวอย่าง Email ที่ส่งออกไป**:
- **To**: `your-email@example.com`
- **Subject**: `แจ้งเตือน: ข้อความจาก Flow`
- **Body**: `สวัสดีครับ นี่คือข้อความทดสอบ`

### ขั้นตอนที่ 5: เพิ่ม Action ใน False Branch (ส่งข้อความไป Teams)

1. **ภายใน False Branch** (ด้านขวา) คลิก **Add an action**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Message**: เลือก `Message` จาก Dynamic Content (จาก Manual Trigger)

> 💡 **คำอธิบาย**: Action นี้จะส่งข้อความไป Teams เมื่อ `Delivery` ไม่เท่ากับ `Email`
> - ใช้ `Message` จาก Manual Trigger เป็นเนื้อหาของ Teams Message

**ตัวอย่าง Teams Message ที่ส่งออกไป**:
- **Recipient**: `your-email@example.com`
- **Message**: `สวัสดีครับ นี่คือข้อความทดสอบ`

### ขั้นตอนที่ 6: ทดสอบ Flow

#### ทดสอบกรณีที่ 1: Delivery = "Email" (True Branch)

1. คลิก **Save**
2. คลิก **Test** → **Manually** → **Run flow**
3. ในหน้า **Run flow**:
   - **Delivery**: พิมพ์ `Email`
   - **Message**: พิมพ์ `นี่คือข้อความทดสอบสำหรับ Email`
   - คลิก **Run flow**
4. รอให้ Flow รันเสร็จ
5. ตรวจสอบผลลัพธ์:
   - ควรเห็น Email ในกล่องจดหมาย (ไม่เห็น Teams Message)
   - Email มี Subject: `แจ้งเตือน: ข้อความจาก Flow`
   - Email มี Body: `นี่คือข้อความทดสอบสำหรับ Email`

#### ทดสอบกรณีที่ 2: Delivery ≠ "Email" (False Branch)

1. คลิก **Test** → **Manually** → **Run flow** อีกครั้ง
2. ในหน้า **Run flow**:
   - **Delivery**: พิมพ์ `Teams` (หรือ `SMS`, `Other` - อะไรก็ได้ที่ไม่ใช่ "Email")
   - **Message**: พิมพ์ `นี่คือข้อความทดสอบสำหรับ Teams`
   - คลิก **Run flow**
3. รอให้ Flow รันเสร็จ
4. ตรวจสอบผลลัพธ์:
   - ควรเห็น Teams Message ใน Teams Chat (ไม่เห็น Email)
   - Teams Message มีเนื้อหา: `นี่คือข้อความทดสอบสำหรับ Teams`

---

## 📊 ผลลัพธ์ที่คาดหวัง

### กรณีที่ 1: Delivery = "Email"

คุณควรจะเห็น:
- ✅ **Email** ในกล่องจดหมาย
- ✅ Subject: `แจ้งเตือน: ข้อความจาก Flow`
- ✅ Body: ข้อความที่คุณป้อนใน `Message`
- ❌ **ไม่เห็น** Teams Message

### กรณีที่ 2: Delivery ≠ "Email"

คุณควรจะเห็น:
- ✅ **Teams Message** ใน Teams Chat
- ✅ ข้อความ: ข้อความที่คุณป้อนใน `Message`
- ❌ **ไม่เห็น** Email

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **Manual Trigger with Parameters**: สร้าง Input Parameters เพื่อรับข้อมูลจากผู้ใช้  
✅ **Condition (If-Else)**: ใช้ Condition เพื่อตรวจสอบเงื่อนไขและแยกการทำงาน  
✅ **True/False Branches**: เข้าใจการทำงานของ True Branch และ False Branch  
✅ **Dynamic Content**: ใช้ Dynamic Content จาก Manual Trigger ใน Actions  
✅ **Email Integration**: ส่ง Email ผ่าน Office 365 Outlook  
✅ **Teams Integration**: ส่งข้อความไปยัง Teams Chat/Channel  

---

## 💡 Tips & Tricks

1. **Case Sensitivity (ความแตกต่างของตัวพิมพ์เล็ก-ใหญ่)**:
   - Condition `Delivery is equal to "Email"` จะตรวจสอบแบบ **Case-Sensitive**
   - `Email` ≠ `email` ≠ `EMAIL`
   - ถ้าต้องการให้ไม่สนใจ Case: ใช้ Expression `toLower(Delivery) is equal to "email"`

2. **การตรวจสอบหลายเงื่อนไข**:
   - ใช้ **And** (ทั้งสองเงื่อนไขต้องเป็นจริง):
     ```
     Delivery is equal to "Email" AND Message is not empty
     ```
   - ใช้ **Or** (มีอันใดอันหนึ่งเป็นจริง):
     ```
     Delivery is equal to "Email" OR Delivery is equal to "Teams"
     ```

3. **การเพิ่มเงื่อนไขหลายแถว**:
   - คลิก **+ Add row** เพื่อเพิ่มเงื่อนไขเพิ่มเติม
   - เลือก Logical Operator: `And` หรือ `Or`

4. **การใช้งาน Expression ใน Condition**:
   - คลิก **Edit in advanced mode** เพื่อใช้ Expression
   - ตัวอย่าง: `equals(toLower(Delivery), 'email')`

5. **การตรวจสอบว่า Text ไม่ว่าง**:
   - ใช้ Condition: `Message is not equal to ""` (ข้อความว่าง)
   - หรือใช้ Expression: `length(Message) > 0`

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **เพิ่มเงื่อนไขหลายข้อ**: 
   - ตรวจสอบทั้ง `Delivery` และ `Message` (ทั้งสองต้องไม่ว่าง)
   - ใช้ Logical Operator: `And`

2. **เพิ่ม Switch Condition**: 
   - แทน Condition เดียว, ใช้ Switch สำหรับหลาย Case:
     - Case "Email" → Send Email
     - Case "Teams" → Send Teams Message
     - Case "SMS" → Send SMS (ถ้ามี Connector)
     - Default → Send Notification

3. **เพิ่มเงื่อนไขซ้อนกัน (Nested Conditions)**:
   - ตรวจสอบ `Delivery` ก่อน
   - ถ้า `Delivery = "Email"`, ตรวจสอบ `Message` อีกที
   - ถ้า `Message` ยาวเกิน 100 ตัวอักษร → ส่ง Email แบบ Long Message
   - ถ้า `Message` สั้น → ส่ง Email แบบ Short Message

4. **เพิ่ม Formatting ในข้อความ**:
   - ใช้ HTML Formatting ใน Email Body:
     ```html
     <h2>แจ้งเตือน</h2>
     <p>ข้อความ: {Message}</p>
     <p><strong>ส่งเมื่อ:</strong> {utcNow()}</p>
     ```

5. **เพิ่ม Attachments**:
   - ส่งไฟล์แนบไปกับ Email (ถ้ามี)
   - ใช้ Dynamic Content: `Attachments` หรือ `Attachments Content`

6. **เพิ่ม Validation**:
   - ตรวจสอบว่า `Delivery` เป็นค่า Valid (Email, Teams, SMS เท่านั้น)
   - ถ้าไม่ Valid → ส่ง Error Message

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: ทำไมต้องใช้ Condition?**  
A: เพื่อให้ Flow ตัดสินใจและทำงานต่างกันตามเงื่อนไขที่กำหนด (เช่น ถ้า Delivery = Email → ส่ง Email, ถ้าไม่ใช่ → ส่ง Teams)

**Q: True Branch และ False Branch คืออะไร?**  
A: True Branch คือ Actions ที่จะทำงานเมื่อเงื่อนไขเป็นจริง, False Branch คือ Actions ที่จะทำงานเมื่อเงื่อนไขเป็นเท็จ

**Q: Condition ตรวจสอบ Case-Sensitive หรือไม่?**  
A: ใช่, Condition ตรวจสอบแบบ Case-Sensitive โดยค่าเริ่มต้น (`Email` ≠ `email`)

**Q: วิธีทำให้ Condition ไม่สนใจ Case?**  
A: ใช้ Expression: `toLower(Delivery) is equal to "email"` หรือ `equals(toLower(Delivery), 'email')`

**Q: สามารถเพิ่มเงื่อนไขหลายข้อได้ไหม?**  
A: ได้, คลิก **+ Add row** เพื่อเพิ่มเงื่อนไขเพิ่มเติม และเลือก Logical Operator (`And` หรือ `Or`)

**Q: วิธีใช้ Expression ใน Condition?**  
A: คลิก **Edit in advanced mode** เพื่อพิมพ์ Expression โดยตรง (เช่น `equals(Delivery, 'Email')`)

**Q: ถ้าไม่ต้องการ False Branch ต้องทำอย่างไร?**  
A: ปล่อย False Branch ว่างไว้ได้ หรือเพิ่ม Action ที่ไม่ทำอะไร (เช่น Compose หรือ Terminate)

**Q: Condition กับ Switch ต่างกันอย่างไร?**  
A: Condition ใช้สำหรับเงื่อนไข 2 ทางเลือก (True/False), Switch ใช้สำหรับหลาย Case (Case 1, Case 2, Case 3, Default)

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การสร้าง Manual Trigger พร้อม Input Parameters
- **การใช้ Condition (If-Else) เพื่อตรวจสอบเงื่อนไข**
- **การแยกการทำงานตาม True/False Branches**
- การส่ง Email เมื่อเงื่อนไขเป็นจริง
- การส่งข้อความไป Teams เมื่อเงื่อนไขเป็นเท็จ

**Flow Structure**:
```
Manual Trigger (Delivery, Message)
  ↓
Condition: Delivery is equal to "Email"
  ├─ True Branch: Send Email (V2)
  └─ False Branch: Post Message (Teams)
```

**Condition Logic**:
```
ถ้า Delivery = "Email"
  → ส่ง Email
ถ้าไม่ใช่
  → ส่งข้อความไป Teams
```

**ข้อดีของการใช้ Condition**:
- ✅ ทำให้ Flow ตัดสินใจได้ (Decision Making)
- ✅ แยกการทำงานตามเงื่อนไข
- ✅ ทำให้ Flow ยืดหยุ่นมากขึ้น
- ✅ ลดการสร้าง Flow หลายตัว

พร้อมสำหรับบทต่อไปแล้ว! 🎉

