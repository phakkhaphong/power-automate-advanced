# Exercise 3e: Forms → Excel Table → Do Until Loop → Teams

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่ใช้ **Microsoft Forms Trigger** ("When a new response is submitted")
- ใช้ **Get response details** เพื่อดึงข้อมูลจากฟอร์ม
- ใช้ **Add a row into a table** เพื่อบันทึกข้อมูลลง Excel Table
- ใช้ **Do until** loop เพื่อรอสถานะการติดต่อจนกว่าจะสำเร็จ
- ส่งข้อความแจ้งเตือนไป Teams ใน Loop จนกว่าจะติดต่อสำเร็จ

> 💡 **หมายเหตุ**: Exercise นี้แสดงการใช้งาน **Do until** loop กับ Excel Table:
> - บันทึกข้อมูลจากฟอร์มลง Excel Table
> - ตั้งค่า ContactStatus = "To be contacted" (เริ่มต้น)
> - วนลูปจนกว่า ContactStatus = "Contact Successful"
> - ส่งข้อความแจ้งเตือนใน Loop จนกว่าจะติดต่อสำเร็จ

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- Microsoft Forms Account (สำหรับสร้างฟอร์ม "Charity Form")
  - 💡 **หมายเหตุ**: ใช้ฟอร์ม "Charity Form" เดียวกับ Exercise 3b
- Microsoft Excel Online หรือ OneDrive for Business
- ความรู้พื้นฐานเรื่อง Flow Creation และการใช้งาน Excel Table
- Teams Account (สำหรับส่งข้อความ)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: ตรวจสอบ Charity Form

ก่อนเริ่ม Flow ตรวจสอบว่าคุณมีฟอร์ม "Charity Form" แล้วหรือไม่:

1. เข้าสู่ **Microsoft Forms** (https://forms.office.com)
2. ตรวจสอบว่ามีฟอร์มชื่อ `Charity Form` ที่มีคำถาม:
   - **What's your name?** (Text)
   - **What's your E-Mail address?** (Text)
   - **What amount do you plan on donating?** (Number)
   - **What type of donor describes you the best?** (Choice)

> 💡 **หมายเหตุ**: ถ้ายังไม่มีฟอร์ม ให้สร้างตาม Exercise 3b หรือสร้างใหม่ตามขั้นตอนที่ 1 ของ Exercise 3b

### ขั้นตอนที่ 2: สร้าง Excel Table

ก่อนเริ่ม Flow ต้องสร้าง Excel Table ก่อน:

1. เข้าสู่ **OneDrive for Business** หรือ **SharePoint**
2. สร้างไฟล์ Excel ใหม่ชื่อ `CharityData.xlsx`
3. สร้าง Table ชื่อ `Charity` ด้วย columns ต่อไปนี้:
   - **Name** (Text)
   - **Email** (Text)
   - **Amount** (Number)
   - **Type** (Text)
   - **ContactStatus** (Text)

4. ตั้งค่า Table:
   - เลือกข้อมูลใน Excel → คลิก **Insert** → **Table**
   - ตั้งชื่อ Table: `Charity`
   - ตรวจสอบว่า "My table has headers" ถูกเลือก

> 💡 **หมายเหตุ**: เก็บ Path ของไฟล์ไว้ (เช่น `/CharityData.xlsx`) เพื่อใช้ใน Flow

**ตัวอย่าง Excel Table Structure**:
```
| Name | Email | Amount | Type | ContactStatus |
|------|-------|--------|------|---------------|
|      |       |        |      |               |
```

### ขั้นตอนที่ 3: สร้าง Flow ใหม่พร้อม Forms Trigger

1. เข้าสู่ **Power Automate** (https://make.powerautomate.com/)
2. คลิก **Create** → เลือก **Automated cloud flow**
3. ตั้งชื่อ Flow: `Exercise 3e - Forms Excel Do Until`
4. ค้นหาและเลือก Trigger: **When a new response is submitted**
5. คลิก **Create**

### ขั้นตอนที่ 4: ตั้งค่า Forms Trigger

1. ใน **When a new response is submitted**:
   - **Form Id**: เลือก `Charity Form` (หรือชื่อฟอร์มที่คุณสร้าง)
   - **Connection**: ตรวจสอบว่าเชื่อมต่อกับ Microsoft Forms Account ของคุณแล้ว

> 💡 **หมายเหตุ**: Trigger นี้จะทำงานอัตโนมัติเมื่อมีคนส่งฟอร์ม "Charity Form"

### ขั้นตอนที่ 5: เพิ่ม Get response details

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Get response details**
3. ตั้งค่า:
   - **Form Id**: เลือก `Charity Form` (เหมือน Trigger)
   - **Response Id**: เลือก `Response Id` จาก Dynamic Content (จาก Trigger)

> 💡 **คำอธิบาย**: Get response details จะดึงข้อมูลรายละเอียดทั้งหมดจากฟอร์มที่ส่งมา
> - ข้อมูลที่ได้: ชื่อ, Email, จำนวนเงินบริจาค, ประเภทผู้บริจาค

**ข้อมูลที่สามารถเข้าถึงได้**:
- `What's your name?` → ชื่อผู้บริจาค
- `What's your E-Mail address?` → Email ผู้บริจาค
- `What amount do you plan on donating?` → จำนวนเงินบริจาค (Number)
- `What type of donor describes you the best?` → ประเภทผู้บริจาค

### ขั้นตอนที่ 6: เพิ่ม Add a row into a table (Excel)

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Add a row into a table** (หรือ **Excel Online (Business)** → **Add a row into a table**)
3. ตั้งค่า:
   - **Location**: เลือก `OneDrive for Business` (หรือ `SharePoint` ถ้าใช้ SharePoint)
   - **Document Library**: เลือก `OneDrive` (หรือ Document Library ที่ใช้)
   - **File**: พิมพ์ `/CharityData.xlsx` (หรือคลิก Browse เพื่อเลือกไฟล์)
   - **Table**: เลือก `Charity` (ชื่อ Table ที่สร้างไว้)

4. ตั้งค่า Columns (Advanced parameters):
   - **Name**: เลือก `What's your name?` จาก Dynamic Content (จาก Get response details)
   - **Email**: เลือก `What's your E-Mail address?` จาก Dynamic Content
   - **Amount**: เลือก `What amount do you plan on donating?` จาก Dynamic Content
   - **Type**: เลือก `What type of donor describes you the best?` จาก Dynamic Content
   - **ContactStatus**: พิมพ์ `To be contacted` (ค่าเริ่มต้น)

> 💡 **คำอธิบาย**: Action นี้จะบันทึกข้อมูลจากฟอร์มลง Excel Table
> - ข้อมูลทั้งหมดจะถูกบันทึกใน Row ใหม่
> - ContactStatus ถูกตั้งเป็น "To be contacted" (ยังไม่ได้ติดต่อ)

**Flow Structure**:
```
When a new response is submitted (Charity Form)
  ↓
Get response details
  ↓
Add a row into a table (Excel)
  - Name: {What's your name?}
  - Email: {What's your E-Mail address?}
  - Amount: {What amount do you plan on donating?}
  - Type: {What type of donor describes you the best?}
  - ContactStatus: "To be contacted"
```

### ขั้นตอนที่ 7: เพิ่ม Do until Loop

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Do until** (หรือ **Control** → **Do until**)
3. ตั้งค่า **Loop until** (เงื่อนไขออกจาก Loop):
   - **Field 1**: เลือก `ContactStatus` จาก Dynamic Content (จาก Add a row into a table)
   - **Operator**: เลือก `is equal to`
   - **Field 2**: พิมพ์ `Contact Successful`

> 💡 **คำอธิบาย**: Do until จะวนลูปจนกว่า ContactStatus จะเท่ากับ "Contact Successful"
> - Loop จะทำงานซ้ำๆ จนกว่าจะติดต่อสำเร็จ
> - ในแต่ละรอบจะส่งข้อความแจ้งเตือนไป Teams

**Do until Structure**:
```
Do until: ContactStatus = "Contact Successful"
  ├─ Post message (Teams) - แจ้งเตือนให้ติดต่อ
  └─ (รอสถานะอัพเดท)
```

### ขั้นตอนที่ 8: ตั้งค่า Advanced Parameters ของ Do until

1. ใน **Do until** คลิก **Show advanced options** หรือ **Advanced parameters**
2. ตั้งค่า:
   - **Count**: พิมพ์ `60` (จำนวนครั้งสูงสุดที่ Loop จะทำงาน)
   - **Timeout**: พิมพ์ `PT1H` (Timeout 1 ชั่วโมง)

> 💡 **คำอธิบาย**: 
> - **Count**: จำกัดจำนวนครั้งที่ Loop จะทำงาน (60 ครั้ง) เพื่อป้องกัน Infinite Loop
> - **Timeout**: จำกัดเวลาที่ Loop จะทำงาน (PT1H = 1 Hour) เพื่อป้องกัน Loop ไม่รู้จบ
> - **PT1H**: ISO 8601 Duration Format (PT = Period of Time, 1H = 1 Hour)

**Advanced Parameters**:
- **Count**: `60` (Maximum iterations)
- **Timeout**: `PT1H` (1 hour timeout)

### ขั้นตอนที่ 9: เพิ่ม Action ใน Do until - Post message to Teams

1. **ภายใน Do until** คลิก **Add an action**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Message**: ใช้ Dynamic Content เพื่อสร้างข้อความ:
     ```
     There has been a donor who has registered on the form but who has not yet been contacted. Please contact them. Their details are below.
     
     Name : {What's your name?}
     EMail : {What's your E-Mail address?}
     ```

> 💡 **คำอธิบาย**: Action นี้จะส่งข้อความแจ้งเตือนไป Teams ในแต่ละรอบของ Loop
> - ข้อความจะแจ้งเตือนว่ามี donor ที่ยังไม่ได้ติดต่อ
> - แสดงข้อมูลชื่อและ Email

**ตัวอย่าง Teams Message**:
```
There has been a donor who has registered on the form but who has not yet been contacted. Please contact them. Their details are below.

Name : สมชาย ใจดี
EMail : somchai@example.com
```

### ขั้นตอนที่ 10: ทดสอบ Flow

#### ทดสอบกรณีที่ 1: ส่งฟอร์มและรอสถานะอัพเดท

1. คลิก **Save**
2. เปิดฟอร์ม "Charity Form" ใน Browser
3. กรอกข้อมูล:
   - **What's your name?**: `ทดสอบ 1`
   - **What's your E-Mail address?**: `test1@example.com`
   - **What amount do you plan on donating?**: `5000`
   - **What type of donor describes you the best?**: `First time donor`
4. ส่งฟอร์ม
5. รอให้ Flow รันอัตโนมัติ
6. ตรวจสอบผลลัพธ์:
   - ควรเห็นข้อมูลใน Excel Table (Row ใหม่)
   - ContactStatus = "To be contacted"
   - ควรเห็น Teams Messages แจ้งเตือน (ส่งซ้ำๆ ใน Loop)
   - Loop จะทำงานจนกว่า ContactStatus จะเปลี่ยนเป็น "Contact Successful"

#### ทดสอบกรณีที่ 2: อัพเดท ContactStatus ใน Excel

1. เปิดไฟล์ Excel `CharityData.xlsx`
2. หา Row ที่เพิ่งสร้าง (ข้อมูลจากฟอร์ม)
3. เปลี่ยนค่า **ContactStatus** จาก `To be contacted` เป็น `Contact Successful`
4. ตรวจสอบ Flow:
   - Flow ควรหยุด Loop
   - ไม่ส่ง Teams Message อีกต่อไป

---

## 📊 ผลลัพธ์ที่คาดหวัง

### กรณีที่ 1: ส่งฟอร์มใหม่

คุณควรจะเห็น:
- ✅ **Excel Table**: ข้อมูลถูกบันทึกใน Row ใหม่
- ✅ **ContactStatus**: ตั้งเป็น "To be contacted"
- ✅ **Teams Messages**: ข้อความแจ้งเตือนส่งซ้ำๆ ใน Loop
- ✅ **Loop ทำงาน**: จนกว่า ContactStatus จะเปลี่ยนเป็น "Contact Successful"

### กรณีที่ 2: อัพเดท ContactStatus เป็น "Contact Successful"

คุณควรจะเห็น:
- ✅ **ContactStatus**: เปลี่ยนเป็น "Contact Successful"
- ✅ **Loop หยุด**: Flow หยุด Loop และไม่ส่ง Teams Message อีกต่อไป
- ✅ **Flow รันเสร็จ**: Flow ทำงานเสร็จสมบูรณ์

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **Microsoft Forms Trigger**: ใช้ "When a new response is submitted" เพื่อ Trigger Flow อัตโนมัติ  
✅ **Get response details**: ดึงข้อมูลรายละเอียดทั้งหมดจากฟอร์มที่ส่งมา  
✅ **Add a row into a table**: บันทึกข้อมูลลง Excel Table  
✅ **Do until Loop**: วนลูปจนกว่าเงื่อนไขจะเป็นจริง (ContactStatus = "Contact Successful")  
✅ **Advanced Parameters**: ตั้งค่า Count และ Timeout เพื่อป้องกัน Infinite Loop  
✅ **Dynamic Content**: ใช้ Dynamic Content จาก Forms และ Excel ใน Actions  
✅ **Teams Integration**: ส่งข้อความไปยัง Teams Chat/Channel  

---

## 💡 Tips & Tricks

1. **การสร้าง Excel Table**:
   - ใช้ Table ใน Excel (ไม่ใช่แค่ข้อมูลในเซลล์)
   - ตั้งชื่อ Table ให้ชัดเจน (เช่น `Charity`)
   - ตรวจสอบว่า Column names ตรงกับที่ใช้ใน Flow

2. **Path ของไฟล์ Excel**:
   - Path ต้องเริ่มต้นด้วย `/` (เช่น `/CharityData.xlsx`)
   - ถ้าไฟล์อยู่ในโฟลเดอร์: `/Folder/CharityData.xlsx`
   - ตรวจสอบ Path ให้ถูกต้อง

3. **ContactStatus Management**:
   - ตั้งค่าเริ่มต้น: "To be contacted"
   - เปลี่ยนเป็น "Contact Successful" เมื่อติดต่อสำเร็จ
   - สามารถเพิ่มสถานะอื่นๆ ได้ (เช่น "Contacted", "Failed")

4. **Do until Loop Best Practices**:
   - ตั้งค่า Count และ Timeout เสมอ
   - ตรวจสอบว่าเงื่อนไขออกจาก Loop จะเป็นจริงเสมอ
   - ใช้ Delay ถ้าจำเป็น (เพื่อลดการส่งข้อความ)

5. **การ Debug Loop**:
   - ใช้ Post message เพื่อดูค่า ContactStatus ในแต่ละรอบ
   - ตรวจสอบ Excel Table ว่าสถานะถูกอัพเดทหรือไม่
   - ตรวจสอบว่า Loop หยุดเมื่อ ContactStatus เปลี่ยน

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **เพิ่ม Delay ใน Loop**:
   - เพิ่ม Delay 5 minutes ในแต่ละรอบ
   - ลดการส่งข้อความแจ้งเตือน (ส่งทุก 5 นาที แทนทุกวินาที)

2. **เพิ่ม Condition ใน Loop**:
   - เพิ่ม Condition เพื่อตรวจสอบว่า ContactStatus = "Failed"
   - ถ้า Failed → ส่ง Email แจ้งเตือนไปยัง Admin

3. **เพิ่มการอัพเดท Excel ใน Loop**:
   - เพิ่ม Action "Update a row" เพื่ออัพเดท ContactStatus ใน Excel
   - ตั้งค่า ContactStatus = "In Progress" เมื่อเริ่ม Loop

4. **เพิ่มการตรวจสอบจำนวนครั้ง**:
   - ใช้ Initialize variable เพื่อนับจำนวนครั้งที่ Loop ทำงาน
   - ถ้าจำนวนครั้ง > 10 → ส่ง Email แจ้งเตือนไปยัง Admin

5. **เพิ่มการส่ง Email แทน Teams**:
   - เปลี่ยนจาก Post message เป็น Send email
   - ส่ง Email แจ้งเตือนแทน Teams Message

6. **ใช้ Switch สำหรับ ContactStatus**:
   - ใช้ Switch เพื่อจัดการสถานะต่างๆ
   - Case 1: "To be contacted" → ส่ง Teams Message
   - Case 2: "Contact Successful" → ส่ง Email ขอบคุณ
   - Default: "Failed" → ส่ง Email แจ้งเตือน

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: ทำไมต้องใช้ Excel Table แทน SharePoint List?**  
A: Excel Table เหมาะสำหรับกรณีที่ต้องการจัดการข้อมูลแบบ Spreadsheet หรือต้องการใช้ Excel Features, SharePoint List เหมาะสำหรับกรณีที่ต้องการ Workflow Management

**Q: วิธีอัพเดท ContactStatus ใน Excel?**  
A: เปิดไฟล์ Excel แล้วแก้ไขค่า ContactStatus ใน Row ที่ต้องการ, หรือใช้ Action "Update a row" ใน Flow

**Q: Loop จะหยุดเมื่อไหร่?**  
A: Loop จะหยุดเมื่อ ContactStatus = "Contact Successful" หรือเมื่อถึง Count หรือ Timeout ที่กำหนด

**Q: ถ้าไม่อัพเดท ContactStatus จะเกิดอะไรขึ้น?**  
A: Loop จะทำงานจนถึง Count (60 ครั้ง) หรือ Timeout (1 ชั่วโมง) แล้วหยุด

**Q: วิธีลดการส่งข้อความแจ้งเตือน?**  
A: เพิ่ม Delay ใน Loop (เช่น 5 minutes) เพื่อลดความถี่ในการส่งข้อความ

**Q: สามารถใช้ Condition แทน Do until ได้ไหม?**  
A: ไม่ได้, Do until ใช้สำหรับวนลูปจนกว่าเงื่อนไขจะเป็นจริง, Condition ใช้สำหรับตรวจสอบเงื่อนไขครั้งเดียว

**Q: Path ของไฟล์ Excel ต้องเขียนอย่างไร?**  
A: Path ต้องเริ่มต้นด้วย `/` (เช่น `/CharityData.xlsx`), ถ้าไฟล์อยู่ในโฟลเดอร์: `/Folder/CharityData.xlsx`

**Q: วิธีทดสอบ Flow นี้?**  
A: ส่งฟอร์มเพื่อ Trigger Flow, ตรวจสอบ Excel Table ว่าข้อมูลถูกบันทึกหรือไม่, ตรวจสอบ Teams Messages, แล้วอัพเดท ContactStatus ใน Excel เพื่อหยุด Loop

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การใช้ Microsoft Forms Trigger และ Get response details
- **การบันทึกข้อมูลลง Excel Table ด้วย Add a row into a table**
- **การใช้ Do until Loop เพื่อรอสถานะการติดต่อ**
- การส่งข้อความแจ้งเตือนไป Teams ใน Loop
- การตั้งค่า Count และ Timeout เพื่อป้องกัน Infinite Loop

**Flow Structure**:
```
When a new response is submitted (Charity Form)
  ↓
Get response details
  ↓
Add a row into a table (Excel)
  - Name, Email, Amount, Type
  - ContactStatus: "To be contacted"
  ↓
Do until (ContactStatus = "Contact Successful")
  ├─ Post message (Teams) - แจ้งเตือนให้ติดต่อ
  └─ (รอสถานะอัพเดท)
  Count: 60, Timeout: PT1H
```

**Do until Logic**:
```
1. บันทึกข้อมูลลง Excel Table
2. ตั้งค่า ContactStatus = "To be contacted"
3. Loop:
   - ส่ง Teams Message แจ้งเตือน
   - ตรวจสอบ: ContactStatus = "Contact Successful"?
     - ถ้าใช่ → หยุด Loop
     - ถ้าไม่ใช่ → วนลูปต่อไป
4. Loop หยุดเมื่อ ContactStatus = "Contact Successful"
```

**ข้อดีของการใช้ Do until กับ Excel**:
- ✅ รอสถานะการทำงานจนกว่าจะสำเร็จ
- ✅ ส่งข้อความแจ้งเตือนซ้ำๆ จนกว่าจะติดต่อ
- ✅ สามารถตั้งค่า Count และ Timeout เพื่อป้องกัน Infinite Loop

**เมื่อไหร่ควรใช้ Do until กับ Excel**:
- ✅ เมื่อต้องรอสถานะการทำงานจนกว่าจะสำเร็จ
- ✅ เมื่อต้องส่งข้อความแจ้งเตือนซ้ำๆ จนกว่าจะติดต่อ
- ✅ เมื่อต้องตรวจสอบสถานะใน Excel Table

**คำแนะนำ**:
- ⚠️ ตั้งค่า Count และ Timeout เสมอเพื่อป้องกัน Infinite Loop
- ⚠️ ตรวจสอบว่าเงื่อนไขออกจาก Loop จะเป็นจริงเสมอ
- ⚠️ ใช้ Delay ใน Loop เพื่อลดการส่งข้อความแจ้งเตือน
- ⚠️ อัพเดท ContactStatus ใน Excel เมื่อติดต่อสำเร็จ

พร้อมสำหรับบทต่อไปแล้ว! 🎉

