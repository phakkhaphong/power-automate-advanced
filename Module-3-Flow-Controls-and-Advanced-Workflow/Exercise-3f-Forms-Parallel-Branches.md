# Exercise 3f: Forms → Parallel Branches → Switch Condition

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่ใช้ **Microsoft Forms Trigger** ("When a new response is submitted")
- ใช้ **Parallel Branches** เพื่อให้ Actions หลายตัวทำงานพร้อมกัน
- ใช้ **Get response details** และ **Post message** ทำงานแบบขนาน
- ใช้ **Switch Condition** เพื่อจัดการหลาย Case ตามประเภทผู้บริจาค
- เข้าใจประโยชน์ของ Parallel Branches ในการลดเวลาในการรัน Flow

> 💡 **หมายเหตุ**: Exercise นี้แสดงการใช้งาน **Parallel Branches** (การทำงานแบบขนาน):
> - Branch 1: Get response details (ดึงข้อมูลจากฟอร์ม)
> - Branch 2: Post message to Teams (แจ้งเตือนการส่งฟอร์ม)
> - ทั้งสอง Branch ทำงานพร้อมกัน (ไม่ต้องรอกัน)
> - หลังจาก Branch 1 เสร็จ → Switch Condition เพื่อจัดการตามประเภทผู้บริจาค

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- Microsoft Forms Account (สำหรับสร้างฟอร์ม "Charity Form")
  - 💡 **หมายเหตุ**: ใช้ฟอร์ม "Charity Form" เดียวกับ Exercise 3b
- ความรู้พื้นฐานเรื่อง Flow Creation และการใช้งาน Condition
- Email Account ที่เชื่อมต่อกับ Power Automate
- Teams Account (สำหรับส่งข้อความ)
- Calendar Account (สำหรับสร้าง Event)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: ตรวจสอบ Charity Form

ก่อนเริ่ม Flow ตรวจสอบว่าคุณมีฟอร์ม "Charity Form" แล้วหรือไม่:

1. เข้าสู่ **Microsoft Forms** (https://forms.office.com)
2. ตรวจสอบว่ามีฟอร์มชื่อ `Charity Form` ที่มีคำถาม:
   - **What's your name?** (Text)
   - **What's your E-Mail address?** (Text)
   - **What amount do you plan on donating?** (Number)
   - **What type of donor describes you the best?** (Choice)
     - Options: `First time donor`, `Occasional donor`, `All-the-time donor`

> 💡 **หมายเหตุ**: ถ้ายังไม่มีฟอร์ม ให้สร้างตาม Exercise 3b หรือสร้างใหม่ตามขั้นตอนที่ 1 ของ Exercise 3b

### ขั้นตอนที่ 2: สร้าง Flow ใหม่พร้อม Forms Trigger

1. เข้าสู่ **Power Automate** (https://make.powerautomate.com/)
2. คลิก **Create** → เลือก **Automated cloud flow**
3. ตั้งชื่อ Flow: `Exercise 3f - Forms Parallel Branches`
4. ค้นหาและเลือก Trigger: **When a new response is submitted**
5. คลิก **Create**

### ขั้นตอนที่ 3: ตั้งค่า Forms Trigger

1. ใน **When a new response is submitted**:
   - **Form Id**: เลือก `Charity Form` (หรือชื่อฟอร์มที่คุณสร้าง)
   - **Connection**: ตรวจสอบว่าเชื่อมต่อกับ Microsoft Forms Account ของคุณแล้ว

> 💡 **หมายเหตุ**: Trigger นี้จะทำงานอัตโนมัติเมื่อมีคนส่งฟอร์ม "Charity Form"

### ขั้นตอนที่ 4: สร้าง Parallel Branches

ตอนนี้เราจะสร้าง Parallel Branches เพื่อให้ Actions ทำงานพร้อมกัน:

#### Branch 1: Get response details

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Get response details**
3. ตั้งค่า:
   - **Form Id**: เลือก `Charity Form` (เหมือน Trigger)
   - **Response Id**: เลือก `Response Id` จาก Dynamic Content (จาก Trigger)

> 💡 **คำอธิบาย**: Branch 1 จะดึงข้อมูลรายละเอียดทั้งหมดจากฟอร์มที่ส่งมา

#### Branch 2: Post message to Teams (Parallel)

1. **คลิกที่ Trigger** (When a new response is submitted)
2. **คลิกที่ + ที่อยู่ด้านล่าง Trigger** (ไม่ใช่ New step)
3. ค้นหาและเลือก **Post message in a chat or channel**
4. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Message**: พิมพ์ `Someone has submitted a Charity form.`

> 💡 **คำอธิบาย**: Branch 2 จะส่งข้อความแจ้งเตือนไปยัง Teams ทันที โดยไม่ต้องรอ Branch 1 เสร็จ
> - **Parallel Branch**: Branch 1 และ Branch 2 ทำงานพร้อมกัน
> - **ลดเวลา**: ไม่ต้องรอ Get response details เสร็จก่อนส่ง Teams Message

**Parallel Branches Structure**:
```
When a new response is submitted (Charity Form)
  ├─ Branch 1: Get response details (Parallel)
  └─ Branch 2: Post message (Teams) - "Someone has submitted a Charity form." (Parallel)
```

**ข้อดีของ Parallel Branches**:
- ✅ **ลดเวลา**: Actions ทำงานพร้อมกัน ไม่ต้องรอกัน
- ✅ **เพิ่มประสิทธิภาพ**: Flow รันเร็วขึ้น
- ✅ **ยืดหยุ่น**: สามารถเพิ่ม Branches ได้ตามต้องการ

### ขั้นตอนที่ 5: เพิ่ม Switch Condition (หลังจาก Branch 1)

หลังจาก Branch 1 (Get response details) เสร็จแล้ว เราจะเพิ่ม Switch Condition:

1. **คลิกที่ + ที่อยู่ด้านล่าง Get response details** (Branch 1)
2. ค้นหาและเลือก **Switch** (หรือ **Control** → **Switch**)
3. ตั้งค่า:
   - **On**: เลือก `What type of donor describes you the best?` จาก Dynamic Content (จาก Get response details)

> 💡 **คำอธิบาย**: Switch จะตรวจสอบค่าของ "What type of donor describes you the best?" และทำงานตาม Case ที่ตรงกัน
> - **Case 1**: "First time donor" → ส่ง Email แจ้งเตือน
> - **Case 2**: "Occasional donor" → ส่ง Email ขอบคุณ
> - **Default**: "All-the-time donor" → สร้าง Calendar Event

**Switch Structure**:
```
Switch: What type of donor describes you the best?
  ├─ Case 1: "First time donor" → Send Email (V2)
  ├─ Case 2: "Occasional donor" → Send Email (V2) 1
  └─ Default: "All-the-time donor" → Create Event (V4)
```

### ขั้นตอนที่ 6: เพิ่ม Case 1 - "First time donor"

1. **ภายใน Case 1** (คลิกที่ Case 1 หรือ **Add case** ถ้ายังไม่มี)
2. ตั้งค่า Case:
   - **Equals**: พิมพ์ `First time donor` (ตรงกับค่าจากฟอร์ม)

3. **ภายใน Case 1** คลิก **Add an action**
4. ค้นหาและเลือก **Send an email (V2)**
5. ตั้งค่า:
   - **To**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Subject**: พิมพ์ `We have a first time donor`
   - **Body**: ใช้ Dynamic Content เพื่อสร้างข้อความ:
     ```
     Let's make sure to reach out of them.
     
     Name: {What's your name?}
     Email: {What's your E-Mail address?}
     Amount: {What amount do you plan on donating?}
     ```

> 💡 **คำอธิบาย**: Action นี้จะส่ง Email เมื่อผู้บริจาคเป็น "First time donor"
> - Subject: `We have a first time donor`
> - Body: รวมข้อมูลชื่อ, Email, และจำนวนเงินบริจาค

**ตัวอย่าง Email ที่ส่งออกไป**:
- **To**: `your-email@example.com`
- **Subject**: `We have a first time donor`
- **Body**: 
  ```
  Let's make sure to reach out of them.
  
  Name: สมชาย ใจดี
  Email: somchai@example.com
  Amount: 5000
  ```

### ขั้นตอนที่ 7: เพิ่ม Case 2 - "Occasional donor"

1. **ภายใน Switch** คลิก **Add case** เพื่อเพิ่ม Case 2
2. ตั้งค่า Case:
   - **Equals**: พิมพ์ `Occasional donor` (ตรงกับค่าจากฟอร์ม)

3. **ภายใน Case 2** คลิก **Add an action**
4. ค้นหาและเลือก **Send an email (V2)**
5. ตั้งค่า:
   - **To**: เลือก `What's your E-Mail address?` จาก Dynamic Content (จาก Get response details)
   - **Subject**: พิมพ์ `Thank you for donating`
   - **Body**: พิมพ์ `Thank you so much more donating - you are clearly an occasional donor`

> 💡 **คำอธิบาย**: Action นี้จะส่ง Email ขอบคุณไปยังผู้บริจาคเมื่อเป็น "Occasional donor"
> - To: Email จากฟอร์ม (ส่งไปยังผู้บริจาคโดยตรง)
> - Subject: `Thank you for donating`
> - Body: ข้อความขอบคุณ

**ตัวอย่าง Email ที่ส่งออกไป**:
- **To**: `donor@example.com` (Email จากฟอร์ม)
- **Subject**: `Thank you for donating`
- **Body**: `Thank you so much more donating - you are clearly an occasional donor`

### ขั้นตอนที่ 8: เพิ่ม Default Case - "All-the-time donor"

1. **ภายใน Switch** คลิกที่ **Default** case (หรือสร้าง Default ถ้ายังไม่มี)
2. **ภายใน Default** คลิก **Add an action**
3. ค้นหาและเลือก **Create event (V4)**
4. ตั้งค่า:
   - **Calendar id**: เลือก Calendar ของคุณ (เช่น `ปฏิทิน` หรือ `Calendar`)
   - **Subject**: พิมพ์ `We have all-time-donor! Let Discuss how to bring him /her in`
   - **Start time**: คลิก **Expression** (fx) และพิมพ์:
     ```
     addDays(utcNow(), 1)
     ```
   - **End time**: คลิก **Expression** (fx) และพิมพ์:
     ```
     addDays(utcNow(), 1)
     ```
     หรือเพิ่มเวลา:
     ```
     addHours(addDays(utcNow(), 1), 1)
     ```
   - **Time zone**: เลือก `(UTC+07:00) Bangkok, Hanoi, Jakarta` (หรือ Time zone ที่คุณต้องการ)
   - **Required attendees**: เพิ่ม Email address ของเพื่อนร่วมงาน (เช่น `colleague@example.com`)

> 💡 **คำอธิบาย**: Action นี้จะสร้าง Calendar Event เมื่อผู้บริจาคเป็น "All-the-time donor"
> - Subject: `We have all-time-donor! Let Discuss how to bring him /her in`
> - Start/End time: พรุ่งนี้
> - Required attendees: เชิญเพื่อนร่วมงานเข้าร่วมประชุม

**ตัวอย่าง Calendar Event**:
- **Subject**: `We have all-time-donor! Let Discuss how to bring him /her in`
- **Start time**: พรุ่งนี้ เวลา 00:00
- **End time**: พรุ่งนี้ เวลา 01:00
- **Time zone**: (UTC+07:00) Bangkok, Hanoi, Jakarta
- **Required attendees**: `colleague@example.com`

### ขั้นตอนที่ 9: ทดสอบ Flow

#### ทดสอบกรณีที่ 1: "First time donor"

1. คลิก **Save**
2. เปิดฟอร์ม "Charity Form" ใน Browser
3. กรอกข้อมูล:
   - **What's your name?**: `ทดสอบ 1`
   - **What's your E-Mail address?**: `test1@example.com`
   - **What amount do you plan on donating?**: `3000`
   - **What type of donor describes you the best?**: `First time donor`
4. ส่งฟอร์ม
5. รอให้ Flow รันอัตโนมัติ
6. ตรวจสอบผลลัพธ์:
   - ควรเห็น Teams Message: `Someone has submitted a Charity form.` (ส่งทันที - Parallel)
   - ควรเห็น Email: `We have a first time donor` (ส่งหลังจาก Get response details เสร็จ)

#### ทดสอบกรณีที่ 2: "Occasional donor"

1. เปิดฟอร์ม "Charity Form" อีกครั้ง
2. กรอกข้อมูล:
   - **What's your name?**: `ทดสอบ 2`
   - **What's your E-Mail address?**: `test2@example.com`
   - **What amount do you plan on donating?**: `5000`
   - **What type of donor describes you the best?**: `Occasional donor`
3. ส่งฟอร์ม
4. รอให้ Flow รันอัตโนมัติ
5. ตรวจสอบผลลัพธ์:
   - ควรเห็น Teams Message: `Someone has submitted a Charity form.` (ส่งทันที - Parallel)
   - ควรเห็น Email: `Thank you for donating` (ส่งไปยัง `test2@example.com`)

#### ทดสอบกรณีที่ 3: "All-the-time donor"

1. เปิดฟอร์ม "Charity Form" อีกครั้ง
2. กรอกข้อมูล:
   - **What's your name?**: `ทดสอบ 3`
   - **What's your E-Mail address?**: `test3@example.com`
   - **What amount do you plan on donating?**: `10000`
   - **What type of donor describes you the best?**: `All-the-time donor`
3. ส่งฟอร์ม
4. รอให้ Flow รันอัตโนมัติ
5. ตรวจสอบผลลัพธ์:
   - ควรเห็น Teams Message: `Someone has submitted a Charity form.` (ส่งทันที - Parallel)
   - ควรเห็น Calendar Event: `We have all-time-donor! Let Discuss how to bring him /her in` (พรุ่งนี้)

---

## 📊 ผลลัพธ์ที่คาดหวัง

### ทุกกรณี

คุณควรจะเห็น:
- ✅ **Teams Message**: `Someone has submitted a Charity form.` (ส่งทันที - Parallel Branch)
- ✅ **Switch Condition**: ทำงานตามประเภทผู้บริจาค:
  - **First time donor**: Email "We have a first time donor"
  - **Occasional donor**: Email "Thank you for donating"
  - **All-the-time donor**: Calendar Event

**ลำดับการทำงาน**:
1. Trigger: เมื่อมีการส่งฟอร์ม
2. **Parallel Branches** (ทำงานพร้อมกัน):
   - Branch 1: Get response details (ดึงข้อมูล)
   - Branch 2: Post message to Teams (แจ้งเตือนทันที)
3. หลังจาก Branch 1 เสร็จ → Switch Condition:
   - ตรวจสอบประเภทผู้บริจาค
   - ส่ง Email หรือสร้าง Calendar Event ตาม Case

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **Microsoft Forms Trigger**: ใช้ "When a new response is submitted" เพื่อ Trigger Flow อัตโนมัติ  
✅ **Parallel Branches**: สร้าง Branches ที่ทำงานพร้อมกันเพื่อลดเวลา  
✅ **Get response details**: ดึงข้อมูลรายละเอียดทั้งหมดจากฟอร์มที่ส่งมา  
✅ **Switch Condition**: ใช้ Switch เพื่อจัดการหลาย Case จากค่าเดียว  
✅ **Multiple Cases**: สร้าง Case หลายตัวเพื่อจัดการเงื่อนไขต่างๆ  
✅ **Default Case**: ใช้ Default case สำหรับกรณีที่ไม่ตรงกับ Case ใดๆ  
✅ **Dynamic Content**: ใช้ Dynamic Content จาก Forms ใน Actions  
✅ **Email Integration**: ส่ง Email ผ่าน Office 365 Outlook  
✅ **Teams Integration**: ส่งข้อความไปยัง Teams Chat/Channel  
✅ **Calendar Integration**: สร้าง Calendar Event อัตโนมัติ  

---

## 💡 Parallel Branches: เมื่อไหร่ควรใช้?

### ใช้ Parallel Branches เมื่อ:
- ✅ Actions หลายตัวไม่ต้องพึ่งพากัน (Independent)
- ✅ ต้องการลดเวลาในการรัน Flow
- ✅ Actions หลายตัวสามารถทำงานพร้อมกันได้
- ✅ ต้องการเพิ่มประสิทธิภาพของ Flow

**ตัวอย่างการใช้งาน**:
```
Trigger
  ├─ Branch 1: Get Data from API 1 (Parallel)
  ├─ Branch 2: Get Data from API 2 (Parallel)
  └─ Branch 3: Send Notification (Parallel)
```

### ไม่ใช้ Parallel Branches เมื่อ:
- ❌ Actions ต้องใช้ผลลัพธ์จาก Actions อื่น (Dependent)
- ❌ ต้องทำงานตามลำดับ (Sequential)
- ❌ ต้องการควบคุมลำดับการทำงาน

**ตัวอย่างที่ไม่ควรใช้ Parallel**:
```
Action 1: Get Data
  ↓ (ต้องใช้ผลลัพธ์จาก Action 1)
Action 2: Process Data
  ↓ (ต้องใช้ผลลัพธ์จาก Action 2)
Action 3: Send Result
```

---

## 💡 Tips & Tricks

1. **การสร้าง Parallel Branches**:
   - คลิกที่ Action หรือ Trigger ที่ต้องการสร้าง Branch
   - คลิกที่ **+** ที่อยู่ด้านล่าง (ไม่ใช่ New step)
   - เลือก Action ที่ต้องการ (จะสร้าง Branch ใหม่)

2. **การจัดการ Branches**:
   - Branches จะทำงานพร้อมกันโดยอัตโนมัติ
   - ไม่ต้องรอ Branch อื่นเสร็จ
   - แต่ละ Branch เป็นอิสระจากกัน

3. **การรวม Branches**:
   - Branches จะรวมกันเมื่อมี Action ใหม่ที่ต้องใช้ผลลัพธ์จาก Branches
   - เช่น Switch Condition ที่ต้องใช้ผลลัพธ์จาก Get response details

4. **การ Debug Parallel Branches**:
   - ตรวจสอบ Run History เพื่อดูว่า Branches ทำงานพร้อมกันหรือไม่
   - ดู Duration ของแต่ละ Branch เพื่อเปรียบเทียบเวลา

5. **การเพิ่ม Branches**:
   - สามารถเพิ่ม Branches ได้หลายตัว
   - แต่ละ Branch จะทำงานพร้อมกัน
   - ระวัง Rate Limits ของ Connectors

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **เพิ่ม Branch ใหม่**:
   - เพิ่ม Branch 3: Send Email แจ้งเตือนไปยัง Admin
   - ทำงานพร้อมกับ Branch 1 และ Branch 2

2. **เพิ่ม Delay ใน Branch**:
   - เพิ่ม Delay 5 seconds ใน Branch 2
   - ดูว่า Branch 1 และ Branch 2 ยังทำงานพร้อมกันหรือไม่

3. **ใช้ Condition ใน Branch**:
   - เพิ่ม Condition ใน Branch 1 เพื่อตรวจสอบจำนวนเงินบริจาค
   - ถ้า > 5000 → ส่ง Email แจ้งเตือน

4. **เพิ่ม Switch ใน Branch ใหม่**:
   - สร้าง Branch ใหม่ที่มี Switch Condition
   - ทำงานพร้อมกับ Branch อื่นๆ

5. **ใช้ Apply to Each ใน Branch**:
   - เพิ่ม Apply to Each ใน Branch 1 เพื่อประมวลผลข้อมูลหลายรายการ
   - ทำงานพร้อมกับ Branch อื่นๆ

6. **เพิ่มการบันทึกข้อมูล**:
   - เพิ่ม Branch ใหม่: บันทึกข้อมูลลง SharePoint List
   - ทำงานพร้อมกับ Branches อื่นๆ

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: Parallel Branches คืออะไร?**  
A: Parallel Branches คือการสร้าง Branches หลายตัวที่ทำงานพร้อมกัน (ไม่ต้องรอกัน) เพื่อลดเวลาในการรัน Flow

**Q: วิธีสร้าง Parallel Branches?**  
A: คลิกที่ Action หรือ Trigger แล้วคลิกที่ **+** ที่อยู่ด้านล่าง (ไม่ใช่ New step) เพื่อสร้าง Branch ใหม่

**Q: Branches จะทำงานพร้อมกันหรือไม่?**  
A: ใช่, Branches จะทำงานพร้อมกันโดยอัตโนมัติ ไม่ต้องรอ Branch อื่นเสร็จ

**Q: ข้อดีของ Parallel Branches คืออะไร?**  
A: ลดเวลาในการรัน Flow, เพิ่มประสิทธิภาพ, และทำให้ Flow รันเร็วขึ้น

**Q: ข้อเสียของ Parallel Branches คืออะไร?**  
A: ใช้ Resources มากขึ้น (Connectors, API Calls), อาจถึง Rate Limits, และ Debug ยากขึ้น

**Q: เมื่อไหร่ควรใช้ Parallel Branches?**  
A: เมื่อ Actions หลายตัวไม่ต้องพึ่งพากัน และต้องการลดเวลาในการรัน Flow

**Q: เมื่อไหร่ไม่ควรใช้ Parallel Branches?**  
A: เมื่อ Actions ต้องใช้ผลลัพธ์จาก Actions อื่น หรือต้องทำงานตามลำดับ

**Q: Branches จะรวมกันเมื่อไหร่?**  
A: Branches จะรวมกันเมื่อมี Action ใหม่ที่ต้องใช้ผลลัพธ์จาก Branches (เช่น Switch Condition)

**Q: สามารถเพิ่ม Branches ได้กี่ตัว?**  
A: สามารถเพิ่มได้หลายตัว แต่ระวัง Rate Limits และ Resource Usage

**Q: วิธี Debug Parallel Branches?**  
A: ตรวจสอบ Run History เพื่อดูว่า Branches ทำงานพร้อมกันหรือไม่, ดู Duration ของแต่ละ Branch

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การใช้ Microsoft Forms Trigger และ Get response details
- **การใช้ Parallel Branches เพื่อให้ Actions ทำงานพร้อมกัน**
- **การสร้าง Branches หลายตัวที่ไม่ต้องรอกัน**
- การใช้ Switch Condition เพื่อจัดการหลาย Case
- การส่ง Email และ Teams Message ตาม Case
- การสร้าง Calendar Event อัตโนมัติ

**Flow Structure**:
```
When a new response is submitted (Charity Form)
  ├─ Branch 1: Get response details (Parallel)
  │   └─ Switch: What type of donor describes you the best?
  │       ├─ Case 1: "First time donor" → Send Email (V2)
  │       ├─ Case 2: "Occasional donor" → Send Email (V2) 1
  │       └─ Default: "All-the-time donor" → Create Event (V4)
  └─ Branch 2: Post message (Teams) - "Someone has submitted a Charity form." (Parallel)
```

**Parallel Branches Logic**:
```
1. Trigger: เมื่อมีการส่งฟอร์ม
2. Parallel Branches (ทำงานพร้อมกัน):
   - Branch 1: Get response details (ดึงข้อมูล)
   - Branch 2: Post message to Teams (แจ้งเตือนทันที)
3. หลังจาก Branch 1 เสร็จ → Switch Condition:
   - ตรวจสอบประเภทผู้บริจาค
   - ส่ง Email หรือสร้าง Calendar Event ตาม Case
```

**ข้อดีของการใช้ Parallel Branches**:
- ✅ ลดเวลาในการรัน Flow
- ✅ เพิ่มประสิทธิภาพ
- ✅ Actions ทำงานพร้อมกัน (ไม่ต้องรอกัน)
- ✅ ยืดหยุ่น (สามารถเพิ่ม Branches ได้ตามต้องการ)

**เมื่อไหร่ควรใช้ Parallel Branches**:
- ✅ เมื่อ Actions หลายตัวไม่ต้องพึ่งพากัน (Independent)
- ✅ เมื่อต้องการลดเวลาในการรัน Flow
- ✅ เมื่อ Actions หลายตัวสามารถทำงานพร้อมกันได้

**คำแนะนำ**:
- ⚠️ ใช้ Parallel Branches เมื่อ Actions ไม่ต้องพึ่งพากัน
- ⚠️ ระวัง Rate Limits ของ Connectors
- ⚠️ ตรวจสอบ Run History เพื่อ Debug
- ⚠️ ใช้เมื่อต้องการลดเวลาในการรัน Flow

พร้อมสำหรับบทต่อไปแล้ว! 🎉

