# Exercise 3c: Forms → Switch Condition → Email/Calendar

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่ใช้ **Microsoft Forms Trigger** ("When a new response is submitted")
- ใช้ **Get response details** เพื่อดึงข้อมูลจากฟอร์ม
- ใช้ **Switch Condition** เพื่อจัดการหลาย Case (กรณี) แทนการใช้ Condition หลายตัว
- ส่ง Email ที่แตกต่างกันตามประเภทผู้บริจาค
- สร้าง Calendar Event สำหรับผู้บริจาคประเภทพิเศษ

> 💡 **หมายเหตุ**: Exercise นี้แสดงการใช้งาน **Switch Condition**:
> - Switch ใช้สำหรับจัดการหลาย Case จากค่าเดียว
> - Case 1: "First time donor" → ส่ง Email แจ้งเตือน
> - Case 2: "Occasional donor" → ส่ง Email ขอบคุณ
> - Default: "All-the-time donor" → สร้าง Calendar Event

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- Microsoft Forms Account (สำหรับสร้างฟอร์ม "Charity Form")
  - 💡 **หมายเหตุ**: ใช้ฟอร์ม "Charity Form" เดียวกับ Exercise 3b
- ความรู้พื้นฐานเรื่อง Flow Creation และการใช้งาน Condition
- Email Account ที่เชื่อมต่อกับ Power Automate
- Teams Account (สำหรับส่งข้อความ)
- Calendar Account (สำหรับสร้าง Event)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: ตรวจสอบ Charity Form

ก่อนเริ่ม Flow ตรวจสอบว่าคุณมีฟอร์ม "Charity Form" แล้วหรือไม่:

1. เข้าสู่ **Microsoft Forms** (https://forms.office.com)
2. ตรวจสอบว่ามีฟอร์มชื่อ `Charity Form` ที่มีคำถาม:
   - **What's your name?** (Text)
   - **What's your E-Mail address?** (Text)
   - **What amount do you plan on donating?** (Number)
   - **What type of donor describes you the best?** (Choice)
     - Options: `First time donor`, `Occasional donor`, `All-the-time donor`

> 💡 **หมายเหตุ**: ถ้ายังไม่มีฟอร์ม ให้สร้างตาม Exercise 3b หรือสร้างใหม่ตามขั้นตอนที่ 1 ของ Exercise 3b

### ขั้นตอนที่ 2: สร้าง Flow ใหม่พร้อม Forms Trigger

1. เข้าสู่ **Power Automate** (https://make.powerautomate.com/)
2. คลิก **Create** → เลือก **Automated cloud flow**
3. ตั้งชื่อ Flow: `Exercise 3c - Forms Switch Condition`
4. ค้นหาและเลือก Trigger: **When a new response is submitted**
5. คลิก **Create**

### ขั้นตอนที่ 3: ตั้งค่า Forms Trigger

1. ใน **When a new response is submitted**:
   - **Form Id**: เลือก `Charity Form` (หรือชื่อฟอร์มที่คุณสร้าง)
   - **Connection**: ตรวจสอบว่าเชื่อมต่อกับ Microsoft Forms Account ของคุณแล้ว

> 💡 **หมายเหตุ**: Trigger นี้จะทำงานอัตโนมัติเมื่อมีคนส่งฟอร์ม "Charity Form"

### ขั้นตอนที่ 4: เพิ่ม Get response details

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Get response details**
3. ตั้งค่า:
   - **Form Id**: เลือก `Charity Form` (เหมือน Trigger)
   - **Response Id**: เลือก `Response Id` จาก Dynamic Content (จาก Trigger)

> 💡 **คำอธิบาย**: Get response details จะดึงข้อมูลรายละเอียดทั้งหมดจากฟอร์มที่ส่งมา
> - ข้อมูลที่ได้: ชื่อ, Email, จำนวนเงินบริจาค, ประเภทผู้บริจาค

**ข้อมูลที่สามารถเข้าถึงได้**:
- `What's your name?` → ชื่อผู้บริจาค
- `What's your E-Mail address?` → Email ผู้บริจาค
- `What amount do you plan on donating?` → จำนวนเงินบริจาค (Number)
- `What type of donor describes you the best?` → ประเภทผู้บริจาค

### ขั้นตอนที่ 5: ส่งข้อความแจ้งเตือนไปยัง Teams

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Message**: ใช้ Dynamic Content เพื่อสร้างข้อความ:
     ```
     {What's your name?} has submitted a Charity form.
     ```

> 💡 **คำอธิบาย**: Action นี้จะส่งข้อความแจ้งเตือนไปยัง Teams ทุกครั้งที่มีการส่งฟอร์ม

### ขั้นตอนที่ 6: เพิ่ม Switch Condition

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Switch** (หรือ **Control** → **Switch**)
3. ตั้งค่า:
   - **On**: เลือก `What type of donor describes you the best?` จาก Dynamic Content (จาก Get response details)

> 💡 **คำอธิบาย**: Switch จะตรวจสอบค่าของ "What type of donor describes you the best?" และทำงานตาม Case ที่ตรงกัน
> - **Case 1**: "First time donor" → ส่ง Email แจ้งเตือน
> - **Case 2**: "Occasional donor" → ส่ง Email ขอบคุณ
> - **Default**: "All-the-time donor" → สร้าง Calendar Event

**Switch Structure**:
```
Switch: What type of donor describes you the best?
  ├─ Case 1: "First time donor" → Send Email (V2)
  ├─ Case 2: "Occasional donor" → Send Email (V2) 1
  └─ Default: "All-the-time donor" → Create Event (V4)
```

### ขั้นตอนที่ 7: เพิ่ม Case 1 - "First time donor"

1. **ภายใน Case 1** (คลิกที่ Case 1 หรือ **Add case** ถ้ายังไม่มี)
2. ตั้งค่า Case:
   - **Equals**: พิมพ์ `First time donor` (ตรงกับค่าจากฟอร์ม)

3. **ภายใน Case 1** คลิก **Add an action**
4. ค้นหาและเลือก **Send an email (V2)**
5. ตั้งค่า:
   - **To**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Subject**: พิมพ์ `We have a first time donor`
   - **Body**: ใช้ Dynamic Content เพื่อสร้างข้อความ:
     ```
     Let's make sure to reach out of them.
     
     Name: {What's your name?}
     Email: {What's your E-Mail address?}
     Amount: {What amount do you plan on donating?}
     ```

> 💡 **คำอธิบาย**: Action นี้จะส่ง Email เมื่อผู้บริจาคเป็น "First time donor"
> - Subject: `We have a first time donor`
> - Body: รวมข้อมูลชื่อ, Email, และจำนวนเงินบริจาค

**ตัวอย่าง Email ที่ส่งออกไป**:
- **To**: `your-email@example.com`
- **Subject**: `We have a first time donor`
- **Body**: 
  ```
  Let's make sure to reach out of them.
  
  Name: สมชาย ใจดี
  Email: somchai@example.com
  Amount: 5000
  ```

### ขั้นตอนที่ 8: เพิ่ม Case 2 - "Occasional donor"

1. **ภายใน Switch** คลิก **Add case** เพื่อเพิ่ม Case 2
2. ตั้งค่า Case:
   - **Equals**: พิมพ์ `Occasional donor` (ตรงกับค่าจากฟอร์ม)

3. **ภายใน Case 2** คลิก **Add an action**
4. ค้นหาและเลือก **Send an email (V2)**
5. ตั้งค่า:
   - **To**: เลือก `What's your E-Mail address?` จาก Dynamic Content (จาก Get response details)
   - **Subject**: พิมพ์ `Thank you for donating`
   - **Body**: พิมพ์ `Thank you so much more donating - you are clearly an occasional donor`

> 💡 **คำอธิบาย**: Action นี้จะส่ง Email ขอบคุณไปยังผู้บริจาคเมื่อเป็น "Occasional donor"
> - To: Email จากฟอร์ม (ส่งไปยังผู้บริจาคโดยตรง)
> - Subject: `Thank you for donating`
> - Body: ข้อความขอบคุณ

**ตัวอย่าง Email ที่ส่งออกไป**:
- **To**: `donor@example.com` (Email จากฟอร์ม)
- **Subject**: `Thank you for donating`
- **Body**: `Thank you so much more donating - you are clearly an occasional donor`

### ขั้นตอนที่ 9: เพิ่ม Default Case - "All-the-time donor"

1. **ภายใน Switch** คลิกที่ **Default** case (หรือสร้าง Default ถ้ายังไม่มี)
2. **ภายใน Default** คลิก **Add an action**
3. ค้นหาและเลือก **Create event (V4)**
4. ตั้งค่า:
   - **Calendar id**: เลือก Calendar ของคุณ (เช่น `ปฏิทิน` หรือ `Calendar`)
   - **Subject**: พิมพ์ `We have all-time-donor! Let Discuss how to bring him /her in`
   - **Start time**: คลิก **Expression** (fx) และพิมพ์:
     ```
     addDays(utcNow(), 1)
     ```
   - **End time**: คลิก **Expression** (fx) และพิมพ์:
     ```
     addDays(utcNow(), 1)
     ```
     หรือเพิ่มเวลา:
     ```
     addHours(addDays(utcNow(), 1), 1)
     ```
   - **Time zone**: เลือก `(UTC+07:00) Bangkok, Hanoi, Jakarta` (หรือ Time zone ที่คุณต้องการ)
   - **Required attendees**: เพิ่ม Email address ของเพื่อนร่วมงาน (เช่น `colleague@example.com`)

> 💡 **คำอธิบาย**: Action นี้จะสร้าง Calendar Event เมื่อผู้บริจาคเป็น "All-the-time donor"
> - Subject: `We have all-time-donor! Let Discuss how to bring him /her in`
> - Start/End time: พรุ่งนี้
> - Required attendees: เชิญเพื่อนร่วมงานเข้าร่วมประชุม

**ตัวอย่าง Calendar Event**:
- **Subject**: `We have all-time-donor! Let Discuss how to bring him /her in`
- **Start time**: พรุ่งนี้ เวลา 00:00
- **End time**: พรุ่งนี้ เวลา 01:00
- **Time zone**: (UTC+07:00) Bangkok, Hanoi, Jakarta
- **Required attendees**: `colleague@example.com`

### ขั้นตอนที่ 10: ทดสอบ Flow

#### ทดสอบกรณีที่ 1: "First time donor"

1. คลิก **Save**
2. เปิดฟอร์ม "Charity Form" ใน Browser
3. กรอกข้อมูล:
   - **What's your name?**: `ทดสอบ 1`
   - **What's your E-Mail address?**: `test1@example.com`
   - **What amount do you plan on donating?**: `3000`
   - **What type of donor describes you the best?**: `First time donor`
4. ส่งฟอร์ม
5. รอให้ Flow รันอัตโนมัติ
6. ตรวจสอบผลลัพธ์:
   - ควรเห็น Teams Message: `ทดสอบ 1 has submitted a Charity form.`
   - ควรเห็น Email: `We have a first time donor` (ส่งไปยัง Email ของคุณ)
   - Email Body มีข้อมูล: Name, Email, Amount

#### ทดสอบกรณีที่ 2: "Occasional donor"

1. เปิดฟอร์ม "Charity Form" อีกครั้ง
2. กรอกข้อมูล:
   - **What's your name?**: `ทดสอบ 2`
   - **What's your E-Mail address?**: `test2@example.com`
   - **What amount do you plan on donating?**: `5000`
   - **What type of donor describes you the best?**: `Occasional donor`
3. ส่งฟอร์ม
4. รอให้ Flow รันอัตโนมัติ
5. ตรวจสอบผลลัพธ์:
   - ควรเห็น Teams Message: `ทดสอบ 2 has submitted a Charity form.`
   - ควรเห็น Email: `Thank you for donating` (ส่งไปยัง `test2@example.com`)
   - Email Body: `Thank you so much more donating - you are clearly an occasional donor`

#### ทดสอบกรณีที่ 3: "All-the-time donor"

1. เปิดฟอร์ม "Charity Form" อีกครั้ง
2. กรอกข้อมูล:
   - **What's your name?**: `ทดสอบ 3`
   - **What's your E-Mail address?**: `test3@example.com`
   - **What amount do you plan on donating?**: `10000`
   - **What type of donor describes you the best?**: `All-the-time donor`
3. ส่งฟอร์ม
4. รอให้ Flow รันอัตโนมัติ
5. ตรวจสอบผลลัพธ์:
   - ควรเห็น Teams Message: `ทดสอบ 3 has submitted a Charity form.`
   - ควรเห็น Calendar Event: `We have all-time-donor! Let Discuss how to bring him /her in` (พรุ่งนี้)
   - Event มี Required attendees

---

## 📊 ผลลัพธ์ที่คาดหวัง

### กรณีที่ 1: "First time donor"

คุณควรจะเห็น:
- ✅ **Teams Message**: `{ชื่อ} has submitted a Charity form.`
- ✅ **Email**: `We have a first time donor` (ส่งไปยัง Email ของคุณ)
- ✅ **Email Body**: รวมข้อมูล Name, Email, Amount
- ❌ **ไม่เห็น** Calendar Event

### กรณีที่ 2: "Occasional donor"

คุณควรจะเห็น:
- ✅ **Teams Message**: `{ชื่อ} has submitted a Charity form.`
- ✅ **Email**: `Thank you for donating` (ส่งไปยัง Email จากฟอร์ม)
- ✅ **Email Body**: `Thank you so much more donating - you are clearly an occasional donor`
- ❌ **ไม่เห็น** Calendar Event

### กรณีที่ 3: "All-the-time donor"

คุณควรจะเห็น:
- ✅ **Teams Message**: `{ชื่อ} has submitted a Charity form.`
- ✅ **Calendar Event**: `We have all-time-donor! Let Discuss how to bring him /her in` (พรุ่งนี้)
- ✅ **Event มี Required attendees**
- ❌ **ไม่เห็น** Email

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **Microsoft Forms Trigger**: ใช้ "When a new response is submitted" เพื่อ Trigger Flow อัตโนมัติ  
✅ **Get response details**: ดึงข้อมูลรายละเอียดทั้งหมดจากฟอร์มที่ส่งมา  
✅ **Switch Condition**: ใช้ Switch เพื่อจัดการหลาย Case จากค่าเดียว  
✅ **Multiple Cases**: สร้าง Case หลายตัวเพื่อจัดการเงื่อนไขต่างๆ  
✅ **Default Case**: ใช้ Default case สำหรับกรณีที่ไม่ตรงกับ Case ใดๆ  
✅ **Dynamic Content**: ใช้ Dynamic Content จาก Forms ใน Actions  
✅ **Email Integration**: ส่ง Email ผ่าน Office 365 Outlook  
✅ **Teams Integration**: ส่งข้อความไปยัง Teams Chat/Channel  
✅ **Calendar Integration**: สร้าง Calendar Event อัตโนมัติ  
✅ **Date/Time Functions**: ใช้ `addDays()`, `addHours()`, `utcNow()` เพื่อจัดการวันที่/เวลา  

---

## 💡 Switch vs Condition: เมื่อไหร่ควรใช้อะไร?

### ใช้ Switch เมื่อ:
- ✅ มีหลาย Case (3+ cases) ที่ต้องตรวจสอบจากค่าเดียว
- ✅ ต้องการ Code ที่อ่านง่ายและเป็นระเบียบ
- ✅ แต่ละ Case มีการทำงานที่แตกต่างกันชัดเจน
- ✅ ต้องการ Default case สำหรับกรณีที่ไม่ตรงกับ Case ใดๆ

**ตัวอย่าง**: Switch สำหรับ Donor Type
```
Switch: Donor Type
  ├─ Case 1: "First time donor"
  ├─ Case 2: "Occasional donor"
  └─ Default: "All-the-time donor"
```

### ใช้ Condition เมื่อ:
- ✅ มีเพียง 2 ทางเลือก (True/False)
- ✅ ต้องตรวจสอบเงื่อนไขที่ซับซ้อน (เช่น มากกว่า, น้อยกว่า, เท่ากับ)
- ✅ ต้องใช้ Logical Operators (And, Or)
- ✅ ต้องสร้าง Nested Conditions

**ตัวอย่าง**: Condition สำหรับจำนวนเงิน
```
Condition: Amount > 3000
  ├─ True: Send Email
  └─ False: Do Nothing
```

---

## 💡 Tips & Tricks

1. **Case Sensitivity (ความแตกต่างของตัวพิมพ์เล็ก-ใหญ่)**:
   - Switch ตรวจสอบแบบ **Case-Sensitive** โดยค่าเริ่มต้น
   - `First time donor` ≠ `first time donor` ≠ `FIRST TIME DONOR`
   - ตรวจสอบให้แน่ใจว่าค่าใน Case ตรงกับค่าจากฟอร์มทุกประการ

2. **การเพิ่ม Case ใหม่**:
   - คลิก **Add case** ใน Switch เพื่อเพิ่ม Case ใหม่
   - ตั้งค่า **Equals** ให้ตรงกับค่าที่ต้องการตรวจสอบ

3. **Default Case**:
   - Default case จะทำงานเมื่อค่าไม่ตรงกับ Case ใดๆ
   - ควรมี Default case เสมอเพื่อจัดการกรณีที่ไม่คาดคิด

4. **การจัดการวันที่/เวลา**:
   - `utcNow()` → วันที่/เวลาปัจจุบัน (UTC)
   - `addDays(utcNow(), 1)` → เพิ่มวัน 1 วัน
   - `addHours(utcNow(), 1)` → เพิ่มชั่วโมง 1 ชั่วโมง

5. **Required attendees ใน Calendar Event**:
   - เพิ่ม Email address ของผู้เข้าร่วม
   - ผู้เข้าร่วมจะได้รับ Email แจ้งเตือนการประชุม

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **เพิ่ม Case สำหรับ "New donor"**:
   - สร้าง Case ใหม่สำหรับ "New donor"
   - ส่ง Email แจ้งเตือนไปยัง Admin

2. **เพิ่มการตรวจสอบจำนวนเงินบริจาคในแต่ละ Case**:
   - Case 1: ถ้าจำนวนเงิน > 5,000 → ส่ง Email พิเศษ
   - Case 2: ถ้าจำนวนเงิน > 10,000 → สร้าง Calendar Event
   - Default: ถ้าจำนวนเงิน > 20,000 → สร้าง Calendar Event พร้อม Required attendees

3. **เปลี่ยน Switch เป็น Condition**:
   - ลองใช้ Condition แทน Switch เพื่อจัดการ 3 Cases
   - เปรียบเทียบความแตกต่างระหว่าง Switch และ Condition

4. **เพิ่มการ Format จำนวนเงิน**:
   - ใช้ `formatNumber()` เพื่อ Format จำนวนเงิน (เช่น 15,000)
   - ใช้ใน Email Body

5. **เพิ่มการบันทึกข้อมูลลง SharePoint List**:
   - สร้าง SharePoint List เพื่อบันทึกข้อมูลการบริจาค
   - ใช้ **Create item** เพื่อบันทึกข้อมูลทุกครั้งที่มีการส่งฟอร์ม
   - แยกข้อมูลตาม Donor Type

6. **เพิ่มการส่ง Email แจ้งเตือนไปยัง Admin**:
   - ส่ง Email แจ้งเตือนไปยัง Admin ทุกครั้งที่มีการส่งฟอร์ม
   - รวมข้อมูล: ชื่อ, Email, จำนวนเงิน, ประเภทผู้บริจาค

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: Switch กับ Condition ต่างกันอย่างไร?**  
A: Switch ใช้สำหรับจัดการหลาย Case จากค่าเดียว, Condition ใช้สำหรับเงื่อนไข 2 ทางเลือก (True/False) หรือเงื่อนไขที่ซับซ้อน

**Q: เมื่อไหร่ควรใช้ Switch?**  
A: เมื่อมีหลาย Case (3+ cases) ที่ต้องตรวจสอบจากค่าเดียว และต้องการ Code ที่อ่านง่าย

**Q: เมื่อไหร่ควรใช้ Condition?**  
A: เมื่อมีเพียง 2 ทางเลือก (True/False) หรือต้องตรวจสอบเงื่อนไขที่ซับซ้อน (เช่น มากกว่า, น้อยกว่า, เท่ากับ)

**Q: Switch ตรวจสอบ Case-Sensitive หรือไม่?**  
A: ใช่, Switch ตรวจสอบแบบ Case-Sensitive โดยค่าเริ่มต้น (`First time donor` ≠ `first time donor`)

**Q: วิธีเพิ่ม Case ใหม่ใน Switch?**  
A: คลิก **Add case** ใน Switch แล้วตั้งค่า **Equals** ให้ตรงกับค่าที่ต้องการตรวจสอบ

**Q: Default case คืออะไร?**  
A: Default case จะทำงานเมื่อค่าไม่ตรงกับ Case ใดๆ ควรมี Default case เสมอเพื่อจัดการกรณีที่ไม่คาดคิด

**Q: สามารถใช้ Switch กับ Condition ร่วมกันได้ไหม?**  
A: ได้, สามารถใช้ Switch ภายใน Condition หรือ Condition ภายใน Switch ได้

**Q: ทำไมต้องใช้ Get response details?**  
A: เพราะ Trigger "When a new response is submitted" ให้เฉพาะ Response Id, ต้องใช้ Get response details เพื่อดึงข้อมูลรายละเอียดทั้งหมดจากฟอร์ม

**Q: วิธีทดสอบ Flow ที่ใช้ Forms Trigger?**  
A: ต้องส่งฟอร์มจริงๆ เพื่อ Trigger Flow, หรือใช้ **Test** mode ใน Power Automate เพื่อจำลองการส่งฟอร์ม

**Q: Calendar Event จะสร้างเมื่อไหร่?**  
A: Calendar Event จะสร้างเมื่อผู้บริจาคเป็น "All-the-time donor" (Default case)

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การใช้ Microsoft Forms Trigger และ Get response details
- **การใช้ Switch Condition เพื่อจัดการหลาย Case**
- การสร้าง Case หลายตัวเพื่อจัดการเงื่อนไขต่างๆ
- การใช้ Default case สำหรับกรณีที่ไม่ตรงกับ Case ใดๆ
- การส่ง Email และ Teams Message ตาม Case
- การสร้าง Calendar Event อัตโนมัติ
- การใช้ Expression functions (`addDays()`, `utcNow()`)

**Flow Structure**:
```
When a new response is submitted (Charity Form)
  ↓
Get response details
  ↓
Post message (Teams) - แจ้งเตือนการส่งฟอร์ม
  ↓
Switch: What type of donor describes you the best?
  ├─ Case 1: "First time donor"
  │   └─ Send Email (V2) - "We have a first time donor"
  ├─ Case 2: "Occasional donor"
  │   └─ Send Email (V2) - "Thank you for donating"
  └─ Default: "All-the-time donor"
      └─ Create Event (V4) - "We have all-time-donor! Let Discuss how to bring him /her in"
```

**Switch Logic**:
```
ถ้า Donor Type = "First time donor"
  → ส่ง Email "We have a first time donor"
ถ้า Donor Type = "Occasional donor"
  → ส่ง Email "Thank you for donating"
ถ้าไม่ใช่ (Default - "All-the-time donor")
  → สร้าง Calendar Event
```

**ข้อดีของการใช้ Switch**:
- ✅ อ่านง่ายและเป็นระเบียบ
- ✅ จัดการหลาย Case ได้ดี
- ✅ มี Default case สำหรับกรณีที่ไม่คาดคิด
- ✅ ลดการสร้าง Condition หลายตัว

**เมื่อไหร่ควรใช้ Switch แทน Condition**:
- ✅ มีหลาย Case (3+ cases) ที่ต้องตรวจสอบจากค่าเดียว
- ✅ ต้องการ Code ที่อ่านง่ายและเป็นระเบียบ
- ✅ แต่ละ Case มีการทำงานที่แตกต่างกันชัดเจน

พร้อมสำหรับบทต่อไปแล้ว! 🎉

