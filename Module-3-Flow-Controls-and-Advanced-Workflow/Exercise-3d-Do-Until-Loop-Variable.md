# Exercise 3d: Do Until Loop → Variable Counter → Teams

## 🎯 วัตถุประสงค์

ใน Exercise นี้ คุณจะได้เรียนรู้การ:
- สร้าง Flow ที่ใช้ **Manual Trigger**
- ใช้ **Initialize variable** เพื่อสร้างและกำหนดค่าเริ่มต้นให้กับตัวแปร
- ใช้ **Do until** loop เพื่อวนลูปจนกว่าเงื่อนไขจะเป็นจริง
- ใช้ **Increment variable** เพื่อเพิ่มค่าตัวแปรใน Loop
- ส่งข้อความไป Teams ในแต่ละรอบของ Loop

> 💡 **หมายเหตุ**: Exercise นี้แสดงการใช้งาน **Do until** loop:
> - วนลูปจนกว่า Counter > 10
> - ในแต่ละรอบ: ส่งข้อความ Teams และเพิ่มค่า Counter
> - ตั้งค่า Count และ Timeout เพื่อป้องกัน Infinite Loop

---

## 📋 สิ่งที่ต้องเตรียม

- Power Automate Account (สามารถใช้ Free Account ได้)
- ความรู้พื้นฐานเรื่อง Flow Creation
- Teams Account (สำหรับส่งข้อความ)

---

## 🚀 ขั้นตอนการปฏิบัติ

### ขั้นตอนที่ 1: สร้าง Flow ใหม่

1. เข้าสู่ **Power Automate** (https://make.powerautomate.com/)
2. คลิก **Create** → เลือก **Instant cloud flow**
3. ตั้งชื่อ Flow: `Exercise 3d - Do Until Loop Variable`
4. เลือก Trigger: **Manually trigger a flow**
5. คลิก **Create**

### ขั้นตอนที่ 2: เพิ่ม Initialize variable

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Initialize variable** (หรือ **Variables** → **Initialize variable**)
3. ตั้งค่า:
   - **Name**: พิมพ์ `Counter`
   - **Type**: เลือก `Integer` จาก Dropdown
   - **Value**: พิมพ์ `1`

> 💡 **คำอธิบาย**: Initialize variable จะสร้างตัวแปร `Counter` ชนิด Integer และกำหนดค่าเริ่มต้นเป็น `1`
> - **Name**: ชื่อตัวแปร (ใช้เรียกใน Actions อื่นๆ)
> - **Type**: ชนิดของตัวแปร (Integer = จำนวนเต็ม)
> - **Value**: ค่าเริ่มต้น (1)

**ตัวแปรที่สร้าง**:
- `Counter` = `1` (Integer)

### ขั้นตอนที่ 3: เพิ่ม Do until Loop

1. เพิ่ม Action: คลิก **+ New step**
2. ค้นหาและเลือก **Do until** (หรือ **Control** → **Do until**)
3. ตั้งค่า **Loop until** (เงื่อนไขออกจาก Loop):
   - **Field 1**: เลือก `Counter` จาก Dynamic Content (จาก Initialize variable)
   - **Operator**: เลือก `is greater than`
   - **Field 2**: พิมพ์ `10` (เป็น Number)

> 💡 **คำอธิบาย**: Do until จะวนลูปจนกว่าเงื่อนไขจะเป็นจริง (Counter > 10)
> - Loop จะทำงานซ้ำๆ จนกว่า Counter จะมากกว่า 10
> - ภายใน Loop จะมี Actions ที่เพิ่มค่า Counter

**Do until Structure**:
```
Do until: Counter > 10
  ├─ Post message (Teams)
  └─ Increment variable (Counter)
```

### ขั้นตอนที่ 4: ตั้งค่า Advanced Parameters ของ Do until

1. ใน **Do until** คลิก **Show advanced options** หรือ **Advanced parameters**
2. ตั้งค่า:
   - **Count**: พิมพ์ `60` (จำนวนครั้งสูงสุดที่ Loop จะทำงาน)
   - **Timeout**: พิมพ์ `PT1H` (Timeout 1 ชั่วโมง)

> 💡 **คำอธิบาย**: 
> - **Count**: จำกัดจำนวนครั้งที่ Loop จะทำงาน (60 ครั้ง) เพื่อป้องกัน Infinite Loop
> - **Timeout**: จำกัดเวลาที่ Loop จะทำงาน (PT1H = 1 Hour) เพื่อป้องกัน Loop ไม่รู้จบ
> - **PT1H**: ISO 8601 Duration Format (PT = Period of Time, 1H = 1 Hour)

**Advanced Parameters**:
- **Count**: `60` (Maximum iterations)
- **Timeout**: `PT1H` (1 hour timeout)

### ขั้นตอนที่ 5: เพิ่ม Action ใน Do until - Post message to Teams

1. **ภายใน Do until** คลิก **Add an action**
2. ค้นหาและเลือก **Post message in a chat or channel**
3. ตั้งค่า:
   - **Post as**: `Flow bot`
   - **Post in**: `Chat with Flow bot`
   - **Recipient**: ใส่ Email address ของคุณ (เช่น `your-email@example.com`)
   - **Message**: ใช้ Dynamic Content เพื่อสร้างข้อความ:
     ```
     Counter = {Counter}
     ```
     หรือพิมพ์:
     ```
     Counter = 
     ```
     แล้วเลือก `Counter` จาก Dynamic Content

> 💡 **คำอธิบาย**: Action นี้จะส่งข้อความไป Teams ในแต่ละรอบของ Loop
> - ข้อความจะแสดงค่า Counter ในปัจจุบัน (1, 2, 3, ..., 10)

**ตัวอย่าง Teams Message ในแต่ละรอบ**:
- รอบที่ 1: `Counter = 1`
- รอบที่ 2: `Counter = 2`
- รอบที่ 3: `Counter = 3`
- ...
- รอบที่ 10: `Counter = 10`
- รอบที่ 11: `Counter = 11` (Loop หยุดเพราะ Counter > 10)

### ขั้นตอนที่ 6: เพิ่ม Action ใน Do until - Increment variable

1. **ภายใน Do until** (หลังจาก Post message) คลิก **Add an action**
2. ค้นหาและเลือก **Increment variable** (หรือ **Variables** → **Increment variable**)
3. ตั้งค่า:
   - **Name**: เลือก `Counter` จาก Dropdown (ตัวแปรที่เราสร้างไว้)
   - **Value**: พิมพ์ `1` (เพิ่มค่าทีละ 1)

> 💡 **คำอธิบาย**: Increment variable จะเพิ่มค่าของตัวแปร `Counter` ทีละ `1`
> - รอบที่ 1: Counter = 1 → Increment → Counter = 2
> - รอบที่ 2: Counter = 2 → Increment → Counter = 3
> - ...
> - รอบที่ 10: Counter = 10 → Increment → Counter = 11
> - รอบที่ 11: Counter = 11 > 10 → Loop หยุด

**Flow Structure**:
```
Manual Trigger
  ↓
Initialize variable (Counter = 1)
  ↓
Do until (Counter > 10)
  ├─ Post message (Teams) - "Counter = {Counter}"
  └─ Increment variable (Counter + 1)
```

### ขั้นตอนที่ 7: ทดสอบ Flow

1. คลิก **Save**
2. คลิก **Test** → **Manually** → **Run flow**
3. รอให้ Flow รันเสร็จ
4. ตรวจสอบผลลัพธ์:
   - ควรเห็น Teams Messages 10 ข้อความ (Counter = 1 ถึง Counter = 10)
   - ข้อความที่ 11 (Counter = 11) จะไม่ถูกส่ง เพราะ Loop หยุดที่ Counter > 10

**ตัวอย่างผลลัพธ์ใน Teams**:
```
Counter = 1
Counter = 2
Counter = 3
Counter = 4
Counter = 5
Counter = 6
Counter = 7
Counter = 8
Counter = 9
Counter = 10
```

---

## 📊 ผลลัพธ์ที่คาดหวัง

คุณควรจะเห็น:
- ✅ **Teams Messages**: 10 ข้อความ (Counter = 1 ถึง Counter = 10)
- ✅ **Loop หยุด**: เมื่อ Counter = 11 (Counter > 10)
- ✅ **Flow รันเสร็จ**: ภายใน 1-2 นาที

**ลำดับการทำงาน**:
1. Initialize Counter = 1
2. Loop รอบที่ 1: Counter = 1 → ส่ง Teams → Increment → Counter = 2
3. Loop รอบที่ 2: Counter = 2 → ส่ง Teams → Increment → Counter = 3
4. ...
5. Loop รอบที่ 10: Counter = 10 → ส่ง Teams → Increment → Counter = 11
6. Loop หยุด: Counter = 11 > 10 ✓

---

## 🎓 สิ่งที่ได้เรียนรู้

✅ **Initialize variable**: สร้างและกำหนดค่าเริ่มต้นให้กับตัวแปร  
✅ **Do until Loop**: วนลูปจนกว่าเงื่อนไขจะเป็นจริง  
✅ **Increment variable**: เพิ่มค่าตัวแปรใน Loop  
✅ **Advanced Parameters**: ตั้งค่า Count และ Timeout เพื่อป้องกัน Infinite Loop  
✅ **Dynamic Content**: ใช้ตัวแปรใน Actions  
✅ **Teams Integration**: ส่งข้อความไปยัง Teams Chat/Channel  

---

## 💡 Tips & Tricks

1. **การตั้งชื่อตัวแปร**:
   - ใช้ชื่อที่สื่อความหมาย (เช่น `Counter`, `Index`, `RetryCount`)
   - หลีกเลี่ยงชื่อที่สับสน (เช่น `x`, `temp`, `var1`)

2. **Count และ Timeout**:
   - **Count**: จำกัดจำนวนครั้งที่ Loop จะทำงาน (ป้องกัน Infinite Loop)
   - **Timeout**: จำกัดเวลาที่ Loop จะทำงาน (ป้องกัน Loop ไม่รู้จบ)
   - **แนะนำ**: ตั้งค่า Count และ Timeout เสมอเมื่อใช้ Do until

3. **ISO 8601 Duration Format**:
   - `PT1H` = 1 Hour (Period of Time, 1 Hour)
   - `PT30M` = 30 Minutes
   - `PT1S` = 1 Second
   - `PT1H30M` = 1 Hour 30 Minutes

4. **การ Debug Loop**:
   - ใช้ Post message เพื่อดูค่า Counter ในแต่ละรอบ
   - ตรวจสอบว่า Increment variable ทำงานถูกต้อง
   - ตรวจสอบว่าเงื่อนไขออกจาก Loop ถูกต้อง

5. **การป้องกัน Infinite Loop**:
   - ตั้งค่า Count และ Timeout เสมอ
   - ตรวจสอบว่าเงื่อนไขออกจาก Loop จะเป็นจริงเสมอ
   - ใช้ Terminate action ถ้าจำเป็น

---

## 🔄 แบบฝึกหัดเพิ่มเติม

1. **เปลี่ยนเงื่อนไข Loop**:
   - เปลี่ยนจาก Counter > 10 เป็น Counter >= 10
   - เปลี่ยนจาก Counter > 10 เป็น Counter > 5
   - ดูว่า Loop ทำงานกี่รอบ

2. **เปลี่ยนค่า Increment**:
   - เปลี่ยนจาก Increment 1 เป็น Increment 2
   - ดูว่า Loop ทำงานกี่รอบ

3. **เพิ่ม Condition ใน Loop**:
   - เพิ่ม Condition เพื่อตรวจสอบว่า Counter = 5
   - ถ้า Counter = 5 → ส่ง Email แจ้งเตือน
   - ถ้าไม่ใช่ → ส่ง Teams Message ตามปกติ

4. **ใช้ Decrement variable**:
   - เปลี่ยนจาก Increment เป็น Decrement
   - ตั้งค่า Counter = 10
   - Loop จนกว่า Counter < 1

5. **เพิ่ม Delay ใน Loop**:
   - เพิ่ม Delay 1 second ในแต่ละรอบ
   - ดูว่า Loop ใช้เวลานานขึ้นแค่ไหน

6. **ใช้ Switch ใน Loop**:
   - ใช้ Switch เพื่อจัดการค่าต่างๆ ของ Counter
   - Case 1: Counter = 1 → ส่ง Email
   - Case 2: Counter = 5 → ส่ง Teams
   - Default: Counter อื่น → ไม่ทำอะไร

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: Do until กับ Apply to Each ต่างกันอย่างไร?**  
A: Do until วนลูปจนกว่าเงื่อนไขจะเป็นจริง (ตรวจสอบเงื่อนไขก่อน), Apply to Each วนลูปผ่านทุก item ใน Array (รู้จำนวนรอบล่วงหน้า)

**Q: ทำไมต้องใช้ Initialize variable ก่อน?**  
A: เพราะต้องสร้างตัวแปรก่อนใช้งาน, ถ้าไม่ Initialize จะไม่สามารถใช้ตัวแปรใน Actions อื่นๆ ได้

**Q: Count และ Timeout คืออะไร?**  
A: Count คือจำนวนครั้งสูงสุดที่ Loop จะทำงาน, Timeout คือเวลาสูงสุดที่ Loop จะทำงาน (ป้องกัน Infinite Loop)

**Q: ISO 8601 Duration Format คืออะไร?**  
A: เป็นรูปแบบมาตรฐานสำหรับระบุระยะเวลา (เช่น PT1H = 1 Hour, PT30M = 30 Minutes)

**Q: Loop จะหยุดเมื่อไหร่?**  
A: Loop จะหยุดเมื่อเงื่อนไขเป็นจริง (Counter > 10) หรือเมื่อถึง Count หรือ Timeout ที่กำหนด

**Q: ถ้าไม่ตั้งค่า Count และ Timeout จะเกิดอะไรขึ้น?**  
A: ถ้าเงื่อนไขไม่เป็นจริง Loop จะทำงานไม่รู้จบ (Infinite Loop) ซึ่งจะทำให้ Flow ใช้เวลานานและอาจเกิด Error

**Q: สามารถใช้ตัวแปรหลายตัวใน Do until ได้ไหม?**  
A: ได้, สามารถใช้ตัวแปรหลายตัวและตรวจสอบเงื่อนไขหลายเงื่อนไขได้ (ใช้ And หรือ Or)

**Q: วิธีใช้ Expression ใน Loop until condition?**  
A: คลิก **Edit in advanced mode** เพื่อใช้ Expression (เช่น `greater(variables('Counter'), 10)`)

---

## 📝 สรุป

ใน Exercise นี้ คุณได้เรียนรู้:
- การสร้างและ Initialize variable (Counter)
- **การใช้ Do until Loop เพื่อวนลูปจนกว่าเงื่อนไขจะเป็นจริง**
- **การใช้ Increment variable เพื่อเพิ่มค่าตัวแปรใน Loop**
- การตั้งค่า Count และ Timeout เพื่อป้องกัน Infinite Loop
- การส่งข้อความไป Teams ในแต่ละรอบของ Loop

**Flow Structure**:
```
Manual Trigger
  ↓
Initialize variable (Counter = 1, Integer)
  ↓
Do until (Counter > 10)
  ├─ Post message (Teams) - "Counter = {Counter}"
  └─ Increment variable (Counter + 1)
  Count: 60, Timeout: PT1H
```

**Loop Logic**:
```
1. Initialize Counter = 1
2. Loop:
   - ส่ง Teams Message: "Counter = {Counter}"
   - Increment Counter = Counter + 1
   - ตรวจสอบ: Counter > 10?
     - ถ้าใช่ → หยุด Loop
     - ถ้าไม่ใช่ → วนลูปต่อไป
3. Loop หยุดเมื่อ Counter = 11 (Counter > 10)
```

**ข้อดีของการใช้ Do until**:
- ✅ วนลูปจนกว่าเงื่อนไขจะเป็นจริง
- ✅ เหมาะสำหรับการรอสถานะหรือการ Retry
- ✅ สามารถตั้งค่า Count และ Timeout เพื่อป้องกัน Infinite Loop

**เมื่อไหร่ควรใช้ Do until**:
- ✅ เมื่อต้องวนลูปจนกว่าเงื่อนไขจะเป็นจริง (ไม่รู้จำนวนรอบล่วงหน้า)
- ✅ เมื่อต้อง Retry จนกว่าจะสำเร็จ
- ✅ เมื่อต้องรอสถานะจนกว่าจะพร้อม

**คำแนะนำ**:
- ⚠️ ตั้งค่า Count และ Timeout เสมอเพื่อป้องกัน Infinite Loop
- ⚠️ ตรวจสอบว่าเงื่อนไขออกจาก Loop จะเป็นจริงเสมอ
- ⚠️ ใช้ Post message หรือ Compose เพื่อ Debug ค่าในแต่ละรอบ

พร้อมสำหรับบทต่อไปแล้ว! 🎉

