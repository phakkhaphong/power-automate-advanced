# บทที่ 3: Flow Controls & Advanced Workflow

## 🎯 ภาพรวม

บทนี้จะเรียนรู้เกี่ยวกับ **Flow Control** (การควบคุม Flow) ที่ใช้สร้าง Logic ที่ซับซ้อน

**สิ่งที่คุณจะได้เรียนรู้**:
- ✅ Conditions (เงื่อนไข): If-Else, Switch
- ✅ Loops (วนลูป): Apply to Each, Do Until
- ✅ Scopes (ขอบเขต): จัดกลุ่ม Actions
- ✅ Parallel Processing (ประมวลผลแบบขนาน): ทำหลายอย่างพร้อมกัน

> 💡 **คิดง่ายๆ**: บทนี้สอนให้ Flow "คิดเป็น" และ "ทำซ้ำ" ได้

---

## เนื้อหาการเรียนรู้

### 1. Conditional Logic: If–Else, Switch, Nested Conditions (เงื่อนไข)

#### If Condition (ถ้า...แล้ว...)

**If Condition** = ตรวจสอบเงื่อนไข ถ้าเป็นจริง → ทำอย่างหนึ่ง, ถ้าไม่จริง → ทำอีกอย่างหนึ่ง

**ตัวอย่างง่ายๆ**:
```
ถ้าเงินเดือน > 50000
  → ส่งไปอนุมัติ Manager
ถ้าไม่ใช่
  → อนุมัติอัตโนมัติ
```

**ประเภทของ If**:
- **Basic If**: ตรวจสอบเงื่อนไขเดียว
- **If–Else**: มี 2 ทางเลือก (ถ้าใช่ → ทำ A, ถ้าไม่ใช่ → ทำ B)
- **Nested If**: เงื่อนไขซ้อนกัน (ถ้า A ใช่ → ตรวจสอบ B อีกที)

**Multiple Conditions** (เงื่อนไขหลายข้อ):
- ใช้ `and`: ทั้งหมดต้องเป็นจริง
  ```
  เงินเดือน > 50000 AND แผนก = "ไอที"
  ```
- ใช้ `or`: มีอันใดอันหนึ่งเป็นจริง
  ```
  เงินเดือน > 50000 OR เป็น Manager
  ```

#### Switch Condition (สวิตช์)

**Switch** = ใช้เมื่อมีหลาย Case (กรณี)

**ตัวอย่าง**:
```
Switch: สถานะ
  Case "รออนุมัติ" → ส่ง Email
  Case "อนุมัติแล้ว" → อัพเดทฐานข้อมูล
  Case "ปฏิเสธ" → ส่ง Notification
  Default → ไม่ทำอะไร
```

**เมื่อไหร่ควรใช้ Switch**:
- เมื่อมีหลาย Case ที่เท่ากัน (เช่น หลายสถานะ)
- ทำให้โค้ดอ่านง่ายกว่า If-Else หลายตัว

#### Best Practices (วิธีที่ดี)
- หลีกเลี่ยงเงื่อนไขซ้อนกันมากเกินไป (อ่านยาก)
- ใช้ Switch เมื่อมีหลาย Case
- แยกเงื่อนไขที่ซับซ้อนออกเป็น Variables

---

### 2. Loops: Apply to Each, Do Until (วนลูป)

#### Apply to Each (วนลูปผ่าน Array)

**Apply to Each** = วนลูปผ่านทุก item ใน Array

**ตัวอย่าง**:
```
มีนักเรียน 10 คน
วนลูป 10 ครั้ง:
  รอบที่ 1: ส่ง Email ให้นักเรียนคนที่ 1
  รอบที่ 2: ส่ง Email ให้นักเรียนคนที่ 2
  ...
  รอบที่ 10: ส่ง Email ให้นักเรียนคนที่ 10
```

**item()** = ข้อมูลในรอบปัจจุบัน
- รอบที่ 1: `item()` = นักเรียนคนที่ 1
- รอบที่ 2: `item()` = นักเรียนคนที่ 2

**Nested Loops** (ลูปซ้อนลูป):
- ระวัง! อาจทำให้ช้ามาก
- ตัวอย่าง: วนลูปนักเรียน 10 คน → วนลูปวิชา 5 วิชา = 50 รอบ!

**Concurrency** (ทำพร้อมกัน):
- ตั้งค่าให้ทำหลายอันพร้อมกัน (เร็วกว่า)
- แต่ระวัง Rate Limits!

#### Do Until (ทำจนกว่า...)

**Do Until** = วนลูปจนกว่าเงื่อนไขจะเป็นจริง

**ตัวอย่าง**:
```
วนลูป:
  - เรียก API
  - ตรวจสอบสถานะ
  - ถ้าสถานะ = "เสร็จสิ้น" → หยุด
  - ถ้ายังไม่เสร็จ → วนลูปต่อไป
```

**Timeout** (หมดเวลา):
- ตั้งค่า Timeout เพื่อป้องกัน Infinite Loop
- ถ้าเกินเวลาที่กำหนด → หยุด

**Iteration Limit** (จำกัดจำนวนครั้ง):
- จำกัดจำนวนครั้งที่วนลูป (เช่น 10 ครั้ง)
- ป้องกัน Loop ไม่รู้จบ

#### Loop Best Practices (วิธีที่ดี)
- หลีกเลี่ยงลูปซ้อนกันมากเกินไป
- กรองข้อมูลก่อน Loop (ใช้ Filter Array)
- ตั้งค่า Timeout และ Iteration Limit

---

### 3. Scope & Action Grouping

#### Scope Action
- **Purpose**: จัดกลุ่ม Actions และจัดการ Error Handling
- **Scope Levels**: Nested Scopes
- **Error Isolation**: แยก Error Handling ในแต่ละ Scope
- **Variable Scope**: ตัวแปรที่สร้างใน Scope ใช้ได้ใน Scope นั้น

#### Action Grouping
- **Logical Grouping**: จัดกลุ่ม Actions ที่เกี่ยวข้องกัน
- **Visual Organization**: ทำให้ Flow อ่านง่ายขึ้น
- **Error Handling**: จัดการ Error ในระดับ Group

---

### 4. Parallel Branching, Delay, Timeout Patterns

#### Parallel Branching
- **Parallel Execution**: รันหลาย Actions พร้อมกัน
- **Use Cases**: เรียก API หลายตัวพร้อมกัน, ประมวลผลข้อมูลหลายชุด
- **Concurrency Control**: ควบคุมจำนวน Actions ที่รันพร้อมกัน
- **Synchronization**: รวมผลลัพธ์จากหลาย Branches

#### Delay Action
- **Fixed Delay**: หน่วงเวลาตามจำนวนวินาที
- **Dynamic Delay**: ใช้ Expression เพื่อคำนวณเวลา Delay
- **Use Cases**: Retry Logic, Rate Limiting, Scheduled Processing

#### Timeout Patterns
- **Action Timeout**: ตั้งค่า Timeout สำหรับแต่ละ Action
- **Flow Timeout**: ตั้งค่า Timeout สำหรับทั้ง Flow
- **Timeout Handling**: จัดการเมื่อเกิด Timeout

---

## ตัวอย่างการใช้งาน

### ตัวอย่าง 1: If–Else Condition
```
Condition: amount > 1000
  Yes: Send Approval Email
  No: Auto Approve
```

### ตัวอย่าง 2: Switch Condition
```
Switch: status
  Case "Pending": Send Notification
  Case "Approved": Process Order
  Case "Rejected": Send Rejection Email
  Default: Log Error
```

### ตัวอย่าง 3: Apply to Each with Filter
```
1. Filter Array: items where status = "Active"
2. Apply to Each: Process each active item
3. Update Status: Mark as processed
```

### ตัวอย่าง 4: Do Until Pattern
```
Do Until: response.status = "completed"
  - Call API
  - Delay 5 seconds
  - Check Status
Timeout: 5 minutes
```

### ตัวอย่าง 5: Parallel Branching
```
Parallel:
  Branch 1: Get User Data
  Branch 2: Get Order Data
  Branch 3: Get Payment Data
Then: Combine Results
```

### ตัวอย่าง 6: Scope with Error Handling
```
Scope: Process Order
  Try:
    - Validate Order
    - Calculate Total
    - Create Order
  Catch:
    - Log Error
    - Send Notification
```

---

## Advanced Patterns

### Pattern 1: Retry Pattern
```
Initialize Variable: retryCount = 0
Do Until: success = true OR retryCount > 3
  - Try Action
  - If Failed: Increment retryCount, Delay 10 seconds
```

### Pattern 2: Rate Limiting
```
Apply to Each: items
  - Process Item
  - Delay 1 second (Rate Limit)
```

### Pattern 3: Batch Processing
```
Initialize Variable: batchSize = 10
Apply to Each: items
  - Add to Batch
  - If Batch Full: Process Batch, Clear Batch
```

### Pattern 4: Conditional Loop
```
Apply to Each: items
  Condition: item.status = "Active"
    Yes: Process Item
    No: Skip Item
```

---

## Performance Optimization

### 1. ลด Nested Loops
- ใช้ Filter Array ก่อน Loop
- ใช้ Select เพื่อ Transform Data
- ใช้ Parallel Processing เมื่อเป็นไปได้

### 2. Concurrency Control
- ตั้งค่า Concurrency สำหรับ Apply to Each
- ใช้ Parallel Branches สำหรับ Independent Tasks
- ระวัง Rate Limits ของ External APIs

### 3. Timeout Management
- ตั้งค่า Timeout ที่เหมาะสม
- ใช้ Retry Logic สำหรับ Transient Errors
- Monitor และ Log Timeout Events

---

## สรุปบทเรียน

หลังจากจบบทนี้ คุณจะสามารถ:
- ✅ ใช้ If–Else และ Switch Conditions ได้อย่างมีประสิทธิภาพ
- ✅ สร้าง Nested Conditions ที่ซับซ้อน
- ✅ ใช้ Apply to Each และ Do Until Loops
- ✅ จัดกลุ่ม Actions ด้วย Scope
- ✅ สร้าง Parallel Branches
- ✅ ใช้ Delay และ Timeout Patterns
- ✅ ปรับปรุง Performance ของ Flow

---

## แบบฝึกหัดเพิ่มเติม

1. สร้าง Flow ที่ใช้ Switch Condition สำหรับหลาย Case
2. สร้าง Flow ที่ใช้ Do Until เพื่อรอ Response จาก API
3. สร้าง Flow ที่ใช้ Parallel Branching เพื่อเรียก API หลายตัวพร้อมกัน
4. สร้าง Flow ที่ใช้ Scope เพื่อแยก Error Handling
5. สร้าง Flow ที่ใช้ Retry Pattern สำหรับการเรียก API

